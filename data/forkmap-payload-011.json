{"schema":"gocar.forkmap.payload.v1","dataset_synced_at":"2026-09-09T22:19:25+00:00","dataset_schema":"gocar.contract.v0","from":1408,"items":[{"k":"fn:BiometricAuthBuilder::build_checked","d":{"4:3":"Build the validated prompt reason and availability policy.","4:4":"Build the validated prompt reason and availability policy.","4:5":"Build the validated prompt reason and availability policy.","4:6":"Build the validated prompt reason and availability policy."},"s":{"4:3":"fn BiometricAuthBuilder::build_checked(self)-> Result < (String , bool) >","4:4":"fn BiometricAuthBuilder::build_checked(self)-> Result < (String , bool) >","4:5":"fn BiometricAuthBuilder::build_checked(self)-> Result < (String , bool) >","4:6":"fn BiometricAuthBuilder::build_checked(self)-> Result < (String , bool) >"},"l":{"4:3":[["app.rs",18422,18426]],"4:4":[["app.rs",18422,18426]],"4:5":[["app.rs",18423,18427]],"4:6":[["app.rs",18423,18427]]}},{"k":"fn:BiometricAuthBuilder::new","d":{"4:3":"Create a biometric prompt with the user-facing reason shown by the OS.","4:4":"Create a biometric prompt with the user-facing reason shown by the OS.","4:5":"Create a biometric prompt with the user-facing reason shown by the OS.","4:6":"Create a biometric prompt with the user-facing reason shown by the OS."},"s":{"4:3":"fn BiometricAuthBuilder::new(impl Into < String >)-> Self","4:4":"fn BiometricAuthBuilder::new(impl Into < String >)-> Self","4:5":"fn BiometricAuthBuilder::new(impl Into < String >)-> Self","4:6":"fn BiometricAuthBuilder::new(impl Into < String >)-> Self"},"l":{"4:3":[["app.rs",18366,18372]],"4:4":[["app.rs",18366,18372]],"4:5":[["app.rs",18367,18373]],"4:6":[["app.rs",18367,18373]]}},{"k":"fn:BiometricAuthBuilder::reason","d":{"4:3":"Return the configured prompt reason.","4:4":"Return the configured prompt reason.","4:5":"Return the configured prompt reason.","4:6":"Return the configured prompt reason."},"s":{"4:3":"fn BiometricAuthBuilder::reason(& self)-> & str","4:4":"fn BiometricAuthBuilder::reason(& self)-> & str","4:5":"fn BiometricAuthBuilder::reason(& self)-> & str","4:6":"fn BiometricAuthBuilder::reason(& self)-> & str"},"l":{"4:3":[["app.rs",18391,18394]],"4:4":[["app.rs",18391,18394]],"4:5":[["app.rs",18392,18395]],"4:6":[["app.rs",18392,18395]]}},{"k":"fn:BiometricAuthBuilder::requires_available","d":{"4:3":"Return whether this prompt requires an available biometric method.","4:4":"Return whether this prompt requires an available biometric method.","4:5":"Return whether this prompt requires an available biometric method.","4:6":"Return whether this prompt requires an available biometric method."},"s":{"4:3":"fn BiometricAuthBuilder::requires_available(& self)-> bool","4:4":"fn BiometricAuthBuilder::requires_available(& self)-> bool","4:5":"fn BiometricAuthBuilder::requires_available(& self)-> bool","4:6":"fn BiometricAuthBuilder::requires_available(& self)-> bool"},"l":{"4:3":[["app.rs",18396,18399]],"4:4":[["app.rs",18396,18399]],"4:5":[["app.rs",18397,18400]],"4:6":[["app.rs",18397,18400]]}},{"k":"fn:BiometricAuthBuilder::unlock_vault","d":{"4:3":"Create a common vault-unlock biometric prompt.","4:4":"Create a common vault-unlock biometric prompt.","4:5":"Create a common vault-unlock biometric prompt.","4:6":"Create a common vault-unlock biometric prompt."},"s":{"4:3":"fn BiometricAuthBuilder::unlock_vault()-> Self","4:4":"fn BiometricAuthBuilder::unlock_vault()-> Self","4:5":"fn BiometricAuthBuilder::unlock_vault()-> Self","4:6":"fn BiometricAuthBuilder::unlock_vault()-> Self"},"l":{"4:3":[["app.rs",18374,18377]],"4:4":[["app.rs",18374,18377]],"4:5":[["app.rs",18375,18378]],"4:6":[["app.rs",18375,18378]]}},{"k":"fn:BiometricAuthBuilder::validate","d":{"4:3":"Validate the prompt copy before asking the platform to show UI.","4:4":"Validate the prompt copy before asking the platform to show UI.","4:5":"Validate the prompt copy before asking the platform to show UI.","4:6":"Validate the prompt copy before asking the platform to show UI."},"s":{"4:3":"fn BiometricAuthBuilder::validate(& self)-> Result < () >","4:4":"fn BiometricAuthBuilder::validate(& self)-> Result < () >","4:5":"fn BiometricAuthBuilder::validate(& self)-> Result < () >","4:6":"fn BiometricAuthBuilder::validate(& self)-> Result < () >"},"l":{"4:3":[["app.rs",18401,18420]],"4:4":[["app.rs",18401,18420]],"4:5":[["app.rs",18402,18421]],"4:6":[["app.rs",18402,18421]]}},{"k":"fn:BiometricAuthRequest::available","d":{"4:3":"Whether biometric authentication was available before the request.","4:4":"Whether biometric authentication was available before the request.","4:5":"Whether biometric authentication was available before the request.","4:6":"Whether biometric authentication was available before the request."},"s":{"4:3":"fn BiometricAuthRequest::available(& self)-> bool","4:4":"fn BiometricAuthRequest::available(& self)-> bool","4:5":"fn BiometricAuthRequest::available(& self)-> bool","4:6":"fn BiometricAuthRequest::available(& self)-> bool"},"l":{"4:3":[["app.rs",18453,18456]],"4:4":[["app.rs",18453,18456]],"4:5":[["app.rs",18454,18457]],"4:6":[["app.rs",18454,18457]]}},{"k":"fn:BiometricAuthRequest::kind","d":{"4:3":"The available biometric method, if one was detected.","4:4":"The available biometric method, if one was detected.","4:5":"The available biometric method, if one was detected.","4:6":"The available biometric method, if one was detected."},"s":{"4:3":"fn BiometricAuthRequest::kind(& self)-> Option < BiometricKind >","4:4":"fn BiometricAuthRequest::kind(& self)-> Option < BiometricKind >","4:5":"fn BiometricAuthRequest::kind(& self)-> Option < BiometricKind >","4:6":"fn BiometricAuthRequest::kind(& self)-> Option < BiometricKind >"},"l":{"4:3":[["app.rs",18458,18464]],"4:4":[["app.rs",18458,18464]],"4:5":[["app.rs",18459,18465]],"4:6":[["app.rs",18459,18465]]}},{"k":"fn:BiometricAuthRequest::prompted","d":{"4:3":"Whether Kael forwarded the prompt to the platform.","4:4":"Whether Kael forwarded the prompt to the platform.","4:5":"Whether Kael forwarded the prompt to the platform.","4:6":"Whether Kael forwarded the prompt to the platform."},"s":{"4:3":"fn BiometricAuthRequest::prompted(& self)-> bool","4:4":"fn BiometricAuthRequest::prompted(& self)-> bool","4:5":"fn BiometricAuthRequest::prompted(& self)-> bool","4:6":"fn BiometricAuthRequest::prompted(& self)-> bool"},"l":{"4:3":[["app.rs",18443,18446]],"4:4":[["app.rs",18443,18446]],"4:5":[["app.rs",18444,18447]],"4:6":[["app.rs",18444,18447]]}},{"k":"fn:BiometricAuthRequest::reason","d":{"4:3":"The validated reason used for the prompt.","4:4":"The validated reason used for the prompt.","4:5":"The validated reason used for the prompt.","4:6":"The validated reason used for the prompt."},"s":{"4:3":"fn BiometricAuthRequest::reason(& self)-> & str","4:4":"fn BiometricAuthRequest::reason(& self)-> & str","4:5":"fn BiometricAuthRequest::reason(& self)-> & str","4:6":"fn BiometricAuthRequest::reason(& self)-> & str"},"l":{"4:3":[["app.rs",18448,18451]],"4:4":[["app.rs",18448,18451]],"4:5":[["app.rs",18449,18452]],"4:6":[["app.rs",18449,18452]]}},{"k":"fn:BiometricAuthRequest::status","d":{"4:3":"The biometric availability observed before the request.","4:4":"The biometric availability observed before the request.","4:5":"The biometric availability observed before the request.","4:6":"The biometric availability observed before the request."},"s":{"4:3":"fn BiometricAuthRequest::status(& self)-> BiometricStatus","4:4":"fn BiometricAuthRequest::status(& self)-> BiometricStatus","4:5":"fn BiometricAuthRequest::status(& self)-> BiometricStatus","4:6":"fn BiometricAuthRequest::status(& self)-> BiometricStatus"},"l":{"4:3":[["app.rs",18438,18441]],"4:4":[["app.rs",18438,18441]],"4:5":[["app.rs",18439,18442]],"4:6":[["app.rs",18439,18442]]}},{"k":"fn:BiometricAuthRequest::to_text","d":{"4:3":"Human-readable, deterministic summary for fallback UI, logs, and agents.","4:4":"Human-readable, deterministic summary for fallback UI, logs, and agents.","4:5":"Human-readable, deterministic summary for fallback UI, logs, and agents.","4:6":"Human-readable, deterministic summary for fallback UI, logs, and agents."},"s":{"4:3":"fn BiometricAuthRequest::to_text(& self)-> String","4:4":"fn BiometricAuthRequest::to_text(& self)-> String","4:5":"fn BiometricAuthRequest::to_text(& self)-> String","4:6":"fn BiometricAuthRequest::to_text(& self)-> String"},"l":{"4:3":[["app.rs",18466,18483]],"4:4":[["app.rs",18466,18483]],"4:5":[["app.rs",18467,18484]],"4:6":[["app.rs",18467,18484]]}},{"k":"fn:Bounds::bottom","d":{"0:1":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","0:2":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","0:3":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","0:4":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","0:5":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","0:6":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","1:0":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","1:1":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","1:2":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:0":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:1":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:10":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:11":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:12":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:13":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:14":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:15":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:16":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:17":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:18":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:19":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:2":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:20":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:21":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:22":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:23":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:24":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:25":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:26":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:27":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:28":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:29":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:3":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:30":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:31":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:32":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:33":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:34":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:35":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:36":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:37":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:38":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:39":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:4":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:40":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:41":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:42":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:43":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:44":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:45":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:46":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:5":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:6":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:7":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:8":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","2:9":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","3:0":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","3:1":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","3:2":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","3:3":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","3:4":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","4:0":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","4:1":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","4:2":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","4:3":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","4:4":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","4:5":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","4:6":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","5:0":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds.","5:1":"Returns the bottom edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the bottom edge of the bounds."},"s":{"0:1":"fn Bounds::bottom(& self)-> T","0:2":"fn Bounds::bottom(& self)-> T","0:3":"fn Bounds::bottom(& self)-> T","0:4":"fn Bounds::bottom(& self)-> T","0:5":"fn Bounds::bottom(& self)-> T","0:6":"fn Bounds::bottom(& self)-> T","1:0":"fn Bounds::bottom(& self)-> T","1:1":"fn Bounds::bottom(& self)-> T","1:2":"fn Bounds::bottom(& self)-> T","2:0":"fn Bounds::bottom(& self)-> T","2:1":"fn Bounds::bottom(& self)-> T","2:10":"fn Bounds::bottom(& self)-> T","2:11":"fn Bounds::bottom(& self)-> T","2:12":"fn Bounds::bottom(& self)-> T","2:13":"fn Bounds::bottom(& self)-> T","2:14":"fn Bounds::bottom(& self)-> T","2:15":"fn Bounds::bottom(& self)-> T","2:16":"fn Bounds::bottom(& self)-> T","2:17":"fn Bounds::bottom(& self)-> T","2:18":"fn Bounds::bottom(& self)-> T","2:19":"fn Bounds::bottom(& self)-> T","2:2":"fn Bounds::bottom(& self)-> T","2:20":"fn Bounds::bottom(& self)-> T","2:21":"fn Bounds::bottom(& self)-> T","2:22":"fn Bounds::bottom(& self)-> T","2:23":"fn Bounds::bottom(& self)-> T","2:24":"fn Bounds::bottom(& self)-> T","2:25":"fn Bounds::bottom(& self)-> T","2:26":"fn Bounds::bottom(& self)-> T","2:27":"fn Bounds::bottom(& self)-> T","2:28":"fn Bounds::bottom(& self)-> T","2:29":"fn Bounds::bottom(& self)-> T","2:3":"fn Bounds::bottom(& self)-> T","2:30":"fn Bounds::bottom(& self)-> T","2:31":"fn Bounds::bottom(& self)-> T","2:32":"fn Bounds::bottom(& self)-> T","2:33":"fn Bounds::bottom(& self)-> T","2:34":"fn Bounds::bottom(& self)-> T","2:35":"fn Bounds::bottom(& self)-> T","2:36":"fn Bounds::bottom(& self)-> T","2:37":"fn Bounds::bottom(& self)-> T","2:38":"fn Bounds::bottom(& self)-> T","2:39":"fn Bounds::bottom(& self)-> T","2:4":"fn Bounds::bottom(& self)-> T","2:40":"fn Bounds::bottom(& self)-> T","2:41":"fn Bounds::bottom(& self)-> T","2:42":"fn Bounds::bottom(& self)-> T","2:43":"fn Bounds::bottom(& self)-> T","2:44":"fn Bounds::bottom(& self)-> T","2:45":"fn Bounds::bottom(& self)-> T","2:46":"fn Bounds::bottom(& self)-> T","2:5":"fn Bounds::bottom(& self)-> T","2:6":"fn Bounds::bottom(& self)-> T","2:7":"fn Bounds::bottom(& self)-> T","2:8":"fn Bounds::bottom(& self)-> T","2:9":"fn Bounds::bottom(& self)-> T","3:0":"fn Bounds::bottom(& self)-> T","3:1":"fn Bounds::bottom(& self)-> T","3:2":"fn Bounds::bottom(& self)-> T","3:3":"fn Bounds::bottom(& self)-> T","3:4":"fn Bounds::bottom(& self)-> T","4:0":"fn Bounds::bottom(& self)-> T","4:1":"fn Bounds::bottom(& self)-> T","4:2":"fn Bounds::bottom(& self)-> T","4:3":"fn Bounds::bottom(& self)-> T","4:4":"fn Bounds::bottom(& self)-> T","4:5":"fn Bounds::bottom(& self)-> T","4:6":"fn Bounds::bottom(& self)-> T","5:0":"fn Bounds::bottom(& self)-> T","5:1":"fn Bounds::bottom(& self)-> T"},"l":{"0:1":[["geometry.rs",1258,1265]],"0:2":[["geometry.rs",1258,1265]],"0:3":[["geometry.rs",1258,1265]],"0:4":[["geometry.rs",1258,1265]],"0:5":[["geometry.rs",1258,1265]],"0:6":[["geometry.rs",1258,1265]],"1:0":[["geometry.rs",1258,1265]],"1:1":[["geometry.rs",1258,1265]],"1:2":[["geometry.rs",1312,1319]],"2:0":[["geometry.rs",1239,1246]],"2:1":[["geometry.rs",1239,1246]],"2:10":[["geometry.rs",1239,1246]],"2:11":[["geometry.rs",1239,1246]],"2:12":[["geometry.rs",1297,1304]],"2:13":[["geometry.rs",1297,1304]],"2:14":[["geometry.rs",1297,1304]],"2:15":[["geometry.rs",1297,1304]],"2:16":[["geometry.rs",1297,1304]],"2:17":[["geometry.rs",1297,1304]],"2:18":[["geometry.rs",1297,1304]],"2:19":[["geometry.rs",1297,1304]],"2:2":[["geometry.rs",1239,1246]],"2:20":[["geometry.rs",1297,1304]],"2:21":[["geometry.rs",1297,1304]],"2:22":[["geometry.rs",1297,1304]],"2:23":[["geometry.rs",1297,1304]],"2:24":[["geometry.rs",1297,1304]],"2:25":[["geometry.rs",1297,1304]],"2:26":[["geometry.rs",1297,1304]],"2:27":[["geometry.rs",1297,1304]],"2:28":[["geometry.rs",1297,1304]],"2:29":[["geometry.rs",1297,1304]],"2:3":[["geometry.rs",1239,1246]],"2:30":[["geometry.rs",1297,1304]],"2:31":[["geometry.rs",1297,1304]],"2:32":[["geometry.rs",1297,1304]],"2:33":[["geometry.rs",1297,1304]],"2:34":[["geometry.rs",1297,1304]],"2:35":[["geometry.rs",1297,1304]],"2:36":[["geometry.rs",1297,1304]],"2:37":[["geometry.rs",1297,1304]],"2:38":[["geometry.rs",1297,1304]],"2:39":[["geometry.rs",1297,1304]],"2:4":[["geometry.rs",1239,1246]],"2:40":[["geometry.rs",1297,1304]],"2:41":[["geometry.rs",1297,1304]],"2:42":[["geometry.rs",1297,1304]],"2:43":[["geometry.rs",1297,1304]],"2:44":[["geometry.rs",1297,1304]],"2:45":[["geometry.rs",1297,1304]],"2:46":[["geometry.rs",1297,1304]],"2:5":[["geometry.rs",1239,1246]],"2:6":[["geometry.rs",1239,1246]],"2:7":[["geometry.rs",1239,1246]],"2:8":[["geometry.rs",1239,1246]],"2:9":[["geometry.rs",1239,1246]],"3:0":[["geometry.rs",1297,1304]],"3:1":[["geometry.rs",1297,1304]],"3:2":[["geometry.rs",1297,1304]],"3:3":[["geometry.rs",1297,1304]],"3:4":[["geometry.rs",1297,1304]],"4:0":[["geometry.rs",1271,1278]],"4:1":[["geometry.rs",1271,1278]],"4:2":[["geometry.rs",1271,1278]],"4:3":[["geometry.rs",1271,1278]],"4:4":[["geometry.rs",1271,1278]],"4:5":[["geometry.rs",1271,1278]],"4:6":[["geometry.rs",1271,1278]],"5:0":[["geometry.rs",1297,1304]],"5:1":[["geometry.rs",1297,1304]]}},{"k":"fn:Bounds::bottom_center","d":{"1:2":"Returns the bottom center point of the bounds.","2:12":"Returns the bottom center point of the bounds.","2:13":"Returns the bottom center point of the bounds.","2:14":"Returns the bottom center point of the bounds.","2:15":"Returns the bottom center point of the bounds.","2:16":"Returns the bottom center point of the bounds.","2:17":"Returns the bottom center point of the bounds.","2:18":"Returns the bottom center point of the bounds.","2:19":"Returns the bottom center point of the bounds.","2:20":"Returns the bottom center point of the bounds.","2:21":"Returns the bottom center point of the bounds.","2:22":"Returns the bottom center point of the bounds.","2:23":"Returns the bottom center point of the bounds.","2:24":"Returns the bottom center point of the bounds.","2:25":"Returns the bottom center point of the bounds.","2:26":"Returns the bottom center point of the bounds.","2:27":"Returns the bottom center point of the bounds.","2:28":"Returns the bottom center point of the bounds.","2:29":"Returns the bottom center point of the bounds.","2:30":"Returns the bottom center point of the bounds.","2:31":"Returns the bottom center point of the bounds.","2:32":"Returns the bottom center point of the bounds.","2:33":"Returns the bottom center point of the bounds.","2:34":"Returns the bottom center point of the bounds.","2:35":"Returns the bottom center point of the bounds.","2:36":"Returns the bottom center point of the bounds.","2:37":"Returns the bottom center point of the bounds.","2:38":"Returns the bottom center point of the bounds.","2:39":"Returns the bottom center point of the bounds.","2:40":"Returns the bottom center point of the bounds.","2:41":"Returns the bottom center point of the bounds.","2:42":"Returns the bottom center point of the bounds.","2:43":"Returns the bottom center point of the bounds.","2:44":"Returns the bottom center point of the bounds.","2:45":"Returns the bottom center point of the bounds.","2:46":"Returns the bottom center point of the bounds.","3:0":"Returns the bottom center point of the bounds.","3:1":"Returns the bottom center point of the bounds.","3:2":"Returns the bottom center point of the bounds.","3:3":"Returns the bottom center point of the bounds.","3:4":"Returns the bottom center point of the bounds.","5:0":"Returns the bottom center point of the bounds.","5:1":"Returns the bottom center point of the bounds."},"s":{"1:2":"fn Bounds::bottom_center(& self)-> Point < T >","2:12":"fn Bounds::bottom_center(& self)-> Point < T >","2:13":"fn Bounds::bottom_center(& self)-> Point < T >","2:14":"fn Bounds::bottom_center(& self)-> Point < T >","2:15":"fn Bounds::bottom_center(& self)-> Point < T >","2:16":"fn Bounds::bottom_center(& self)-> Point < T >","2:17":"fn Bounds::bottom_center(& self)-> Point < T >","2:18":"fn Bounds::bottom_center(& self)-> Point < T >","2:19":"fn Bounds::bottom_center(& self)-> Point < T >","2:20":"fn Bounds::bottom_center(& self)-> Point < T >","2:21":"fn Bounds::bottom_center(& self)-> Point < T >","2:22":"fn Bounds::bottom_center(& self)-> Point < T >","2:23":"fn Bounds::bottom_center(& self)-> Point < T >","2:24":"fn Bounds::bottom_center(& self)-> Point < T >","2:25":"fn Bounds::bottom_center(& self)-> Point < T >","2:26":"fn Bounds::bottom_center(& self)-> Point < T >","2:27":"fn Bounds::bottom_center(& self)-> Point < T >","2:28":"fn Bounds::bottom_center(& self)-> Point < T >","2:29":"fn Bounds::bottom_center(& self)-> Point < T >","2:30":"fn Bounds::bottom_center(& self)-> Point < T >","2:31":"fn Bounds::bottom_center(& self)-> Point < T >","2:32":"fn Bounds::bottom_center(& self)-> Point < T >","2:33":"fn Bounds::bottom_center(& self)-> Point < T >","2:34":"fn Bounds::bottom_center(& self)-> Point < T >","2:35":"fn Bounds::bottom_center(& self)-> Point < T >","2:36":"fn Bounds::bottom_center(& self)-> Point < T >","2:37":"fn Bounds::bottom_center(& self)-> Point < T >","2:38":"fn Bounds::bottom_center(& self)-> Point < T >","2:39":"fn Bounds::bottom_center(& self)-> Point < T >","2:40":"fn Bounds::bottom_center(& self)-> Point < T >","2:41":"fn Bounds::bottom_center(& self)-> Point < T >","2:42":"fn Bounds::bottom_center(& self)-> Point < T >","2:43":"fn Bounds::bottom_center(& self)-> Point < T >","2:44":"fn Bounds::bottom_center(& self)-> Point < T >","2:45":"fn Bounds::bottom_center(& self)-> Point < T >","2:46":"fn Bounds::bottom_center(& self)-> Point < T >","3:0":"fn Bounds::bottom_center(& self)-> Point < T >","3:1":"fn Bounds::bottom_center(& self)-> Point < T >","3:2":"fn Bounds::bottom_center(& self)-> Point < T >","3:3":"fn Bounds::bottom_center(& self)-> Point < T >","3:4":"fn Bounds::bottom_center(& self)-> Point < T >","5:0":"fn Bounds::bottom_center(& self)-> Point < T >","5:1":"fn Bounds::bottom_center(& self)-> Point < T >"},"l":{"1:2":[["geometry.rs",915,921]],"2:12":[["geometry.rs",900,906]],"2:13":[["geometry.rs",900,906]],"2:14":[["geometry.rs",900,906]],"2:15":[["geometry.rs",900,906]],"2:16":[["geometry.rs",900,906]],"2:17":[["geometry.rs",900,906]],"2:18":[["geometry.rs",900,906]],"2:19":[["geometry.rs",900,906]],"2:20":[["geometry.rs",900,906]],"2:21":[["geometry.rs",900,906]],"2:22":[["geometry.rs",900,906]],"2:23":[["geometry.rs",900,906]],"2:24":[["geometry.rs",900,906]],"2:25":[["geometry.rs",900,906]],"2:26":[["geometry.rs",900,906]],"2:27":[["geometry.rs",900,906]],"2:28":[["geometry.rs",900,906]],"2:29":[["geometry.rs",900,906]],"2:30":[["geometry.rs",900,906]],"2:31":[["geometry.rs",900,906]],"2:32":[["geometry.rs",900,906]],"2:33":[["geometry.rs",900,906]],"2:34":[["geometry.rs",900,906]],"2:35":[["geometry.rs",900,906]],"2:36":[["geometry.rs",900,906]],"2:37":[["geometry.rs",900,906]],"2:38":[["geometry.rs",900,906]],"2:39":[["geometry.rs",900,906]],"2:40":[["geometry.rs",900,906]],"2:41":[["geometry.rs",900,906]],"2:42":[["geometry.rs",900,906]],"2:43":[["geometry.rs",900,906]],"2:44":[["geometry.rs",900,906]],"2:45":[["geometry.rs",900,906]],"2:46":[["geometry.rs",900,906]],"3:0":[["geometry.rs",900,906]],"3:1":[["geometry.rs",900,906]],"3:2":[["geometry.rs",900,906]],"3:3":[["geometry.rs",900,906]],"3:4":[["geometry.rs",900,906]],"5:0":[["geometry.rs",900,906]],"5:1":[["geometry.rs",900,906]]}},{"k":"fn:Bounds::bottom_left","d":{"0:1":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","0:2":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","0:3":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","0:4":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","0:5":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","0:6":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","1:0":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","1:1":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","1:2":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:0":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:1":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:10":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:11":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:12":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:13":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:14":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:15":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:16":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:17":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:18":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:19":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:2":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:20":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:21":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:22":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:23":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:24":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:25":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:26":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:27":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:28":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:29":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:3":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:30":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:31":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:32":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:33":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:34":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:35":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:36":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:37":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:38":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:39":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:4":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:40":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:41":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:42":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:43":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:44":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:45":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:46":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:5":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:6":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:7":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:8":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:9":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","3:0":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","3:1":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","3:2":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","3:3":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","3:4":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","4:0":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","4:1":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","4:2":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","4:3":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","4:4":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","4:5":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","4:6":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","5:0":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","5:1":"Returns the bottom left corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom left corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.bottom_left();\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```"},"s":{"0:1":"fn Bounds::bottom_left(& self)-> Point < T >","0:2":"fn Bounds::bottom_left(& self)-> Point < T >","0:3":"fn Bounds::bottom_left(& self)-> Point < T >","0:4":"fn Bounds::bottom_left(& self)-> Point < T >","0:5":"fn Bounds::bottom_left(& self)-> Point < T >","0:6":"fn Bounds::bottom_left(& self)-> Point < T >","1:0":"fn Bounds::bottom_left(& self)-> Point < T >","1:1":"fn Bounds::bottom_left(& self)-> Point < T >","1:2":"fn Bounds::bottom_left(& self)-> Point < T >","2:0":"fn Bounds::bottom_left(& self)-> Point < T >","2:1":"fn Bounds::bottom_left(& self)-> Point < T >","2:10":"fn Bounds::bottom_left(& self)-> Point < T >","2:11":"fn Bounds::bottom_left(& self)-> Point < T >","2:12":"fn Bounds::bottom_left(& self)-> Point < T >","2:13":"fn Bounds::bottom_left(& self)-> Point < T >","2:14":"fn Bounds::bottom_left(& self)-> Point < T >","2:15":"fn Bounds::bottom_left(& self)-> Point < T >","2:16":"fn Bounds::bottom_left(& self)-> Point < T >","2:17":"fn Bounds::bottom_left(& self)-> Point < T >","2:18":"fn Bounds::bottom_left(& self)-> Point < T >","2:19":"fn Bounds::bottom_left(& self)-> Point < T >","2:2":"fn Bounds::bottom_left(& self)-> Point < T >","2:20":"fn Bounds::bottom_left(& self)-> Point < T >","2:21":"fn Bounds::bottom_left(& self)-> Point < T >","2:22":"fn Bounds::bottom_left(& self)-> Point < T >","2:23":"fn Bounds::bottom_left(& self)-> Point < T >","2:24":"fn Bounds::bottom_left(& self)-> Point < T >","2:25":"fn Bounds::bottom_left(& self)-> Point < T >","2:26":"fn Bounds::bottom_left(& self)-> Point < T >","2:27":"fn Bounds::bottom_left(& self)-> Point < T >","2:28":"fn Bounds::bottom_left(& self)-> Point < T >","2:29":"fn Bounds::bottom_left(& self)-> Point < T >","2:3":"fn Bounds::bottom_left(& self)-> Point < T >","2:30":"fn Bounds::bottom_left(& self)-> Point < T >","2:31":"fn Bounds::bottom_left(& self)-> Point < T >","2:32":"fn Bounds::bottom_left(& self)-> Point < T >","2:33":"fn Bounds::bottom_left(& self)-> Point < T >","2:34":"fn Bounds::bottom_left(& self)-> Point < T >","2:35":"fn Bounds::bottom_left(& self)-> Point < T >","2:36":"fn Bounds::bottom_left(& self)-> Point < T >","2:37":"fn Bounds::bottom_left(& self)-> Point < T >","2:38":"fn Bounds::bottom_left(& self)-> Point < T >","2:39":"fn Bounds::bottom_left(& self)-> Point < T >","2:4":"fn Bounds::bottom_left(& self)-> Point < T >","2:40":"fn Bounds::bottom_left(& self)-> Point < T >","2:41":"fn Bounds::bottom_left(& self)-> Point < T >","2:42":"fn Bounds::bottom_left(& self)-> Point < T >","2:43":"fn Bounds::bottom_left(& self)-> Point < T >","2:44":"fn Bounds::bottom_left(& self)-> Point < T >","2:45":"fn Bounds::bottom_left(& self)-> Point < T >","2:46":"fn Bounds::bottom_left(& self)-> Point < T >","2:5":"fn Bounds::bottom_left(& self)-> Point < T >","2:6":"fn Bounds::bottom_left(& self)-> Point < T >","2:7":"fn Bounds::bottom_left(& self)-> Point < T >","2:8":"fn Bounds::bottom_left(& self)-> Point < T >","2:9":"fn Bounds::bottom_left(& self)-> Point < T >","3:0":"fn Bounds::bottom_left(& self)-> Point < T >","3:1":"fn Bounds::bottom_left(& self)-> Point < T >","3:2":"fn Bounds::bottom_left(& self)-> Point < T >","3:3":"fn Bounds::bottom_left(& self)-> Point < T >","3:4":"fn Bounds::bottom_left(& self)-> Point < T >","4:0":"fn Bounds::bottom_left(& self)-> Point < T >","4:1":"fn Bounds::bottom_left(& self)-> Point < T >","4:2":"fn Bounds::bottom_left(& self)-> Point < T >","4:3":"fn Bounds::bottom_left(& self)-> Point < T >","4:4":"fn Bounds::bottom_left(& self)-> Point < T >","4:5":"fn Bounds::bottom_left(& self)-> Point < T >","4:6":"fn Bounds::bottom_left(& self)-> Point < T >","5:0":"fn Bounds::bottom_left(& self)-> Point < T >","5:1":"fn Bounds::bottom_left(& self)-> Point < T >"},"l":{"0:1":[["geometry.rs",1333,1355]],"0:2":[["geometry.rs",1333,1355]],"0:3":[["geometry.rs",1333,1355]],"0:4":[["geometry.rs",1333,1355]],"0:5":[["geometry.rs",1333,1355]],"0:6":[["geometry.rs",1333,1355]],"1:0":[["geometry.rs",1333,1355]],"1:1":[["geometry.rs",1333,1355]],"1:2":[["geometry.rs",1387,1409]],"2:0":[["geometry.rs",1314,1336]],"2:1":[["geometry.rs",1314,1336]],"2:10":[["geometry.rs",1314,1336]],"2:11":[["geometry.rs",1314,1336]],"2:12":[["geometry.rs",1372,1394]],"2:13":[["geometry.rs",1372,1394]],"2:14":[["geometry.rs",1372,1394]],"2:15":[["geometry.rs",1372,1394]],"2:16":[["geometry.rs",1372,1394]],"2:17":[["geometry.rs",1372,1394]],"2:18":[["geometry.rs",1372,1394]],"2:19":[["geometry.rs",1372,1394]],"2:2":[["geometry.rs",1314,1336]],"2:20":[["geometry.rs",1372,1394]],"2:21":[["geometry.rs",1372,1394]],"2:22":[["geometry.rs",1372,1394]],"2:23":[["geometry.rs",1372,1394]],"2:24":[["geometry.rs",1372,1394]],"2:25":[["geometry.rs",1372,1394]],"2:26":[["geometry.rs",1372,1394]],"2:27":[["geometry.rs",1372,1394]],"2:28":[["geometry.rs",1372,1394]],"2:29":[["geometry.rs",1372,1394]],"2:3":[["geometry.rs",1314,1336]],"2:30":[["geometry.rs",1372,1394]],"2:31":[["geometry.rs",1372,1394]],"2:32":[["geometry.rs",1372,1394]],"2:33":[["geometry.rs",1372,1394]],"2:34":[["geometry.rs",1372,1394]],"2:35":[["geometry.rs",1372,1394]],"2:36":[["geometry.rs",1372,1394]],"2:37":[["geometry.rs",1372,1394]],"2:38":[["geometry.rs",1372,1394]],"2:39":[["geometry.rs",1372,1394]],"2:4":[["geometry.rs",1314,1336]],"2:40":[["geometry.rs",1372,1394]],"2:41":[["geometry.rs",1372,1394]],"2:42":[["geometry.rs",1372,1394]],"2:43":[["geometry.rs",1372,1394]],"2:44":[["geometry.rs",1372,1394]],"2:45":[["geometry.rs",1372,1394]],"2:46":[["geometry.rs",1372,1394]],"2:5":[["geometry.rs",1314,1336]],"2:6":[["geometry.rs",1314,1336]],"2:7":[["geometry.rs",1314,1336]],"2:8":[["geometry.rs",1314,1336]],"2:9":[["geometry.rs",1314,1336]],"3:0":[["geometry.rs",1372,1394]],"3:1":[["geometry.rs",1372,1394]],"3:2":[["geometry.rs",1372,1394]],"3:3":[["geometry.rs",1372,1394]],"3:4":[["geometry.rs",1372,1394]],"4:0":[["geometry.rs",1346,1368]],"4:1":[["geometry.rs",1346,1368]],"4:2":[["geometry.rs",1346,1368]],"4:3":[["geometry.rs",1346,1368]],"4:4":[["geometry.rs",1346,1368]],"4:5":[["geometry.rs",1346,1368]],"4:6":[["geometry.rs",1346,1368]],"5:0":[["geometry.rs",1372,1394]],"5:1":[["geometry.rs",1372,1394]]}},{"k":"fn:Bounds::bottom_right","d":{"0:1":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","0:2":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","0:3":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","0:4":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","0:5":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","0:6":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","1:0":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","1:1":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","1:2":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:0":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:1":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:10":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:11":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:12":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:13":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:14":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:15":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:16":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:17":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:18":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:19":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:2":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:20":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:21":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:22":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:23":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:24":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:25":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:26":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:27":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:28":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:29":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:3":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:30":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:31":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:32":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:33":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:34":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:35":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:36":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:37":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:38":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:39":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:4":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:40":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:41":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:42":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:43":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:44":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:45":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:46":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:5":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:6":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:7":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:8":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","2:9":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","3:0":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","3:1":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","3:2":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","3:3":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","3:4":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","4:0":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","4:1":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","4:2":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","4:3":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","4:4":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","4:5":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","4:6":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","5:0":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```","5:1":"Returns the bottom right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the bottom right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_right = bounds.bottom_right();\nassert_eq!(bottom_right, Point { x: 10, y: 20 });\n```"},"s":{"0:1":"fn Bounds::bottom_right(& self)-> Point < T >","0:2":"fn Bounds::bottom_right(& self)-> Point < T >","0:3":"fn Bounds::bottom_right(& self)-> Point < T >","0:4":"fn Bounds::bottom_right(& self)-> Point < T >","0:5":"fn Bounds::bottom_right(& self)-> Point < T >","0:6":"fn Bounds::bottom_right(& self)-> Point < T >","1:0":"fn Bounds::bottom_right(& self)-> Point < T >","1:1":"fn Bounds::bottom_right(& self)-> Point < T >","1:2":"fn Bounds::bottom_right(& self)-> Point < T >","2:0":"fn Bounds::bottom_right(& self)-> Point < T >","2:1":"fn Bounds::bottom_right(& self)-> Point < T >","2:10":"fn Bounds::bottom_right(& self)-> Point < T >","2:11":"fn Bounds::bottom_right(& self)-> Point < T >","2:12":"fn Bounds::bottom_right(& self)-> Point < T >","2:13":"fn Bounds::bottom_right(& self)-> Point < T >","2:14":"fn Bounds::bottom_right(& self)-> Point < T >","2:15":"fn Bounds::bottom_right(& self)-> Point < T >","2:16":"fn Bounds::bottom_right(& self)-> Point < T >","2:17":"fn Bounds::bottom_right(& self)-> Point < T >","2:18":"fn Bounds::bottom_right(& self)-> Point < T >","2:19":"fn Bounds::bottom_right(& self)-> Point < T >","2:2":"fn Bounds::bottom_right(& self)-> Point < T >","2:20":"fn Bounds::bottom_right(& self)-> Point < T >","2:21":"fn Bounds::bottom_right(& self)-> Point < T >","2:22":"fn Bounds::bottom_right(& self)-> Point < T >","2:23":"fn Bounds::bottom_right(& self)-> Point < T >","2:24":"fn Bounds::bottom_right(& self)-> Point < T >","2:25":"fn Bounds::bottom_right(& self)-> Point < T >","2:26":"fn Bounds::bottom_right(& self)-> Point < T >","2:27":"fn Bounds::bottom_right(& self)-> Point < T >","2:28":"fn Bounds::bottom_right(& self)-> Point < T >","2:29":"fn Bounds::bottom_right(& self)-> Point < T >","2:3":"fn Bounds::bottom_right(& self)-> Point < T >","2:30":"fn Bounds::bottom_right(& self)-> Point < T >","2:31":"fn Bounds::bottom_right(& self)-> Point < T >","2:32":"fn Bounds::bottom_right(& self)-> Point < T >","2:33":"fn Bounds::bottom_right(& self)-> Point < T >","2:34":"fn Bounds::bottom_right(& self)-> Point < T >","2:35":"fn Bounds::bottom_right(& self)-> Point < T >","2:36":"fn Bounds::bottom_right(& self)-> Point < T >","2:37":"fn Bounds::bottom_right(& self)-> Point < T >","2:38":"fn Bounds::bottom_right(& self)-> Point < T >","2:39":"fn Bounds::bottom_right(& self)-> Point < T >","2:4":"fn Bounds::bottom_right(& self)-> Point < T >","2:40":"fn Bounds::bottom_right(& self)-> Point < T >","2:41":"fn Bounds::bottom_right(& self)-> Point < T >","2:42":"fn Bounds::bottom_right(& self)-> Point < T >","2:43":"fn Bounds::bottom_right(& self)-> Point < T >","2:44":"fn Bounds::bottom_right(& self)-> Point < T >","2:45":"fn Bounds::bottom_right(& self)-> Point < T >","2:46":"fn Bounds::bottom_right(& self)-> Point < T >","2:5":"fn Bounds::bottom_right(& self)-> Point < T >","2:6":"fn Bounds::bottom_right(& self)-> Point < T >","2:7":"fn Bounds::bottom_right(& self)-> Point < T >","2:8":"fn Bounds::bottom_right(& self)-> Point < T >","2:9":"fn Bounds::bottom_right(& self)-> Point < T >","3:0":"fn Bounds::bottom_right(& self)-> Point < T >","3:1":"fn Bounds::bottom_right(& self)-> Point < T >","3:2":"fn Bounds::bottom_right(& self)-> Point < T >","3:3":"fn Bounds::bottom_right(& self)-> Point < T >","3:4":"fn Bounds::bottom_right(& self)-> Point < T >","4:0":"fn Bounds::bottom_right(& self)-> Point < T >","4:1":"fn Bounds::bottom_right(& self)-> Point < T >","4:2":"fn Bounds::bottom_right(& self)-> Point < T >","4:3":"fn Bounds::bottom_right(& self)-> Point < T >","4:4":"fn Bounds::bottom_right(& self)-> Point < T >","4:5":"fn Bounds::bottom_right(& self)-> Point < T >","4:6":"fn Bounds::bottom_right(& self)-> Point < T >","5:0":"fn Bounds::bottom_right(& self)-> Point < T >","5:1":"fn Bounds::bottom_right(& self)-> Point < T >"},"l":{"0:1":[["geometry.rs",1309,1331]],"0:2":[["geometry.rs",1309,1331]],"0:3":[["geometry.rs",1309,1331]],"0:4":[["geometry.rs",1309,1331]],"0:5":[["geometry.rs",1309,1331]],"0:6":[["geometry.rs",1309,1331]],"1:0":[["geometry.rs",1309,1331]],"1:1":[["geometry.rs",1309,1331]],"1:2":[["geometry.rs",1363,1385]],"2:0":[["geometry.rs",1290,1312]],"2:1":[["geometry.rs",1290,1312]],"2:10":[["geometry.rs",1290,1312]],"2:11":[["geometry.rs",1290,1312]],"2:12":[["geometry.rs",1348,1370]],"2:13":[["geometry.rs",1348,1370]],"2:14":[["geometry.rs",1348,1370]],"2:15":[["geometry.rs",1348,1370]],"2:16":[["geometry.rs",1348,1370]],"2:17":[["geometry.rs",1348,1370]],"2:18":[["geometry.rs",1348,1370]],"2:19":[["geometry.rs",1348,1370]],"2:2":[["geometry.rs",1290,1312]],"2:20":[["geometry.rs",1348,1370]],"2:21":[["geometry.rs",1348,1370]],"2:22":[["geometry.rs",1348,1370]],"2:23":[["geometry.rs",1348,1370]],"2:24":[["geometry.rs",1348,1370]],"2:25":[["geometry.rs",1348,1370]],"2:26":[["geometry.rs",1348,1370]],"2:27":[["geometry.rs",1348,1370]],"2:28":[["geometry.rs",1348,1370]],"2:29":[["geometry.rs",1348,1370]],"2:3":[["geometry.rs",1290,1312]],"2:30":[["geometry.rs",1348,1370]],"2:31":[["geometry.rs",1348,1370]],"2:32":[["geometry.rs",1348,1370]],"2:33":[["geometry.rs",1348,1370]],"2:34":[["geometry.rs",1348,1370]],"2:35":[["geometry.rs",1348,1370]],"2:36":[["geometry.rs",1348,1370]],"2:37":[["geometry.rs",1348,1370]],"2:38":[["geometry.rs",1348,1370]],"2:39":[["geometry.rs",1348,1370]],"2:4":[["geometry.rs",1290,1312]],"2:40":[["geometry.rs",1348,1370]],"2:41":[["geometry.rs",1348,1370]],"2:42":[["geometry.rs",1348,1370]],"2:43":[["geometry.rs",1348,1370]],"2:44":[["geometry.rs",1348,1370]],"2:45":[["geometry.rs",1348,1370]],"2:46":[["geometry.rs",1348,1370]],"2:5":[["geometry.rs",1290,1312]],"2:6":[["geometry.rs",1290,1312]],"2:7":[["geometry.rs",1290,1312]],"2:8":[["geometry.rs",1290,1312]],"2:9":[["geometry.rs",1290,1312]],"3:0":[["geometry.rs",1348,1370]],"3:1":[["geometry.rs",1348,1370]],"3:2":[["geometry.rs",1348,1370]],"3:3":[["geometry.rs",1348,1370]],"3:4":[["geometry.rs",1348,1370]],"4:0":[["geometry.rs",1322,1344]],"4:1":[["geometry.rs",1322,1344]],"4:2":[["geometry.rs",1322,1344]],"4:3":[["geometry.rs",1322,1344]],"4:4":[["geometry.rs",1322,1344]],"4:5":[["geometry.rs",1322,1344]],"4:6":[["geometry.rs",1322,1344]],"5:0":[["geometry.rs",1348,1370]],"5:1":[["geometry.rs",1348,1370]]}},{"k":"fn:Bounds::center","d":{"0:1":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","0:2":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","0:3":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","0:4":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","0:5":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","0:6":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","1:0":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","1:1":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","1:2":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:0":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:1":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:10":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:11":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:12":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:13":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:14":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:15":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:16":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:17":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:18":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:19":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:2":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:20":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:21":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:22":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:23":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:24":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:25":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:26":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:27":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:28":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:29":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:3":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:30":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:31":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:32":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:33":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:34":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:35":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:36":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:37":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:38":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:39":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:4":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:40":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:41":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:42":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:43":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:44":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:45":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:46":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:5":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:6":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:7":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:8":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","2:9":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","3:0":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","3:1":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","3:2":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","3:3":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","3:4":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","4:0":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","4:1":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","4:2":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","4:3":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","4:4":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","4:5":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","4:6":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","5:0":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```","5:1":"Returns the center point of the bounds.\n\nCalculates the center by taking the origin's x and y coordinates and adding half the width and height\nof the bounds, respectively. The center is represented as a `Point<T>` where `T` is the type of the\ncoordinate system.\n\n# Returns\n\nA `Point<T>` representing the center of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet center = bounds.center();\nassert_eq!(center, Point { x: 5, y: 10 });\n```"},"s":{"0:1":"fn Bounds::center(& self)-> Point < T >","0:2":"fn Bounds::center(& self)-> Point < T >","0:3":"fn Bounds::center(& self)-> Point < T >","0:4":"fn Bounds::center(& self)-> Point < T >","0:5":"fn Bounds::center(& self)-> Point < T >","0:6":"fn Bounds::center(& self)-> Point < T >","1:0":"fn Bounds::center(& self)-> Point < T >","1:1":"fn Bounds::center(& self)-> Point < T >","1:2":"fn Bounds::center(& self)-> Point < T >","2:0":"fn Bounds::center(& self)-> Point < T >","2:1":"fn Bounds::center(& self)-> Point < T >","2:10":"fn Bounds::center(& self)-> Point < T >","2:11":"fn Bounds::center(& self)-> Point < T >","2:12":"fn Bounds::center(& self)-> Point < T >","2:13":"fn Bounds::center(& self)-> Point < T >","2:14":"fn Bounds::center(& self)-> Point < T >","2:15":"fn Bounds::center(& self)-> Point < T >","2:16":"fn Bounds::center(& self)-> Point < T >","2:17":"fn Bounds::center(& self)-> Point < T >","2:18":"fn Bounds::center(& self)-> Point < T >","2:19":"fn Bounds::center(& self)-> Point < T >","2:2":"fn Bounds::center(& self)-> Point < T >","2:20":"fn Bounds::center(& self)-> Point < T >","2:21":"fn Bounds::center(& self)-> Point < T >","2:22":"fn Bounds::center(& self)-> Point < T >","2:23":"fn Bounds::center(& self)-> Point < T >","2:24":"fn Bounds::center(& self)-> Point < T >","2:25":"fn Bounds::center(& self)-> Point < T >","2:26":"fn Bounds::center(& self)-> Point < T >","2:27":"fn Bounds::center(& self)-> Point < T >","2:28":"fn Bounds::center(& self)-> Point < T >","2:29":"fn Bounds::center(& self)-> Point < T >","2:3":"fn Bounds::center(& self)-> Point < T >","2:30":"fn Bounds::center(& self)-> Point < T >","2:31":"fn Bounds::center(& self)-> Point < T >","2:32":"fn Bounds::center(& self)-> Point < T >","2:33":"fn Bounds::center(& self)-> Point < T >","2:34":"fn Bounds::center(& self)-> Point < T >","2:35":"fn Bounds::center(& self)-> Point < T >","2:36":"fn Bounds::center(& self)-> Point < T >","2:37":"fn Bounds::center(& self)-> Point < T >","2:38":"fn Bounds::center(& self)-> Point < T >","2:39":"fn Bounds::center(& self)-> Point < T >","2:4":"fn Bounds::center(& self)-> Point < T >","2:40":"fn Bounds::center(& self)-> Point < T >","2:41":"fn Bounds::center(& self)-> Point < T >","2:42":"fn Bounds::center(& self)-> Point < T >","2:43":"fn Bounds::center(& self)-> Point < T >","2:44":"fn Bounds::center(& self)-> Point < T >","2:45":"fn Bounds::center(& self)-> Point < T >","2:46":"fn Bounds::center(& self)-> Point < T >","2:5":"fn Bounds::center(& self)-> Point < T >","2:6":"fn Bounds::center(& self)-> Point < T >","2:7":"fn Bounds::center(& self)-> Point < T >","2:8":"fn Bounds::center(& self)-> Point < T >","2:9":"fn Bounds::center(& self)-> Point < T >","3:0":"fn Bounds::center(& self)-> Point < T >","3:1":"fn Bounds::center(& self)-> Point < T >","3:2":"fn Bounds::center(& self)-> Point < T >","3:3":"fn Bounds::center(& self)-> Point < T >","3:4":"fn Bounds::center(& self)-> Point < T >","4:0":"fn Bounds::center(& self)-> Point < T >","4:1":"fn Bounds::center(& self)-> Point < T >","4:2":"fn Bounds::center(& self)-> Point < T >","4:3":"fn Bounds::center(& self)-> Point < T >","4:4":"fn Bounds::center(& self)-> Point < T >","4:5":"fn Bounds::center(& self)-> Point < T >","4:6":"fn Bounds::center(& self)-> Point < T >","5:0":"fn Bounds::center(& self)-> Point < T >","5:1":"fn Bounds::center(& self)-> Point < T >"},"l":{"0:1":[["geometry.rs",950,976]],"0:2":[["geometry.rs",950,976]],"0:3":[["geometry.rs",950,976]],"0:4":[["geometry.rs",950,976]],"0:5":[["geometry.rs",950,976]],"0:6":[["geometry.rs",950,976]],"1:0":[["geometry.rs",950,976]],"1:1":[["geometry.rs",950,976]],"1:2":[["geometry.rs",992,1018]],"2:0":[["geometry.rs",919,945]],"2:1":[["geometry.rs",919,945]],"2:10":[["geometry.rs",919,945]],"2:11":[["geometry.rs",919,945]],"2:12":[["geometry.rs",977,1003]],"2:13":[["geometry.rs",977,1003]],"2:14":[["geometry.rs",977,1003]],"2:15":[["geometry.rs",977,1003]],"2:16":[["geometry.rs",977,1003]],"2:17":[["geometry.rs",977,1003]],"2:18":[["geometry.rs",977,1003]],"2:19":[["geometry.rs",977,1003]],"2:2":[["geometry.rs",919,945]],"2:20":[["geometry.rs",977,1003]],"2:21":[["geometry.rs",977,1003]],"2:22":[["geometry.rs",977,1003]],"2:23":[["geometry.rs",977,1003]],"2:24":[["geometry.rs",977,1003]],"2:25":[["geometry.rs",977,1003]],"2:26":[["geometry.rs",977,1003]],"2:27":[["geometry.rs",977,1003]],"2:28":[["geometry.rs",977,1003]],"2:29":[["geometry.rs",977,1003]],"2:3":[["geometry.rs",919,945]],"2:30":[["geometry.rs",977,1003]],"2:31":[["geometry.rs",977,1003]],"2:32":[["geometry.rs",977,1003]],"2:33":[["geometry.rs",977,1003]],"2:34":[["geometry.rs",977,1003]],"2:35":[["geometry.rs",977,1003]],"2:36":[["geometry.rs",977,1003]],"2:37":[["geometry.rs",977,1003]],"2:38":[["geometry.rs",977,1003]],"2:39":[["geometry.rs",977,1003]],"2:4":[["geometry.rs",919,945]],"2:40":[["geometry.rs",977,1003]],"2:41":[["geometry.rs",977,1003]],"2:42":[["geometry.rs",977,1003]],"2:43":[["geometry.rs",977,1003]],"2:44":[["geometry.rs",977,1003]],"2:45":[["geometry.rs",977,1003]],"2:46":[["geometry.rs",977,1003]],"2:5":[["geometry.rs",919,945]],"2:6":[["geometry.rs",919,945]],"2:7":[["geometry.rs",919,945]],"2:8":[["geometry.rs",919,945]],"2:9":[["geometry.rs",919,945]],"3:0":[["geometry.rs",977,1003]],"3:1":[["geometry.rs",977,1003]],"3:2":[["geometry.rs",977,1003]],"3:3":[["geometry.rs",977,1003]],"3:4":[["geometry.rs",977,1003]],"4:0":[["geometry.rs",950,976]],"4:1":[["geometry.rs",950,976]],"4:2":[["geometry.rs",950,976]],"4:3":[["geometry.rs",950,976]],"4:4":[["geometry.rs",950,976]],"4:5":[["geometry.rs",950,976]],"4:6":[["geometry.rs",950,976]],"5:0":[["geometry.rs",977,1003]],"5:1":[["geometry.rs",977,1003]]}},{"k":"fn:Bounds::centered","d":{"0:1":"Generate a centered bounds for the given display or primary display if none is provided","0:2":"Generate a centered bounds for the given display or primary display if none is provided","0:3":"Generate a centered bounds for the given display or primary display if none is provided","0:4":"Generate a centered bounds for the given display or primary display if none is provided","0:5":"Generate a centered bounds for the given display or primary display if none is provided","0:6":"Generate a centered bounds for the given display or primary display if none is provided","1:0":"Generate a centered bounds for the given display or primary display if none is provided","1:1":"Generate a centered bounds for the given display or primary display if none is provided","1:2":"Generate a centered bounds for the given display or primary display if none is provided","2:0":"Generate a centered bounds for the given display or primary display if none is provided","2:1":"Generate a centered bounds for the given display or primary display if none is provided","2:10":"Generate a centered bounds for the given display or primary display if none is provided","2:11":"Generate a centered bounds for the given display or primary display if none is provided","2:12":"Generate a centered bounds for the given display or primary display if none is provided","2:13":"Generate a centered bounds for the given display or primary display if none is provided","2:14":"Generate a centered bounds for the given display or primary display if none is provided","2:15":"Generate a centered bounds for the given display or primary display if none is provided","2:16":"Generate a centered bounds for the given display or primary display if none is provided","2:17":"Generate a centered bounds for the given display or primary display if none is provided","2:18":"Generate a centered bounds for the given display or primary display if none is provided","2:19":"Generate a centered bounds for the given display or primary display if none is provided","2:2":"Generate a centered bounds for the given display or primary display if none is provided","2:20":"Generate a centered bounds for the given display or primary display if none is provided","2:21":"Generate a centered bounds for the given display or primary display if none is provided","2:22":"Generate a centered bounds for the given display or primary display if none is provided","2:23":"Generate a centered bounds for the given display or primary display if none is provided","2:24":"Generate a centered bounds for the given display or primary display if none is provided","2:25":"Generate a centered bounds for the given display or primary display if none is provided","2:26":"Generate a centered bounds for the given display or primary display if none is provided","2:27":"Generate a centered bounds for the given display or primary display if none is provided","2:28":"Generate a centered bounds for the given display or primary display if none is provided","2:29":"Generate a centered bounds for the given display or primary display if none is provided","2:3":"Generate a centered bounds for the given display or primary display if none is provided","2:30":"Generate a centered bounds for the given display or primary display if none is provided","2:31":"Generate a centered bounds for the given display or primary display if none is provided","2:32":"Generate a centered bounds for the given display or primary display if none is provided","2:33":"Generate a centered bounds for the given display or primary display if none is provided","2:34":"Generate a centered bounds for the given display or primary display if none is provided","2:35":"Generate a centered bounds for the given display or primary display if none is provided","2:36":"Generate a centered bounds for the given display or primary display if none is provided","2:37":"Generate a centered bounds for the given display or primary display if none is provided","2:38":"Generate a centered bounds for the given display or primary display if none is provided","2:39":"Generate a centered bounds for the given display or primary display if none is provided","2:4":"Generate a centered bounds for the given display or primary display if none is provided","2:40":"Generate a centered bounds for the given display or primary display if none is provided","2:41":"Generate a centered bounds for the given display or primary display if none is provided","2:42":"Generate a centered bounds for the given display or primary display if none is provided","2:43":"Generate a centered bounds for the given display or primary display if none is provided","2:44":"Generate a centered bounds for the given display or primary display if none is provided","2:45":"Generate a centered bounds for the given display or primary display if none is provided","2:46":"Generate a centered bounds for the given display or primary display if none is provided","2:5":"Generate a centered bounds for the given display or primary display if none is provided","2:6":"Generate a centered bounds for the given display or primary display if none is provided","2:7":"Generate a centered bounds for the given display or primary display if none is provided","2:8":"Generate a centered bounds for the given display or primary display if none is provided","2:9":"Generate a centered bounds for the given display or primary display if none is provided","3:0":"Generate a centered bounds for the given display or primary display if none is provided","3:1":"Generate a centered bounds for the given display or primary display if none is provided","3:2":"Generate a centered bounds for the given display or primary display if none is provided","3:3":"Generate a centered bounds for the given display or primary display if none is provided","3:4":"Generate a centered bounds for the given display or primary display if none is provided","4:0":"Generate a centered bounds for the given display or primary display if none is provided","4:1":"Generate a centered bounds for the given display or primary display if none is provided","4:2":"Generate a centered bounds for the given display or primary display if none is provided","4:3":"Generate a centered bounds for the given display or primary display if none is provided","4:4":"Generate a centered bounds for the given display or primary display if none is provided","4:5":"Generate a centered bounds for the given display or primary display if none is provided","4:6":"Generate a centered bounds for the given display or primary display if none is provided","5:0":"Generate a centered bounds for the given display or primary display if none is provided","5:1":"Generate a centered bounds for the given display or primary display if none is provided"},"s":{"0:1":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","0:2":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","0:3":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","0:4":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","0:5":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","0:6":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","1:0":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","1:1":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","1:2":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:0":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:1":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:10":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:11":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:12":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:13":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:14":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:15":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:16":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:17":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:18":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:19":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:2":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:20":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:21":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:22":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:23":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:24":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:25":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:26":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:27":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:28":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:29":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:3":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:30":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:31":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:32":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:33":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:34":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:35":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:36":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:37":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:38":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:39":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:4":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:40":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:41":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:42":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:43":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:44":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:45":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:46":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:5":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:6":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:7":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:8":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","2:9":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","3:0":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","3:1":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","3:2":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","3:3":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","3:4":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","4:0":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","4:1":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","4:2":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","4:3":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","4:4":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","4:5":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","4:6":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","5:0":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self","5:1":"fn Bounds::centered(Option < DisplayId >, Size < Pixels >, & App)-> Self"},"l":{"0:1":[["geometry.rs",770,782]],"0:2":[["geometry.rs",770,782]],"0:3":[["geometry.rs",770,782]],"0:4":[["geometry.rs",770,782]],"0:5":[["geometry.rs",770,782]],"0:6":[["geometry.rs",770,782]],"1:0":[["geometry.rs",770,782]],"1:1":[["geometry.rs",770,782]],"1:2":[["geometry.rs",754,766]],"2:0":[["geometry.rs",739,751]],"2:1":[["geometry.rs",739,751]],"2:10":[["geometry.rs",739,751]],"2:11":[["geometry.rs",739,751]],"2:12":[["geometry.rs",739,751]],"2:13":[["geometry.rs",739,751]],"2:14":[["geometry.rs",739,751]],"2:15":[["geometry.rs",739,751]],"2:16":[["geometry.rs",739,751]],"2:17":[["geometry.rs",739,751]],"2:18":[["geometry.rs",739,751]],"2:19":[["geometry.rs",739,751]],"2:2":[["geometry.rs",739,751]],"2:20":[["geometry.rs",739,751]],"2:21":[["geometry.rs",739,751]],"2:22":[["geometry.rs",739,751]],"2:23":[["geometry.rs",739,751]],"2:24":[["geometry.rs",739,751]],"2:25":[["geometry.rs",739,751]],"2:26":[["geometry.rs",739,751]],"2:27":[["geometry.rs",739,751]],"2:28":[["geometry.rs",739,751]],"2:29":[["geometry.rs",739,751]],"2:3":[["geometry.rs",739,751]],"2:30":[["geometry.rs",739,751]],"2:31":[["geometry.rs",739,751]],"2:32":[["geometry.rs",739,751]],"2:33":[["geometry.rs",739,751]],"2:34":[["geometry.rs",739,751]],"2:35":[["geometry.rs",739,751]],"2:36":[["geometry.rs",739,751]],"2:37":[["geometry.rs",739,751]],"2:38":[["geometry.rs",739,751]],"2:39":[["geometry.rs",739,751]],"2:4":[["geometry.rs",739,751]],"2:40":[["geometry.rs",739,751]],"2:41":[["geometry.rs",739,751]],"2:42":[["geometry.rs",739,751]],"2:43":[["geometry.rs",739,751]],"2:44":[["geometry.rs",739,751]],"2:45":[["geometry.rs",739,751]],"2:46":[["geometry.rs",739,751]],"2:5":[["geometry.rs",739,751]],"2:6":[["geometry.rs",739,751]],"2:7":[["geometry.rs",739,751]],"2:8":[["geometry.rs",739,751]],"2:9":[["geometry.rs",739,751]],"3:0":[["geometry.rs",739,751]],"3:1":[["geometry.rs",739,751]],"3:2":[["geometry.rs",739,751]],"3:3":[["geometry.rs",739,751]],"3:4":[["geometry.rs",739,751]],"4:0":[["geometry.rs",770,782]],"4:1":[["geometry.rs",770,782]],"4:2":[["geometry.rs",770,782]],"4:3":[["geometry.rs",770,782]],"4:4":[["geometry.rs",770,782]],"4:5":[["geometry.rs",770,782]],"4:6":[["geometry.rs",770,782]],"5:0":[["geometry.rs",739,751]],"5:1":[["geometry.rs",739,751]]}},{"k":"fn:Bounds::centered_at","d":{"0:1":"Creates a new bounds centered at the given point.","0:2":"Creates a new bounds centered at the given point.","0:3":"Creates a new bounds centered at the given point.","0:4":"Creates a new bounds centered at the given point.","0:5":"Creates a new bounds centered at the given point.","0:6":"Creates a new bounds centered at the given point.","1:0":"Creates a new bounds centered at the given point.","1:1":"Creates a new bounds centered at the given point.","1:2":"Creates a new bounds centered at the given point.","2:0":"Creates a new bounds centered at the given point.","2:1":"Creates a new bounds centered at the given point.","2:10":"Creates a new bounds centered at the given point.","2:11":"Creates a new bounds centered at the given point.","2:12":"Creates a new bounds centered at the given point.","2:13":"Creates a new bounds centered at the given point.","2:14":"Creates a new bounds centered at the given point.","2:15":"Creates a new bounds centered at the given point.","2:16":"Creates a new bounds centered at the given point.","2:17":"Creates a new bounds centered at the given point.","2:18":"Creates a new bounds centered at the given point.","2:19":"Creates a new bounds centered at the given point.","2:2":"Creates a new bounds centered at the given point.","2:20":"Creates a new bounds centered at the given point.","2:21":"Creates a new bounds centered at the given point.","2:22":"Creates a new bounds centered at the given point.","2:23":"Creates a new bounds centered at the given point.","2:24":"Creates a new bounds centered at the given point.","2:25":"Creates a new bounds centered at the given point.","2:26":"Creates a new bounds centered at the given point.","2:27":"Creates a new bounds centered at the given point.","2:28":"Creates a new bounds centered at the given point.","2:29":"Creates a new bounds centered at the given point.","2:3":"Creates a new bounds centered at the given point.","2:30":"Creates a new bounds centered at the given point.","2:31":"Creates a new bounds centered at the given point.","2:32":"Creates a new bounds centered at the given point.","2:33":"Creates a new bounds centered at the given point.","2:34":"Creates a new bounds centered at the given point.","2:35":"Creates a new bounds centered at the given point.","2:36":"Creates a new bounds centered at the given point.","2:37":"Creates a new bounds centered at the given point.","2:38":"Creates a new bounds centered at the given point.","2:39":"Creates a new bounds centered at the given point.","2:4":"Creates a new bounds centered at the given point.","2:40":"Creates a new bounds centered at the given point.","2:41":"Creates a new bounds centered at the given point.","2:42":"Creates a new bounds centered at the given point.","2:43":"Creates a new bounds centered at the given point.","2:44":"Creates a new bounds centered at the given point.","2:45":"Creates a new bounds centered at the given point.","2:46":"Creates a new bounds centered at the given point.","2:5":"Creates a new bounds centered at the given point.","2:6":"Creates a new bounds centered at the given point.","2:7":"Creates a new bounds centered at the given point.","2:8":"Creates a new bounds centered at the given point.","2:9":"Creates a new bounds centered at the given point.","3:0":"Creates a new bounds centered at the given point.","3:1":"Creates a new bounds centered at the given point.","3:2":"Creates a new bounds centered at the given point.","3:3":"Creates a new bounds centered at the given point.","3:4":"Creates a new bounds centered at the given point.","4:0":"Creates a new bounds centered at the given point.","4:1":"Creates a new bounds centered at the given point.","4:2":"Creates a new bounds centered at the given point.","4:3":"Creates a new bounds centered at the given point.","4:4":"Creates a new bounds centered at the given point.","4:5":"Creates a new bounds centered at the given point.","4:6":"Creates a new bounds centered at the given point.","5:0":"Creates a new bounds centered at the given point.","5:1":"Creates a new bounds centered at the given point."},"s":{"0:1":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","0:2":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","0:3":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","0:4":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","0:5":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","0:6":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","1:0":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","1:1":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","1:2":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:0":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:1":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:10":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:11":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:12":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:13":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:14":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:15":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:16":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:17":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:18":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:19":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:2":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:20":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:21":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:22":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:23":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:24":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:25":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:26":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:27":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:28":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:29":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:3":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:30":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:31":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:32":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:33":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:34":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:35":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:36":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:37":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:38":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:39":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:4":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:40":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:41":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:42":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:43":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:44":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:45":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:46":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:5":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:6":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:7":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:8":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","2:9":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","3:0":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","3:1":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","3:2":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","3:3":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","3:4":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","4:0":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","4:1":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","4:2":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","4:3":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","4:4":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","4:5":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","4:6":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","5:0":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self","5:1":"fn Bounds::centered_at(Point < T >, Size < T >)-> Self"},"l":{"0:1":[["geometry.rs",888,895]],"0:2":[["geometry.rs",888,895]],"0:3":[["geometry.rs",888,895]],"0:4":[["geometry.rs",888,895]],"0:5":[["geometry.rs",888,895]],"0:6":[["geometry.rs",888,895]],"1:0":[["geometry.rs",888,895]],"1:1":[["geometry.rs",888,895]],"1:2":[["geometry.rs",893,900]],"2:0":[["geometry.rs",857,864]],"2:1":[["geometry.rs",857,864]],"2:10":[["geometry.rs",857,864]],"2:11":[["geometry.rs",857,864]],"2:12":[["geometry.rs",878,885]],"2:13":[["geometry.rs",878,885]],"2:14":[["geometry.rs",878,885]],"2:15":[["geometry.rs",878,885]],"2:16":[["geometry.rs",878,885]],"2:17":[["geometry.rs",878,885]],"2:18":[["geometry.rs",878,885]],"2:19":[["geometry.rs",878,885]],"2:2":[["geometry.rs",857,864]],"2:20":[["geometry.rs",878,885]],"2:21":[["geometry.rs",878,885]],"2:22":[["geometry.rs",878,885]],"2:23":[["geometry.rs",878,885]],"2:24":[["geometry.rs",878,885]],"2:25":[["geometry.rs",878,885]],"2:26":[["geometry.rs",878,885]],"2:27":[["geometry.rs",878,885]],"2:28":[["geometry.rs",878,885]],"2:29":[["geometry.rs",878,885]],"2:3":[["geometry.rs",857,864]],"2:30":[["geometry.rs",878,885]],"2:31":[["geometry.rs",878,885]],"2:32":[["geometry.rs",878,885]],"2:33":[["geometry.rs",878,885]],"2:34":[["geometry.rs",878,885]],"2:35":[["geometry.rs",878,885]],"2:36":[["geometry.rs",878,885]],"2:37":[["geometry.rs",878,885]],"2:38":[["geometry.rs",878,885]],"2:39":[["geometry.rs",878,885]],"2:4":[["geometry.rs",857,864]],"2:40":[["geometry.rs",878,885]],"2:41":[["geometry.rs",878,885]],"2:42":[["geometry.rs",878,885]],"2:43":[["geometry.rs",878,885]],"2:44":[["geometry.rs",878,885]],"2:45":[["geometry.rs",878,885]],"2:46":[["geometry.rs",878,885]],"2:5":[["geometry.rs",857,864]],"2:6":[["geometry.rs",857,864]],"2:7":[["geometry.rs",857,864]],"2:8":[["geometry.rs",857,864]],"2:9":[["geometry.rs",857,864]],"3:0":[["geometry.rs",878,885]],"3:1":[["geometry.rs",878,885]],"3:2":[["geometry.rs",878,885]],"3:3":[["geometry.rs",878,885]],"3:4":[["geometry.rs",878,885]],"4:0":[["geometry.rs",888,895]],"4:1":[["geometry.rs",888,895]],"4:2":[["geometry.rs",888,895]],"4:3":[["geometry.rs",888,895]],"4:4":[["geometry.rs",888,895]],"4:5":[["geometry.rs",888,895]],"4:6":[["geometry.rs",888,895]],"5:0":[["geometry.rs",878,885]],"5:1":[["geometry.rs",878,885]]}},{"k":"fn:Bounds::contains","d":{"0:1":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","0:2":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","0:3":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","0:4":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","0:5":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","0:6":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","1:0":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","1:1":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","1:2":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:0":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:1":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:10":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:11":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:12":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:13":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:14":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:15":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:16":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:17":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:18":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:19":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:2":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:20":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:21":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:22":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:23":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:24":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:25":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:26":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:27":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:28":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:29":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:3":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:30":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:31":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:32":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:33":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:34":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:35":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:36":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:37":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:38":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:39":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:4":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:40":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:41":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:42":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:43":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:44":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:45":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:46":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:5":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:6":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:7":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:8":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","2:9":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","3:0":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","3:1":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","3:2":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","3:3":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","3:4":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","4:0":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use kael::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","4:1":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use kael::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","4:2":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use kael::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","4:3":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use kael::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","4:4":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use kael::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","4:5":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use kael::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","4:6":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use kael::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","5:0":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```","5:1":"Checks if the given point is within the bounds.\n\nThis method determines whether a point lies inside the rectangle defined by the bounds,\nincluding the edges. The point is considered inside if its x-coordinate is greater than\nor equal to the left edge and less than or equal to the right edge, and its y-coordinate\nis greater than or equal to the top edge and less than or equal to the bottom edge of the bounds.\n\n# Arguments\n\n* `point` - A reference to a `Point<T>` that represents the point to check.\n\n# Returns\n\nReturns `true` if the point is within the bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Point, Bounds, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet inside_point = Point { x: 5, y: 5 };\nlet outside_point = Point { x: 15, y: 15 };\n\nassert!(bounds.contains(&inside_point));\nassert!(!bounds.contains(&outside_point));\n```"},"s":{"0:1":"fn Bounds::contains(& self, & Point < T >)-> bool","0:2":"fn Bounds::contains(& self, & Point < T >)-> bool","0:3":"fn Bounds::contains(& self, & Point < T >)-> bool","0:4":"fn Bounds::contains(& self, & Point < T >)-> bool","0:5":"fn Bounds::contains(& self, & Point < T >)-> bool","0:6":"fn Bounds::contains(& self, & Point < T >)-> bool","1:0":"fn Bounds::contains(& self, & Point < T >)-> bool","1:1":"fn Bounds::contains(& self, & Point < T >)-> bool","1:2":"fn Bounds::contains(& self, & Point < T >)-> bool","2:0":"fn Bounds::contains(& self, & Point < T >)-> bool","2:1":"fn Bounds::contains(& self, & Point < T >)-> bool","2:10":"fn Bounds::contains(& self, & Point < T >)-> bool","2:11":"fn Bounds::contains(& self, & Point < T >)-> bool","2:12":"fn Bounds::contains(& self, & Point < T >)-> bool","2:13":"fn Bounds::contains(& self, & Point < T >)-> bool","2:14":"fn Bounds::contains(& self, & Point < T >)-> bool","2:15":"fn Bounds::contains(& self, & Point < T >)-> bool","2:16":"fn Bounds::contains(& self, & Point < T >)-> bool","2:17":"fn Bounds::contains(& self, & Point < T >)-> bool","2:18":"fn Bounds::contains(& self, & Point < T >)-> bool","2:19":"fn Bounds::contains(& self, & Point < T >)-> bool","2:2":"fn Bounds::contains(& self, & Point < T >)-> bool","2:20":"fn Bounds::contains(& self, & Point < T >)-> bool","2:21":"fn Bounds::contains(& self, & Point < T >)-> bool","2:22":"fn Bounds::contains(& self, & Point < T >)-> bool","2:23":"fn Bounds::contains(& self, & Point < T >)-> bool","2:24":"fn Bounds::contains(& self, & Point < T >)-> bool","2:25":"fn Bounds::contains(& self, & Point < T >)-> bool","2:26":"fn Bounds::contains(& self, & Point < T >)-> bool","2:27":"fn Bounds::contains(& self, & Point < T >)-> bool","2:28":"fn Bounds::contains(& self, & Point < T >)-> bool","2:29":"fn Bounds::contains(& self, & Point < T >)-> bool","2:3":"fn Bounds::contains(& self, & Point < T >)-> bool","2:30":"fn Bounds::contains(& self, & Point < T >)-> bool","2:31":"fn Bounds::contains(& self, & Point < T >)-> bool","2:32":"fn Bounds::contains(& self, & Point < T >)-> bool","2:33":"fn Bounds::contains(& self, & Point < T >)-> bool","2:34":"fn Bounds::contains(& self, & Point < T >)-> bool","2:35":"fn Bounds::contains(& self, & Point < T >)-> bool","2:36":"fn Bounds::contains(& self, & Point < T >)-> bool","2:37":"fn Bounds::contains(& self, & Point < T >)-> bool","2:38":"fn Bounds::contains(& self, & Point < T >)-> bool","2:39":"fn Bounds::contains(& self, & Point < T >)-> bool","2:4":"fn Bounds::contains(& self, & Point < T >)-> bool","2:40":"fn Bounds::contains(& self, & Point < T >)-> bool","2:41":"fn Bounds::contains(& self, & Point < T >)-> bool","2:42":"fn Bounds::contains(& self, & Point < T >)-> bool","2:43":"fn Bounds::contains(& self, & Point < T >)-> bool","2:44":"fn Bounds::contains(& self, & Point < T >)-> bool","2:45":"fn Bounds::contains(& self, & Point < T >)-> bool","2:46":"fn Bounds::contains(& self, & Point < T >)-> bool","2:5":"fn Bounds::contains(& self, & Point < T >)-> bool","2:6":"fn Bounds::contains(& self, & Point < T >)-> bool","2:7":"fn Bounds::contains(& self, & Point < T >)-> bool","2:8":"fn Bounds::contains(& self, & Point < T >)-> bool","2:9":"fn Bounds::contains(& self, & Point < T >)-> bool","3:0":"fn Bounds::contains(& self, & Point < T >)-> bool","3:1":"fn Bounds::contains(& self, & Point < T >)-> bool","3:2":"fn Bounds::contains(& self, & Point < T >)-> bool","3:3":"fn Bounds::contains(& self, & Point < T >)-> bool","3:4":"fn Bounds::contains(& self, & Point < T >)-> bool","4:0":"fn Bounds::contains(& self, & Point < T >)-> bool","4:1":"fn Bounds::contains(& self, & Point < T >)-> bool","4:2":"fn Bounds::contains(& self, & Point < T >)-> bool","4:3":"fn Bounds::contains(& self, & Point < T >)-> bool","4:4":"fn Bounds::contains(& self, & Point < T >)-> bool","4:5":"fn Bounds::contains(& self, & Point < T >)-> bool","4:6":"fn Bounds::contains(& self, & Point < T >)-> bool","5:0":"fn Bounds::contains(& self, & Point < T >)-> bool","5:1":"fn Bounds::contains(& self, & Point < T >)-> bool"},"l":{"0:1":[["geometry.rs",1388,1422]],"0:2":[["geometry.rs",1388,1422]],"0:3":[["geometry.rs",1388,1422]],"0:4":[["geometry.rs",1388,1422]],"0:5":[["geometry.rs",1388,1422]],"0:6":[["geometry.rs",1388,1422]],"1:0":[["geometry.rs",1388,1422]],"1:1":[["geometry.rs",1388,1422]],"1:2":[["geometry.rs",1451,1485]],"2:0":[["geometry.rs",1369,1403]],"2:1":[["geometry.rs",1369,1403]],"2:10":[["geometry.rs",1369,1403]],"2:11":[["geometry.rs",1369,1403]],"2:12":[["geometry.rs",1436,1470]],"2:13":[["geometry.rs",1436,1470]],"2:14":[["geometry.rs",1436,1470]],"2:15":[["geometry.rs",1436,1470]],"2:16":[["geometry.rs",1436,1470]],"2:17":[["geometry.rs",1436,1470]],"2:18":[["geometry.rs",1436,1470]],"2:19":[["geometry.rs",1436,1470]],"2:2":[["geometry.rs",1369,1403]],"2:20":[["geometry.rs",1436,1470]],"2:21":[["geometry.rs",1436,1470]],"2:22":[["geometry.rs",1436,1470]],"2:23":[["geometry.rs",1436,1470]],"2:24":[["geometry.rs",1436,1470]],"2:25":[["geometry.rs",1436,1470]],"2:26":[["geometry.rs",1436,1470]],"2:27":[["geometry.rs",1436,1470]],"2:28":[["geometry.rs",1436,1470]],"2:29":[["geometry.rs",1436,1470]],"2:3":[["geometry.rs",1369,1403]],"2:30":[["geometry.rs",1436,1470]],"2:31":[["geometry.rs",1436,1470]],"2:32":[["geometry.rs",1436,1470]],"2:33":[["geometry.rs",1436,1470]],"2:34":[["geometry.rs",1436,1470]],"2:35":[["geometry.rs",1436,1470]],"2:36":[["geometry.rs",1436,1470]],"2:37":[["geometry.rs",1436,1470]],"2:38":[["geometry.rs",1436,1470]],"2:39":[["geometry.rs",1436,1470]],"2:4":[["geometry.rs",1369,1403]],"2:40":[["geometry.rs",1436,1470]],"2:41":[["geometry.rs",1436,1470]],"2:42":[["geometry.rs",1436,1470]],"2:43":[["geometry.rs",1436,1470]],"2:44":[["geometry.rs",1436,1470]],"2:45":[["geometry.rs",1436,1470]],"2:46":[["geometry.rs",1436,1470]],"2:5":[["geometry.rs",1369,1403]],"2:6":[["geometry.rs",1369,1403]],"2:7":[["geometry.rs",1369,1403]],"2:8":[["geometry.rs",1369,1403]],"2:9":[["geometry.rs",1369,1403]],"3:0":[["geometry.rs",1436,1470]],"3:1":[["geometry.rs",1436,1470]],"3:2":[["geometry.rs",1436,1470]],"3:3":[["geometry.rs",1436,1470]],"3:4":[["geometry.rs",1436,1470]],"4:0":[["geometry.rs",1401,1435]],"4:1":[["geometry.rs",1401,1435]],"4:2":[["geometry.rs",1401,1435]],"4:3":[["geometry.rs",1401,1435]],"4:4":[["geometry.rs",1401,1435]],"4:5":[["geometry.rs",1401,1435]],"4:6":[["geometry.rs",1401,1435]],"5:0":[["geometry.rs",1436,1470]],"5:1":[["geometry.rs",1436,1470]]}},{"k":"fn:Bounds::contract","d":{"4:0":"Returns bounds contracted inward by the given amount on all sides (inverse of dilate).","4:1":"Returns bounds contracted inward by the given amount on all sides (inverse of dilate).","4:2":"Returns bounds contracted inward by the given amount on all sides (inverse of dilate).","4:3":"Returns bounds contracted inward by the given amount on all sides (inverse of dilate).","4:4":"Returns bounds contracted inward by the given amount on all sides (inverse of dilate).","4:5":"Returns bounds contracted inward by the given amount on all sides (inverse of dilate).","4:6":"Returns bounds contracted inward by the given amount on all sides (inverse of dilate)."},"s":{"4:0":"fn Bounds::contract(& self, T)-> Bounds < T >","4:1":"fn Bounds::contract(& self, T)-> Bounds < T >","4:2":"fn Bounds::contract(& self, T)-> Bounds < T >","4:3":"fn Bounds::contract(& self, T)-> Bounds < T >","4:4":"fn Bounds::contract(& self, T)-> Bounds < T >","4:5":"fn Bounds::contract(& self, T)-> Bounds < T >","4:6":"fn Bounds::contract(& self, T)-> Bounds < T >"},"l":{"4:0":[["geometry.rs",1045,1056]],"4:1":[["geometry.rs",1045,1056]],"4:2":[["geometry.rs",1045,1056]],"4:3":[["geometry.rs",1045,1056]],"4:4":[["geometry.rs",1045,1056]],"4:5":[["geometry.rs",1045,1056]],"4:6":[["geometry.rs",1045,1056]]}},{"k":"fn:Bounds::corner","d":{"0:1":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","0:2":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","0:3":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","0:4":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","0:5":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","0:6":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","1:0":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","1:1":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","1:2":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:0":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:1":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:10":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:11":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:12":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:13":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:14":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:15":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:16":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:17":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:18":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:19":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:2":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:20":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:21":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:22":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:23":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:24":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:25":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:26":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:27":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:28":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:29":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:3":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:30":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:31":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:32":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:33":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:34":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:35":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:36":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:37":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:38":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:39":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:4":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:40":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:41":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:42":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:43":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:44":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:45":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:46":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:5":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:6":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:7":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:8":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","2:9":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","3:0":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","3:1":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","3:2":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","3:3":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","3:4":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","4:0":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse kael::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","4:1":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse kael::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","4:2":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse kael::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","4:3":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse kael::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","4:4":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse kael::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","4:5":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse kael::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","4:6":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse kael::{Bounds, Corner, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Corner::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","5:0":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```","5:1":"Returns the requested corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the corner of the bounds requested by the parameter.\n\n# Examples\n\n```\nuse gpui::{Bounds, Anchor, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet bottom_left = bounds.corner(Anchor::BottomLeft);\nassert_eq!(bottom_left, Point { x: 0, y: 20 });\n```"},"s":{"0:1":"fn Bounds::corner(& self, Corner)-> Point < T >","0:2":"fn Bounds::corner(& self, Corner)-> Point < T >","0:3":"fn Bounds::corner(& self, Corner)-> Point < T >","0:4":"fn Bounds::corner(& self, Corner)-> Point < T >","0:5":"fn Bounds::corner(& self, Corner)-> Point < T >","0:6":"fn Bounds::corner(& self, Corner)-> Point < T >","1:0":"fn Bounds::corner(& self, Corner)-> Point < T >","1:1":"fn Bounds::corner(& self, Corner)-> Point < T >","1:2":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:0":"fn Bounds::corner(& self, Corner)-> Point < T >","2:1":"fn Bounds::corner(& self, Corner)-> Point < T >","2:10":"fn Bounds::corner(& self, Corner)-> Point < T >","2:11":"fn Bounds::corner(& self, Corner)-> Point < T >","2:12":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:13":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:14":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:15":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:16":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:17":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:18":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:19":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:2":"fn Bounds::corner(& self, Corner)-> Point < T >","2:20":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:21":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:22":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:23":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:24":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:25":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:26":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:27":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:28":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:29":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:3":"fn Bounds::corner(& self, Corner)-> Point < T >","2:30":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:31":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:32":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:33":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:34":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:35":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:36":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:37":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:38":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:39":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:4":"fn Bounds::corner(& self, Corner)-> Point < T >","2:40":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:41":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:42":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:43":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:44":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:45":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:46":"fn Bounds::corner(& self, Anchor)-> Point < T >","2:5":"fn Bounds::corner(& self, Corner)-> Point < T >","2:6":"fn Bounds::corner(& self, Corner)-> Point < T >","2:7":"fn Bounds::corner(& self, Corner)-> Point < T >","2:8":"fn Bounds::corner(& self, Corner)-> Point < T >","2:9":"fn Bounds::corner(& self, Corner)-> Point < T >","3:0":"fn Bounds::corner(& self, Anchor)-> Point < T >","3:1":"fn Bounds::corner(& self, Anchor)-> Point < T >","3:2":"fn Bounds::corner(& self, Anchor)-> Point < T >","3:3":"fn Bounds::corner(& self, Anchor)-> Point < T >","3:4":"fn Bounds::corner(& self, Anchor)-> Point < T >","4:0":"fn Bounds::corner(& self, Corner)-> Point < T >","4:1":"fn Bounds::corner(& self, Corner)-> Point < T >","4:2":"fn Bounds::corner(& self, Corner)-> Point < T >","4:3":"fn Bounds::corner(& self, Corner)-> Point < T >","4:4":"fn Bounds::corner(& self, Corner)-> Point < T >","4:5":"fn Bounds::corner(& self, Corner)-> Point < T >","4:6":"fn Bounds::corner(& self, Corner)-> Point < T >","5:0":"fn Bounds::corner(& self, Anchor)-> Point < T >","5:1":"fn Bounds::corner(& self, Anchor)-> Point < T >"},"l":{"0:1":[["geometry.rs",1357,1381]],"0:2":[["geometry.rs",1357,1381]],"0:3":[["geometry.rs",1357,1381]],"0:4":[["geometry.rs",1357,1381]],"0:5":[["geometry.rs",1357,1381]],"0:6":[["geometry.rs",1357,1381]],"1:0":[["geometry.rs",1357,1381]],"1:1":[["geometry.rs",1357,1381]],"1:2":[["geometry.rs",1416,1444]],"2:0":[["geometry.rs",1338,1362]],"2:1":[["geometry.rs",1338,1362]],"2:10":[["geometry.rs",1338,1362]],"2:11":[["geometry.rs",1338,1362]],"2:12":[["geometry.rs",1401,1429]],"2:13":[["geometry.rs",1401,1429]],"2:14":[["geometry.rs",1401,1429]],"2:15":[["geometry.rs",1401,1429]],"2:16":[["geometry.rs",1401,1429]],"2:17":[["geometry.rs",1401,1429]],"2:18":[["geometry.rs",1401,1429]],"2:19":[["geometry.rs",1401,1429]],"2:2":[["geometry.rs",1338,1362]],"2:20":[["geometry.rs",1401,1429]],"2:21":[["geometry.rs",1401,1429]],"2:22":[["geometry.rs",1401,1429]],"2:23":[["geometry.rs",1401,1429]],"2:24":[["geometry.rs",1401,1429]],"2:25":[["geometry.rs",1401,1429]],"2:26":[["geometry.rs",1401,1429]],"2:27":[["geometry.rs",1401,1429]],"2:28":[["geometry.rs",1401,1429]],"2:29":[["geometry.rs",1401,1429]],"2:3":[["geometry.rs",1338,1362]],"2:30":[["geometry.rs",1401,1429]],"2:31":[["geometry.rs",1401,1429]],"2:32":[["geometry.rs",1401,1429]],"2:33":[["geometry.rs",1401,1429]],"2:34":[["geometry.rs",1401,1429]],"2:35":[["geometry.rs",1401,1429]],"2:36":[["geometry.rs",1401,1429]],"2:37":[["geometry.rs",1401,1429]],"2:38":[["geometry.rs",1401,1429]],"2:39":[["geometry.rs",1401,1429]],"2:4":[["geometry.rs",1338,1362]],"2:40":[["geometry.rs",1401,1429]],"2:41":[["geometry.rs",1401,1429]],"2:42":[["geometry.rs",1401,1429]],"2:43":[["geometry.rs",1401,1429]],"2:44":[["geometry.rs",1401,1429]],"2:45":[["geometry.rs",1401,1429]],"2:46":[["geometry.rs",1401,1429]],"2:5":[["geometry.rs",1338,1362]],"2:6":[["geometry.rs",1338,1362]],"2:7":[["geometry.rs",1338,1362]],"2:8":[["geometry.rs",1338,1362]],"2:9":[["geometry.rs",1338,1362]],"3:0":[["geometry.rs",1401,1429]],"3:1":[["geometry.rs",1401,1429]],"3:2":[["geometry.rs",1401,1429]],"3:3":[["geometry.rs",1401,1429]],"3:4":[["geometry.rs",1401,1429]],"4:0":[["geometry.rs",1370,1394]],"4:1":[["geometry.rs",1370,1394]],"4:2":[["geometry.rs",1370,1394]],"4:3":[["geometry.rs",1370,1394]],"4:4":[["geometry.rs",1370,1394]],"4:5":[["geometry.rs",1370,1394]],"4:6":[["geometry.rs",1370,1394]],"5:0":[["geometry.rs",1401,1429]],"5:1":[["geometry.rs",1401,1429]]}},{"k":"fn:Bounds::dilate","d":{"0:1":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","0:2":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","0:3":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","0:4":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","0:5":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","0:6":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","1:0":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","1:1":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","1:2":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:0":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:1":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:10":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:11":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:12":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:13":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:14":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:15":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:16":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:17":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:18":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:19":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:2":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:20":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:21":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:22":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:23":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:24":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:25":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:26":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:27":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:28":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:29":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:3":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:30":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:31":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:32":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:33":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:34":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:35":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:36":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:37":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:38":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:39":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:4":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:40":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:41":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:42":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:43":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:44":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:45":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:46":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:5":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:6":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:7":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:8":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:9":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","3:0":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","3:1":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","3:2":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","3:3":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","3:4":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","4:0":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","4:1":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","4:2":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","4:3":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","4:4":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","4:5":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","4:6":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","5:0":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```","5:1":"Dilates the bounds by a specified amount in all directions.\n\nThis method expands the bounds by the given `amount`, increasing the size\nand adjusting the origin so that the bounds grow outwards equally in all directions.\nThe resulting bounds will have its width and height increased by twice the `amount`\n(since it grows in both directions), and the origin will be moved by `-amount`\nin both the x and y directions.\n\n# Arguments\n\n* `amount` - The amount by which to dilate the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet mut bounds = Bounds {\n    origin: Point { x: 10, y: 10 },\n    size: Size { width: 10, height: 10 },\n};\nlet expanded_bounds = bounds.dilate(5);\nassert_eq!(expanded_bounds, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 20, height: 20 },\n});\n```"},"s":{"0:1":"fn Bounds::dilate(& self, T)-> Bounds < T >","0:2":"fn Bounds::dilate(& self, T)-> Bounds < T >","0:3":"fn Bounds::dilate(& self, T)-> Bounds < T >","0:4":"fn Bounds::dilate(& self, T)-> Bounds < T >","0:5":"fn Bounds::dilate(& self, T)-> Bounds < T >","0:6":"fn Bounds::dilate(& self, T)-> Bounds < T >","1:0":"fn Bounds::dilate(& self, T)-> Bounds < T >","1:1":"fn Bounds::dilate(& self, T)-> Bounds < T >","1:2":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:0":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:1":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:10":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:11":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:12":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:13":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:14":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:15":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:16":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:17":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:18":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:19":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:2":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:20":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:21":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:22":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:23":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:24":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:25":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:26":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:27":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:28":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:29":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:3":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:30":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:31":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:32":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:33":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:34":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:35":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:36":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:37":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:38":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:39":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:4":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:40":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:41":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:42":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:43":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:44":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:45":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:46":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:5":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:6":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:7":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:8":"fn Bounds::dilate(& self, T)-> Bounds < T >","2:9":"fn Bounds::dilate(& self, T)-> Bounds < T >","3:0":"fn Bounds::dilate(& self, T)-> Bounds < T >","3:1":"fn Bounds::dilate(& self, T)-> Bounds < T >","3:2":"fn Bounds::dilate(& self, T)-> Bounds < T >","3:3":"fn Bounds::dilate(& self, T)-> Bounds < T >","3:4":"fn Bounds::dilate(& self, T)-> Bounds < T >","4:0":"fn Bounds::dilate(& self, T)-> Bounds < T >","4:1":"fn Bounds::dilate(& self, T)-> Bounds < T >","4:2":"fn Bounds::dilate(& self, T)-> Bounds < T >","4:3":"fn Bounds::dilate(& self, T)-> Bounds < T >","4:4":"fn Bounds::dilate(& self, T)-> Bounds < T >","4:5":"fn Bounds::dilate(& self, T)-> Bounds < T >","4:6":"fn Bounds::dilate(& self, T)-> Bounds < T >","5:0":"fn Bounds::dilate(& self, T)-> Bounds < T >","5:1":"fn Bounds::dilate(& self, T)-> Bounds < T >"},"l":{"0:1":[["geometry.rs",1010,1043]],"0:2":[["geometry.rs",1010,1043]],"0:3":[["geometry.rs",1010,1043]],"0:4":[["geometry.rs",1010,1043]],"0:5":[["geometry.rs",1010,1043]],"0:6":[["geometry.rs",1010,1043]],"1:0":[["geometry.rs",1010,1043]],"1:1":[["geometry.rs",1010,1043]],"1:2":[["geometry.rs",1052,1085]],"2:0":[["geometry.rs",979,1012]],"2:1":[["geometry.rs",979,1012]],"2:10":[["geometry.rs",979,1012]],"2:11":[["geometry.rs",979,1012]],"2:12":[["geometry.rs",1037,1070]],"2:13":[["geometry.rs",1037,1070]],"2:14":[["geometry.rs",1037,1070]],"2:15":[["geometry.rs",1037,1070]],"2:16":[["geometry.rs",1037,1070]],"2:17":[["geometry.rs",1037,1070]],"2:18":[["geometry.rs",1037,1070]],"2:19":[["geometry.rs",1037,1070]],"2:2":[["geometry.rs",979,1012]],"2:20":[["geometry.rs",1037,1070]],"2:21":[["geometry.rs",1037,1070]],"2:22":[["geometry.rs",1037,1070]],"2:23":[["geometry.rs",1037,1070]],"2:24":[["geometry.rs",1037,1070]],"2:25":[["geometry.rs",1037,1070]],"2:26":[["geometry.rs",1037,1070]],"2:27":[["geometry.rs",1037,1070]],"2:28":[["geometry.rs",1037,1070]],"2:29":[["geometry.rs",1037,1070]],"2:3":[["geometry.rs",979,1012]],"2:30":[["geometry.rs",1037,1070]],"2:31":[["geometry.rs",1037,1070]],"2:32":[["geometry.rs",1037,1070]],"2:33":[["geometry.rs",1037,1070]],"2:34":[["geometry.rs",1037,1070]],"2:35":[["geometry.rs",1037,1070]],"2:36":[["geometry.rs",1037,1070]],"2:37":[["geometry.rs",1037,1070]],"2:38":[["geometry.rs",1037,1070]],"2:39":[["geometry.rs",1037,1070]],"2:4":[["geometry.rs",979,1012]],"2:40":[["geometry.rs",1037,1070]],"2:41":[["geometry.rs",1037,1070]],"2:42":[["geometry.rs",1037,1070]],"2:43":[["geometry.rs",1037,1070]],"2:44":[["geometry.rs",1037,1070]],"2:45":[["geometry.rs",1037,1070]],"2:46":[["geometry.rs",1037,1070]],"2:5":[["geometry.rs",979,1012]],"2:6":[["geometry.rs",979,1012]],"2:7":[["geometry.rs",979,1012]],"2:8":[["geometry.rs",979,1012]],"2:9":[["geometry.rs",979,1012]],"3:0":[["geometry.rs",1037,1070]],"3:1":[["geometry.rs",1037,1070]],"3:2":[["geometry.rs",1037,1070]],"3:3":[["geometry.rs",1037,1070]],"3:4":[["geometry.rs",1037,1070]],"4:0":[["geometry.rs",1010,1043]],"4:1":[["geometry.rs",1010,1043]],"4:2":[["geometry.rs",1010,1043]],"4:3":[["geometry.rs",1010,1043]],"4:4":[["geometry.rs",1010,1043]],"4:5":[["geometry.rs",1010,1043]],"4:6":[["geometry.rs",1010,1043]],"5:0":[["geometry.rs",1037,1070]],"5:1":[["geometry.rs",1037,1070]]}},{"k":"fn:Bounds::extend","d":{"0:1":"Extends the bounds different amounts in each direction.","0:2":"Extends the bounds different amounts in each direction.","0:3":"Extends the bounds different amounts in each direction.","0:4":"Extends the bounds different amounts in each direction.","0:5":"Extends the bounds different amounts in each direction.","0:6":"Extends the bounds different amounts in each direction.","1:0":"Extends the bounds different amounts in each direction.","1:1":"Extends the bounds different amounts in each direction.","1:2":"Extends the bounds different amounts in each direction.","2:0":"Extends the bounds different amounts in each direction.","2:1":"Extends the bounds different amounts in each direction.","2:10":"Extends the bounds different amounts in each direction.","2:11":"Extends the bounds different amounts in each direction.","2:12":"Extends the bounds different amounts in each direction.","2:13":"Extends the bounds different amounts in each direction.","2:14":"Extends the bounds different amounts in each direction.","2:15":"Extends the bounds different amounts in each direction.","2:16":"Extends the bounds different amounts in each direction.","2:17":"Extends the bounds different amounts in each direction.","2:18":"Extends the bounds different amounts in each direction.","2:19":"Extends the bounds different amounts in each direction.","2:2":"Extends the bounds different amounts in each direction.","2:20":"Extends the bounds different amounts in each direction.","2:21":"Extends the bounds different amounts in each direction.","2:22":"Extends the bounds different amounts in each direction.","2:23":"Extends the bounds different amounts in each direction.","2:24":"Extends the bounds different amounts in each direction.","2:25":"Extends the bounds different amounts in each direction.","2:26":"Extends the bounds different amounts in each direction.","2:27":"Extends the bounds different amounts in each direction.","2:28":"Extends the bounds different amounts in each direction.","2:29":"Extends the bounds different amounts in each direction.","2:3":"Extends the bounds different amounts in each direction.","2:30":"Extends the bounds different amounts in each direction.","2:31":"Extends the bounds different amounts in each direction.","2:32":"Extends the bounds different amounts in each direction.","2:33":"Extends the bounds different amounts in each direction.","2:34":"Extends the bounds different amounts in each direction.","2:35":"Extends the bounds different amounts in each direction.","2:36":"Extends the bounds different amounts in each direction.","2:37":"Extends the bounds different amounts in each direction.","2:38":"Extends the bounds different amounts in each direction.","2:39":"Extends the bounds different amounts in each direction.","2:4":"Extends the bounds different amounts in each direction.","2:40":"Extends the bounds different amounts in each direction.","2:41":"Extends the bounds different amounts in each direction.","2:42":"Extends the bounds different amounts in each direction.","2:43":"Extends the bounds different amounts in each direction.","2:44":"Extends the bounds different amounts in each direction.","2:45":"Extends the bounds different amounts in each direction.","2:46":"Extends the bounds different amounts in each direction.","2:5":"Extends the bounds different amounts in each direction.","2:6":"Extends the bounds different amounts in each direction.","2:7":"Extends the bounds different amounts in each direction.","2:8":"Extends the bounds different amounts in each direction.","2:9":"Extends the bounds different amounts in each direction.","3:0":"Extends the bounds different amounts in each direction.","3:1":"Extends the bounds different amounts in each direction.","3:2":"Extends the bounds different amounts in each direction.","3:3":"Extends the bounds different amounts in each direction.","3:4":"Extends the bounds different amounts in each direction.","4:0":"Extends the bounds different amounts in each direction.","4:1":"Extends the bounds different amounts in each direction.","4:2":"Extends the bounds different amounts in each direction.","4:3":"Extends the bounds different amounts in each direction.","4:4":"Extends the bounds different amounts in each direction.","4:5":"Extends the bounds different amounts in each direction.","4:6":"Extends the bounds different amounts in each direction.","5:0":"Extends the bounds different amounts in each direction.","5:1":"Extends the bounds different amounts in each direction."},"s":{"0:1":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","0:2":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","0:3":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","0:4":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","0:5":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","0:6":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","1:0":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","1:1":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","1:2":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:0":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:1":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:10":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:11":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:12":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:13":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:14":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:15":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:16":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:17":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:18":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:19":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:2":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:20":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:21":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:22":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:23":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:24":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:25":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:26":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:27":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:28":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:29":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:3":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:30":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:31":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:32":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:33":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:34":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:35":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:36":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:37":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:38":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:39":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:4":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:40":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:41":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:42":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:43":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:44":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:45":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:46":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:5":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:6":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:7":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:8":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","2:9":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","3:0":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","3:1":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","3:2":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","3:3":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","3:4":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","4:0":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","4:1":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","4:2":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","4:3":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","4:4":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","4:5":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","4:6":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","5:0":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >","5:1":"fn Bounds::extend(& self, Edges < T >)-> Bounds < T >"},"l":{"0:1":[["geometry.rs",1045,1056]],"0:2":[["geometry.rs",1045,1056]],"0:3":[["geometry.rs",1045,1056]],"0:4":[["geometry.rs",1045,1056]],"0:5":[["geometry.rs",1045,1056]],"0:6":[["geometry.rs",1045,1056]],"1:0":[["geometry.rs",1045,1056]],"1:1":[["geometry.rs",1045,1056]],"1:2":[["geometry.rs",1087,1098]],"2:0":[["geometry.rs",1014,1025]],"2:1":[["geometry.rs",1014,1025]],"2:10":[["geometry.rs",1014,1025]],"2:11":[["geometry.rs",1014,1025]],"2:12":[["geometry.rs",1072,1083]],"2:13":[["geometry.rs",1072,1083]],"2:14":[["geometry.rs",1072,1083]],"2:15":[["geometry.rs",1072,1083]],"2:16":[["geometry.rs",1072,1083]],"2:17":[["geometry.rs",1072,1083]],"2:18":[["geometry.rs",1072,1083]],"2:19":[["geometry.rs",1072,1083]],"2:2":[["geometry.rs",1014,1025]],"2:20":[["geometry.rs",1072,1083]],"2:21":[["geometry.rs",1072,1083]],"2:22":[["geometry.rs",1072,1083]],"2:23":[["geometry.rs",1072,1083]],"2:24":[["geometry.rs",1072,1083]],"2:25":[["geometry.rs",1072,1083]],"2:26":[["geometry.rs",1072,1083]],"2:27":[["geometry.rs",1072,1083]],"2:28":[["geometry.rs",1072,1083]],"2:29":[["geometry.rs",1072,1083]],"2:3":[["geometry.rs",1014,1025]],"2:30":[["geometry.rs",1072,1083]],"2:31":[["geometry.rs",1072,1083]],"2:32":[["geometry.rs",1072,1083]],"2:33":[["geometry.rs",1072,1083]],"2:34":[["geometry.rs",1072,1083]],"2:35":[["geometry.rs",1072,1083]],"2:36":[["geometry.rs",1072,1083]],"2:37":[["geometry.rs",1072,1083]],"2:38":[["geometry.rs",1072,1083]],"2:39":[["geometry.rs",1072,1083]],"2:4":[["geometry.rs",1014,1025]],"2:40":[["geometry.rs",1072,1083]],"2:41":[["geometry.rs",1072,1083]],"2:42":[["geometry.rs",1072,1083]],"2:43":[["geometry.rs",1072,1083]],"2:44":[["geometry.rs",1072,1083]],"2:45":[["geometry.rs",1072,1083]],"2:46":[["geometry.rs",1072,1083]],"2:5":[["geometry.rs",1014,1025]],"2:6":[["geometry.rs",1014,1025]],"2:7":[["geometry.rs",1014,1025]],"2:8":[["geometry.rs",1014,1025]],"2:9":[["geometry.rs",1014,1025]],"3:0":[["geometry.rs",1072,1083]],"3:1":[["geometry.rs",1072,1083]],"3:2":[["geometry.rs",1072,1083]],"3:3":[["geometry.rs",1072,1083]],"3:4":[["geometry.rs",1072,1083]],"4:0":[["geometry.rs",1058,1069]],"4:1":[["geometry.rs",1058,1069]],"4:2":[["geometry.rs",1058,1069]],"4:3":[["geometry.rs",1058,1069]],"4:4":[["geometry.rs",1058,1069]],"4:5":[["geometry.rs",1058,1069]],"4:6":[["geometry.rs",1058,1069]],"5:0":[["geometry.rs",1072,1083]],"5:1":[["geometry.rs",1072,1083]]}},{"k":"fn:Bounds::from_anchor_and_size","d":{"1:2":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:12":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:13":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:14":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:15":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:16":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:17":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:18":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:19":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:20":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:21":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:22":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:23":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:24":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:25":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:26":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:27":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:28":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:29":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:30":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:31":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:32":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:33":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:34":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:35":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:36":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:37":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:38":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:39":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:40":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:41":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:42":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:43":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:44":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:45":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:46":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","3:0":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","3:1":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","3:2":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","3:3":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","3:4":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","5:0":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","5:1":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin."},"s":{"1:2":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:12":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:13":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:14":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:15":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:16":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:17":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:18":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:19":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:20":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:21":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:22":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:23":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:24":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:25":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:26":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:27":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:28":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:29":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:30":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:31":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:32":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:33":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:34":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:35":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:36":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:37":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:38":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:39":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:40":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:41":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:42":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:43":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:44":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:45":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","2:46":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","3:0":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","3:1":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","3:2":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","3:3":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","3:4":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","5:0":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >","5:1":"fn Bounds::from_anchor_and_size(Anchor, Point < T >, Size < T >)-> Bounds < T >"},"l":{"1:2":[["geometry.rs",850,886]],"2:12":[["geometry.rs",835,871]],"2:13":[["geometry.rs",835,871]],"2:14":[["geometry.rs",835,871]],"2:15":[["geometry.rs",835,871]],"2:16":[["geometry.rs",835,871]],"2:17":[["geometry.rs",835,871]],"2:18":[["geometry.rs",835,871]],"2:19":[["geometry.rs",835,871]],"2:20":[["geometry.rs",835,871]],"2:21":[["geometry.rs",835,871]],"2:22":[["geometry.rs",835,871]],"2:23":[["geometry.rs",835,871]],"2:24":[["geometry.rs",835,871]],"2:25":[["geometry.rs",835,871]],"2:26":[["geometry.rs",835,871]],"2:27":[["geometry.rs",835,871]],"2:28":[["geometry.rs",835,871]],"2:29":[["geometry.rs",835,871]],"2:30":[["geometry.rs",835,871]],"2:31":[["geometry.rs",835,871]],"2:32":[["geometry.rs",835,871]],"2:33":[["geometry.rs",835,871]],"2:34":[["geometry.rs",835,871]],"2:35":[["geometry.rs",835,871]],"2:36":[["geometry.rs",835,871]],"2:37":[["geometry.rs",835,871]],"2:38":[["geometry.rs",835,871]],"2:39":[["geometry.rs",835,871]],"2:40":[["geometry.rs",835,871]],"2:41":[["geometry.rs",835,871]],"2:42":[["geometry.rs",835,871]],"2:43":[["geometry.rs",835,871]],"2:44":[["geometry.rs",835,871]],"2:45":[["geometry.rs",835,871]],"2:46":[["geometry.rs",835,871]],"3:0":[["geometry.rs",835,871]],"3:1":[["geometry.rs",835,871]],"3:2":[["geometry.rs",835,871]],"3:3":[["geometry.rs",835,871]],"3:4":[["geometry.rs",835,871]],"5:0":[["geometry.rs",835,871]],"5:1":[["geometry.rs",835,871]]}},{"k":"fn:Bounds::from_corner_and_size","d":{"0:1":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","0:2":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","0:3":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","0:4":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","0:5":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","0:6":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","1:0":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","1:1":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:0":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:1":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:10":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:11":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:2":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:3":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:4":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:5":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:6":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:7":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:8":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","2:9":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","4:0":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","4:1":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","4:2":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","4:3":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","4:4":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","4:5":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin.","4:6":"Constructs a `Bounds` from a corner point and size. The specified corner will be placed at\nthe specified origin."},"s":{"0:1":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","0:2":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","0:3":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","0:4":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","0:5":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","0:6":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","1:0":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","1:1":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","2:0":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","2:1":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","2:10":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","2:11":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","2:2":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","2:3":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","2:4":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","2:5":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","2:6":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","2:7":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","2:8":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","2:9":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","4:0":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","4:1":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","4:2":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","4:3":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","4:4":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","4:5":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >","4:6":"fn Bounds::from_corner_and_size(Corner, Point < T >, Size < T >)-> Bounds < T >"},"l":{"0:1":[["geometry.rs",861,881]],"0:2":[["geometry.rs",861,881]],"0:3":[["geometry.rs",861,881]],"0:4":[["geometry.rs",861,881]],"0:5":[["geometry.rs",861,881]],"0:6":[["geometry.rs",861,881]],"1:0":[["geometry.rs",861,881]],"1:1":[["geometry.rs",861,881]],"2:0":[["geometry.rs",830,850]],"2:1":[["geometry.rs",830,850]],"2:10":[["geometry.rs",830,850]],"2:11":[["geometry.rs",830,850]],"2:2":[["geometry.rs",830,850]],"2:3":[["geometry.rs",830,850]],"2:4":[["geometry.rs",830,850]],"2:5":[["geometry.rs",830,850]],"2:6":[["geometry.rs",830,850]],"2:7":[["geometry.rs",830,850]],"2:8":[["geometry.rs",830,850]],"2:9":[["geometry.rs",830,850]],"4:0":[["geometry.rs",861,881]],"4:1":[["geometry.rs",861,881]],"4:2":[["geometry.rs",861,881]],"4:3":[["geometry.rs",861,881]],"4:4":[["geometry.rs",861,881]],"4:5":[["geometry.rs",861,881]],"4:6":[["geometry.rs",861,881]]}},{"k":"fn:Bounds::from_corners","d":{"0:1":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","0:2":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","0:3":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","0:4":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","0:5":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","0:6":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","1:0":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","1:1":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","1:2":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:0":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:1":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:10":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:11":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:12":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:13":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:14":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:15":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:16":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:17":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:18":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:19":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:2":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:20":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:21":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:22":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:23":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:24":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:25":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:26":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:27":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:28":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:29":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:3":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:30":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:31":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:32":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:33":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:34":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:35":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:36":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:37":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:38":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:39":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:4":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:40":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:41":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:42":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:43":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:44":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:45":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:46":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:5":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:6":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:7":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:8":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","2:9":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","3:0":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","3:1":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","3:2":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","3:3":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","3:4":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","4:0":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use kael::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","4:1":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use kael::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","4:2":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use kael::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","4:3":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use kael::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","4:4":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use kael::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","4:5":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use kael::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","4:6":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use kael::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","5:0":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```","5:1":"Constructs a `Bounds` from two corner points: the top left and bottom right corners.\n\nThis function calculates the origin and size of the `Bounds` based on the provided corner points.\nThe origin is set to the top left corner, and the size is determined by the difference between\nthe x and y coordinates of the bottom right and top left points.\n\n# Arguments\n\n* `top_left` - A `Point<T>` representing the top left corner of the rectangle.\n* `bottom_right` - A `Point<T>` representing the bottom right corner of the rectangle.\n\n# Returns\n\nReturns a `Bounds<T>` that encompasses the area defined by the two corner points.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point};\nlet top_left = Point { x: 0, y: 0 };\nlet bottom_right = Point { x: 10, y: 10 };\nlet bounds = Bounds::from_corners(top_left, bottom_right);\n\nassert_eq!(bounds.origin, top_left);\nassert_eq!(bounds.size.width, 10);\nassert_eq!(bounds.size.height, 10);\n```"},"s":{"0:1":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","0:2":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","0:3":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","0:4":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","0:5":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","0:6":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","1:0":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","1:1":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","1:2":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:0":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:1":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:10":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:11":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:12":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:13":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:14":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:15":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:16":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:17":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:18":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:19":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:2":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:20":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:21":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:22":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:23":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:24":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:25":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:26":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:27":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:28":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:29":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:3":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:30":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:31":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:32":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:33":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:34":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:35":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:36":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:37":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:38":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:39":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:4":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:40":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:41":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:42":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:43":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:44":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:45":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:46":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:5":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:6":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:7":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:8":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","2:9":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","3:0":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","3:1":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","3:2":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","3:3":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","3:4":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","4:0":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","4:1":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","4:2":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","4:3":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","4:4":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","4:5":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","4:6":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","5:0":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self","5:1":"fn Bounds::from_corners(Point < T >, Point < T >)-> Self"},"l":{"0:1":[["geometry.rs",822,859]],"0:2":[["geometry.rs",822,859]],"0:3":[["geometry.rs",822,859]],"0:4":[["geometry.rs",822,859]],"0:5":[["geometry.rs",822,859]],"0:6":[["geometry.rs",822,859]],"1:0":[["geometry.rs",822,859]],"1:1":[["geometry.rs",822,859]],"1:2":[["geometry.rs",806,843]],"2:0":[["geometry.rs",791,828]],"2:1":[["geometry.rs",791,828]],"2:10":[["geometry.rs",791,828]],"2:11":[["geometry.rs",791,828]],"2:12":[["geometry.rs",791,828]],"2:13":[["geometry.rs",791,828]],"2:14":[["geometry.rs",791,828]],"2:15":[["geometry.rs",791,828]],"2:16":[["geometry.rs",791,828]],"2:17":[["geometry.rs",791,828]],"2:18":[["geometry.rs",791,828]],"2:19":[["geometry.rs",791,828]],"2:2":[["geometry.rs",791,828]],"2:20":[["geometry.rs",791,828]],"2:21":[["geometry.rs",791,828]],"2:22":[["geometry.rs",791,828]],"2:23":[["geometry.rs",791,828]],"2:24":[["geometry.rs",791,828]],"2:25":[["geometry.rs",791,828]],"2:26":[["geometry.rs",791,828]],"2:27":[["geometry.rs",791,828]],"2:28":[["geometry.rs",791,828]],"2:29":[["geometry.rs",791,828]],"2:3":[["geometry.rs",791,828]],"2:30":[["geometry.rs",791,828]],"2:31":[["geometry.rs",791,828]],"2:32":[["geometry.rs",791,828]],"2:33":[["geometry.rs",791,828]],"2:34":[["geometry.rs",791,828]],"2:35":[["geometry.rs",791,828]],"2:36":[["geometry.rs",791,828]],"2:37":[["geometry.rs",791,828]],"2:38":[["geometry.rs",791,828]],"2:39":[["geometry.rs",791,828]],"2:4":[["geometry.rs",791,828]],"2:40":[["geometry.rs",791,828]],"2:41":[["geometry.rs",791,828]],"2:42":[["geometry.rs",791,828]],"2:43":[["geometry.rs",791,828]],"2:44":[["geometry.rs",791,828]],"2:45":[["geometry.rs",791,828]],"2:46":[["geometry.rs",791,828]],"2:5":[["geometry.rs",791,828]],"2:6":[["geometry.rs",791,828]],"2:7":[["geometry.rs",791,828]],"2:8":[["geometry.rs",791,828]],"2:9":[["geometry.rs",791,828]],"3:0":[["geometry.rs",791,828]],"3:1":[["geometry.rs",791,828]],"3:2":[["geometry.rs",791,828]],"3:3":[["geometry.rs",791,828]],"3:4":[["geometry.rs",791,828]],"4:0":[["geometry.rs",822,859]],"4:1":[["geometry.rs",822,859]],"4:2":[["geometry.rs",822,859]],"4:3":[["geometry.rs",822,859]],"4:4":[["geometry.rs",822,859]],"4:5":[["geometry.rs",822,859]],"4:6":[["geometry.rs",822,859]],"5:0":[["geometry.rs",791,828]],"5:1":[["geometry.rs",791,828]]}},{"k":"fn:Bounds::half_perimeter","d":{"0:1":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","0:2":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","0:3":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","0:4":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","0:5":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","0:6":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","1:0":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","1:1":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","1:2":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:0":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:1":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:10":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:11":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:12":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:13":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:14":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:15":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:16":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:17":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:18":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:19":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:2":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:20":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:21":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:22":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:23":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:24":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:25":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:26":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:27":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:28":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:29":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:3":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:30":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:31":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:32":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:33":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:34":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:35":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:36":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:37":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:38":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:39":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:4":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:40":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:41":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:42":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:43":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:44":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:45":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:46":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:5":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:6":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:7":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:8":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","2:9":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","3:0":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","3:1":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","3:2":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","3:3":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","3:4":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","4:0":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","4:1":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","4:2":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","4:3":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","4:4":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","4:5":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","4:6":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","5:0":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```","5:1":"Calculates the half perimeter of a rectangle defined by the bounds.\n\nThe half perimeter is calculated as the sum of the width and the height of the rectangle.\nThis method is generic over the type `T` which must implement the `Sub` trait to allow\ncalculation of the width and height from the bounds' origin and size, as well as the `Add` trait\nto sum the width and height for the half perimeter.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet half_perimeter = bounds.half_perimeter();\nassert_eq!(half_perimeter, 30);\n```"},"s":{"0:1":"fn Bounds::half_perimeter(& self)-> T","0:2":"fn Bounds::half_perimeter(& self)-> T","0:3":"fn Bounds::half_perimeter(& self)-> T","0:4":"fn Bounds::half_perimeter(& self)-> T","0:5":"fn Bounds::half_perimeter(& self)-> T","0:6":"fn Bounds::half_perimeter(& self)-> T","1:0":"fn Bounds::half_perimeter(& self)-> T","1:1":"fn Bounds::half_perimeter(& self)-> T","1:2":"fn Bounds::half_perimeter(& self)-> T","2:0":"fn Bounds::half_perimeter(& self)-> T","2:1":"fn Bounds::half_perimeter(& self)-> T","2:10":"fn Bounds::half_perimeter(& self)-> T","2:11":"fn Bounds::half_perimeter(& self)-> T","2:12":"fn Bounds::half_perimeter(& self)-> T","2:13":"fn Bounds::half_perimeter(& self)-> T","2:14":"fn Bounds::half_perimeter(& self)-> T","2:15":"fn Bounds::half_perimeter(& self)-> T","2:16":"fn Bounds::half_perimeter(& self)-> T","2:17":"fn Bounds::half_perimeter(& self)-> T","2:18":"fn Bounds::half_perimeter(& self)-> T","2:19":"fn Bounds::half_perimeter(& self)-> T","2:2":"fn Bounds::half_perimeter(& self)-> T","2:20":"fn Bounds::half_perimeter(& self)-> T","2:21":"fn Bounds::half_perimeter(& self)-> T","2:22":"fn Bounds::half_perimeter(& self)-> T","2:23":"fn Bounds::half_perimeter(& self)-> T","2:24":"fn Bounds::half_perimeter(& self)-> T","2:25":"fn Bounds::half_perimeter(& self)-> T","2:26":"fn Bounds::half_perimeter(& self)-> T","2:27":"fn Bounds::half_perimeter(& self)-> T","2:28":"fn Bounds::half_perimeter(& self)-> T","2:29":"fn Bounds::half_perimeter(& self)-> T","2:3":"fn Bounds::half_perimeter(& self)-> T","2:30":"fn Bounds::half_perimeter(& self)-> T","2:31":"fn Bounds::half_perimeter(& self)-> T","2:32":"fn Bounds::half_perimeter(& self)-> T","2:33":"fn Bounds::half_perimeter(& self)-> T","2:34":"fn Bounds::half_perimeter(& self)-> T","2:35":"fn Bounds::half_perimeter(& self)-> T","2:36":"fn Bounds::half_perimeter(& self)-> T","2:37":"fn Bounds::half_perimeter(& self)-> T","2:38":"fn Bounds::half_perimeter(& self)-> T","2:39":"fn Bounds::half_perimeter(& self)-> T","2:4":"fn Bounds::half_perimeter(& self)-> T","2:40":"fn Bounds::half_perimeter(& self)-> T","2:41":"fn Bounds::half_perimeter(& self)-> T","2:42":"fn Bounds::half_perimeter(& self)-> T","2:43":"fn Bounds::half_perimeter(& self)-> T","2:44":"fn Bounds::half_perimeter(& self)-> T","2:45":"fn Bounds::half_perimeter(& self)-> T","2:46":"fn Bounds::half_perimeter(& self)-> T","2:5":"fn Bounds::half_perimeter(& self)-> T","2:6":"fn Bounds::half_perimeter(& self)-> T","2:7":"fn Bounds::half_perimeter(& self)-> T","2:8":"fn Bounds::half_perimeter(& self)-> T","2:9":"fn Bounds::half_perimeter(& self)-> T","3:0":"fn Bounds::half_perimeter(& self)-> T","3:1":"fn Bounds::half_perimeter(& self)-> T","3:2":"fn Bounds::half_perimeter(& self)-> T","3:3":"fn Bounds::half_perimeter(& self)-> T","3:4":"fn Bounds::half_perimeter(& self)-> T","4:0":"fn Bounds::half_perimeter(& self)-> T","4:1":"fn Bounds::half_perimeter(& self)-> T","4:2":"fn Bounds::half_perimeter(& self)-> T","4:3":"fn Bounds::half_perimeter(& self)-> T","4:4":"fn Bounds::half_perimeter(& self)-> T","4:5":"fn Bounds::half_perimeter(& self)-> T","4:6":"fn Bounds::half_perimeter(& self)-> T","5:0":"fn Bounds::half_perimeter(& self)-> T","5:1":"fn Bounds::half_perimeter(& self)-> T"},"l":{"0:1":[["geometry.rs",983,1003]],"0:2":[["geometry.rs",983,1003]],"0:3":[["geometry.rs",983,1003]],"0:4":[["geometry.rs",983,1003]],"0:5":[["geometry.rs",983,1003]],"0:6":[["geometry.rs",983,1003]],"1:0":[["geometry.rs",983,1003]],"1:1":[["geometry.rs",983,1003]],"1:2":[["geometry.rs",1025,1045]],"2:0":[["geometry.rs",952,972]],"2:1":[["geometry.rs",952,972]],"2:10":[["geometry.rs",952,972]],"2:11":[["geometry.rs",952,972]],"2:12":[["geometry.rs",1010,1030]],"2:13":[["geometry.rs",1010,1030]],"2:14":[["geometry.rs",1010,1030]],"2:15":[["geometry.rs",1010,1030]],"2:16":[["geometry.rs",1010,1030]],"2:17":[["geometry.rs",1010,1030]],"2:18":[["geometry.rs",1010,1030]],"2:19":[["geometry.rs",1010,1030]],"2:2":[["geometry.rs",952,972]],"2:20":[["geometry.rs",1010,1030]],"2:21":[["geometry.rs",1010,1030]],"2:22":[["geometry.rs",1010,1030]],"2:23":[["geometry.rs",1010,1030]],"2:24":[["geometry.rs",1010,1030]],"2:25":[["geometry.rs",1010,1030]],"2:26":[["geometry.rs",1010,1030]],"2:27":[["geometry.rs",1010,1030]],"2:28":[["geometry.rs",1010,1030]],"2:29":[["geometry.rs",1010,1030]],"2:3":[["geometry.rs",952,972]],"2:30":[["geometry.rs",1010,1030]],"2:31":[["geometry.rs",1010,1030]],"2:32":[["geometry.rs",1010,1030]],"2:33":[["geometry.rs",1010,1030]],"2:34":[["geometry.rs",1010,1030]],"2:35":[["geometry.rs",1010,1030]],"2:36":[["geometry.rs",1010,1030]],"2:37":[["geometry.rs",1010,1030]],"2:38":[["geometry.rs",1010,1030]],"2:39":[["geometry.rs",1010,1030]],"2:4":[["geometry.rs",952,972]],"2:40":[["geometry.rs",1010,1030]],"2:41":[["geometry.rs",1010,1030]],"2:42":[["geometry.rs",1010,1030]],"2:43":[["geometry.rs",1010,1030]],"2:44":[["geometry.rs",1010,1030]],"2:45":[["geometry.rs",1010,1030]],"2:46":[["geometry.rs",1010,1030]],"2:5":[["geometry.rs",952,972]],"2:6":[["geometry.rs",952,972]],"2:7":[["geometry.rs",952,972]],"2:8":[["geometry.rs",952,972]],"2:9":[["geometry.rs",952,972]],"3:0":[["geometry.rs",1010,1030]],"3:1":[["geometry.rs",1010,1030]],"3:2":[["geometry.rs",1010,1030]],"3:3":[["geometry.rs",1010,1030]],"3:4":[["geometry.rs",1010,1030]],"4:0":[["geometry.rs",983,1003]],"4:1":[["geometry.rs",983,1003]],"4:2":[["geometry.rs",983,1003]],"4:3":[["geometry.rs",983,1003]],"4:4":[["geometry.rs",983,1003]],"4:5":[["geometry.rs",983,1003]],"4:6":[["geometry.rs",983,1003]],"5:0":[["geometry.rs",1010,1030]],"5:1":[["geometry.rs",1010,1030]]}},{"k":"fn:Bounds::inset","d":{"0:1":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","0:2":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","0:3":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","0:4":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","0:5":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","0:6":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","1:0":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","1:1":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","1:2":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:0":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:1":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:10":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:11":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:12":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:13":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:14":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:15":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:16":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:17":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:18":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:19":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:2":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:20":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:21":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:22":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:23":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:24":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:25":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:26":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:27":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:28":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:29":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:3":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:30":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:31":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:32":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:33":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:34":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:35":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:36":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:37":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:38":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:39":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:4":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:40":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:41":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:42":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:43":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:44":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:45":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:46":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:5":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:6":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:7":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:8":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","2:9":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","3:0":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","3:1":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","3:2":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","3:3":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","3:4":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","4:0":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","4:1":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","4:2":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","4:3":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","4:4":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","4:5":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","4:6":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","5:0":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values.","5:1":"Inset the bounds by a specified amount. Equivalent to `dilate` with the amount negated.\n\nNote that this may panic if T does not support negative values."},"s":{"0:1":"fn Bounds::inset(& self, T)-> Self","0:2":"fn Bounds::inset(& self, T)-> Self","0:3":"fn Bounds::inset(& self, T)-> Self","0:4":"fn Bounds::inset(& self, T)-> Self","0:5":"fn Bounds::inset(& self, T)-> Self","0:6":"fn Bounds::inset(& self, T)-> Self","1:0":"fn Bounds::inset(& self, T)-> Self","1:1":"fn Bounds::inset(& self, T)-> Self","1:2":"fn Bounds::inset(& self, T)-> Self","2:0":"fn Bounds::inset(& self, T)-> Self","2:1":"fn Bounds::inset(& self, T)-> Self","2:10":"fn Bounds::inset(& self, T)-> Self","2:11":"fn Bounds::inset(& self, T)-> Self","2:12":"fn Bounds::inset(& self, T)-> Self","2:13":"fn Bounds::inset(& self, T)-> Self","2:14":"fn Bounds::inset(& self, T)-> Self","2:15":"fn Bounds::inset(& self, T)-> Self","2:16":"fn Bounds::inset(& self, T)-> Self","2:17":"fn Bounds::inset(& self, T)-> Self","2:18":"fn Bounds::inset(& self, T)-> Self","2:19":"fn Bounds::inset(& self, T)-> Self","2:2":"fn Bounds::inset(& self, T)-> Self","2:20":"fn Bounds::inset(& self, T)-> Self","2:21":"fn Bounds::inset(& self, T)-> Self","2:22":"fn Bounds::inset(& self, T)-> Self","2:23":"fn Bounds::inset(& self, T)-> Self","2:24":"fn Bounds::inset(& self, T)-> Self","2:25":"fn Bounds::inset(& self, T)-> Self","2:26":"fn Bounds::inset(& self, T)-> Self","2:27":"fn Bounds::inset(& self, T)-> Self","2:28":"fn Bounds::inset(& self, T)-> Self","2:29":"fn Bounds::inset(& self, T)-> Self","2:3":"fn Bounds::inset(& self, T)-> Self","2:30":"fn Bounds::inset(& self, T)-> Self","2:31":"fn Bounds::inset(& self, T)-> Self","2:32":"fn Bounds::inset(& self, T)-> Self","2:33":"fn Bounds::inset(& self, T)-> Self","2:34":"fn Bounds::inset(& self, T)-> Self","2:35":"fn Bounds::inset(& self, T)-> Self","2:36":"fn Bounds::inset(& self, T)-> Self","2:37":"fn Bounds::inset(& self, T)-> Self","2:38":"fn Bounds::inset(& self, T)-> Self","2:39":"fn Bounds::inset(& self, T)-> Self","2:4":"fn Bounds::inset(& self, T)-> Self","2:40":"fn Bounds::inset(& self, T)-> Self","2:41":"fn Bounds::inset(& self, T)-> Self","2:42":"fn Bounds::inset(& self, T)-> Self","2:43":"fn Bounds::inset(& self, T)-> Self","2:44":"fn Bounds::inset(& self, T)-> Self","2:45":"fn Bounds::inset(& self, T)-> Self","2:46":"fn Bounds::inset(& self, T)-> Self","2:5":"fn Bounds::inset(& self, T)-> Self","2:6":"fn Bounds::inset(& self, T)-> Self","2:7":"fn Bounds::inset(& self, T)-> Self","2:8":"fn Bounds::inset(& self, T)-> Self","2:9":"fn Bounds::inset(& self, T)-> Self","3:0":"fn Bounds::inset(& self, T)-> Self","3:1":"fn Bounds::inset(& self, T)-> Self","3:2":"fn Bounds::inset(& self, T)-> Self","3:3":"fn Bounds::inset(& self, T)-> Self","3:4":"fn Bounds::inset(& self, T)-> Self","4:0":"fn Bounds::inset(& self, T)-> Self","4:1":"fn Bounds::inset(& self, T)-> Self","4:2":"fn Bounds::inset(& self, T)-> Self","4:3":"fn Bounds::inset(& self, T)-> Self","4:4":"fn Bounds::inset(& self, T)-> Self","4:5":"fn Bounds::inset(& self, T)-> Self","4:6":"fn Bounds::inset(& self, T)-> Self","5:0":"fn Bounds::inset(& self, T)-> Self","5:1":"fn Bounds::inset(& self, T)-> Self"},"l":{"0:1":[["geometry.rs",1069,1074]],"0:2":[["geometry.rs",1069,1074]],"0:3":[["geometry.rs",1069,1074]],"0:4":[["geometry.rs",1069,1074]],"0:5":[["geometry.rs",1069,1074]],"0:6":[["geometry.rs",1069,1074]],"1:0":[["geometry.rs",1069,1074]],"1:1":[["geometry.rs",1069,1074]],"1:2":[["geometry.rs",1111,1116]],"2:0":[["geometry.rs",1038,1043]],"2:1":[["geometry.rs",1038,1043]],"2:10":[["geometry.rs",1038,1043]],"2:11":[["geometry.rs",1038,1043]],"2:12":[["geometry.rs",1096,1101]],"2:13":[["geometry.rs",1096,1101]],"2:14":[["geometry.rs",1096,1101]],"2:15":[["geometry.rs",1096,1101]],"2:16":[["geometry.rs",1096,1101]],"2:17":[["geometry.rs",1096,1101]],"2:18":[["geometry.rs",1096,1101]],"2:19":[["geometry.rs",1096,1101]],"2:2":[["geometry.rs",1038,1043]],"2:20":[["geometry.rs",1096,1101]],"2:21":[["geometry.rs",1096,1101]],"2:22":[["geometry.rs",1096,1101]],"2:23":[["geometry.rs",1096,1101]],"2:24":[["geometry.rs",1096,1101]],"2:25":[["geometry.rs",1096,1101]],"2:26":[["geometry.rs",1096,1101]],"2:27":[["geometry.rs",1096,1101]],"2:28":[["geometry.rs",1096,1101]],"2:29":[["geometry.rs",1096,1101]],"2:3":[["geometry.rs",1038,1043]],"2:30":[["geometry.rs",1096,1101]],"2:31":[["geometry.rs",1096,1101]],"2:32":[["geometry.rs",1096,1101]],"2:33":[["geometry.rs",1096,1101]],"2:34":[["geometry.rs",1096,1101]],"2:35":[["geometry.rs",1096,1101]],"2:36":[["geometry.rs",1096,1101]],"2:37":[["geometry.rs",1096,1101]],"2:38":[["geometry.rs",1096,1101]],"2:39":[["geometry.rs",1096,1101]],"2:4":[["geometry.rs",1038,1043]],"2:40":[["geometry.rs",1096,1101]],"2:41":[["geometry.rs",1096,1101]],"2:42":[["geometry.rs",1096,1101]],"2:43":[["geometry.rs",1096,1101]],"2:44":[["geometry.rs",1096,1101]],"2:45":[["geometry.rs",1096,1101]],"2:46":[["geometry.rs",1096,1101]],"2:5":[["geometry.rs",1038,1043]],"2:6":[["geometry.rs",1038,1043]],"2:7":[["geometry.rs",1038,1043]],"2:8":[["geometry.rs",1038,1043]],"2:9":[["geometry.rs",1038,1043]],"3:0":[["geometry.rs",1096,1101]],"3:1":[["geometry.rs",1096,1101]],"3:2":[["geometry.rs",1096,1101]],"3:3":[["geometry.rs",1096,1101]],"3:4":[["geometry.rs",1096,1101]],"4:0":[["geometry.rs",1082,1087]],"4:1":[["geometry.rs",1082,1087]],"4:2":[["geometry.rs",1082,1087]],"4:3":[["geometry.rs",1082,1087]],"4:4":[["geometry.rs",1082,1087]],"4:5":[["geometry.rs",1082,1087]],"4:6":[["geometry.rs",1082,1087]],"5:0":[["geometry.rs",1096,1101]],"5:1":[["geometry.rs",1096,1101]]}},{"k":"fn:Bounds::intersect","d":{"0:1":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","0:2":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","0:3":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","0:4":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","0:5":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","0:6":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","1:0":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","1:1":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","1:2":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:0":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:1":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:10":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:11":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:12":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:13":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:14":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:15":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:16":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:17":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:18":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:19":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:2":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:20":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:21":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:22":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:23":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:24":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:25":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:26":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:27":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:28":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:29":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:3":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:30":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:31":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:32":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:33":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:34":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:35":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:36":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:37":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:38":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:39":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:4":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:40":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:41":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:42":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:43":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:44":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:45":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:46":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:5":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:6":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:7":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:8":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","2:9":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","3:0":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","3:1":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","3:2":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","3:3":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","3:4":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","4:0":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","4:1":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","4:2":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","4:3":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","4:4":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","4:5":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","4:6":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","5:0":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```","5:1":"Calculates the intersection of two `Bounds` objects.\n\nThis method computes the overlapping region of two `Bounds`. If the bounds do not intersect,\nthe resulting `Bounds` will have a size with width and height of zero.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to intersect with.\n\n# Returns\n\nReturns a `Bounds` representing the intersection area. If there is no intersection,\nthe returned `Bounds` will have a size with width and height of zero.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet intersection = bounds1.intersect(&bounds2);\n\nassert_eq!(intersection, Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 5, height: 5 },\n});\n```"},"s":{"0:1":"fn Bounds::intersect(& self, & Self)-> Self","0:2":"fn Bounds::intersect(& self, & Self)-> Self","0:3":"fn Bounds::intersect(& self, & Self)-> Self","0:4":"fn Bounds::intersect(& self, & Self)-> Self","0:5":"fn Bounds::intersect(& self, & Self)-> Self","0:6":"fn Bounds::intersect(& self, & Self)-> Self","1:0":"fn Bounds::intersect(& self, & Self)-> Self","1:1":"fn Bounds::intersect(& self, & Self)-> Self","1:2":"fn Bounds::intersect(& self, & Self)-> Self","2:0":"fn Bounds::intersect(& self, & Self)-> Self","2:1":"fn Bounds::intersect(& self, & Self)-> Self","2:10":"fn Bounds::intersect(& self, & Self)-> Self","2:11":"fn Bounds::intersect(& self, & Self)-> Self","2:12":"fn Bounds::intersect(& self, & Self)-> Self","2:13":"fn Bounds::intersect(& self, & Self)-> Self","2:14":"fn Bounds::intersect(& self, & Self)-> Self","2:15":"fn Bounds::intersect(& self, & Self)-> Self","2:16":"fn Bounds::intersect(& self, & Self)-> Self","2:17":"fn Bounds::intersect(& self, & Self)-> Self","2:18":"fn Bounds::intersect(& self, & Self)-> Self","2:19":"fn Bounds::intersect(& self, & Self)-> Self","2:2":"fn Bounds::intersect(& self, & Self)-> Self","2:20":"fn Bounds::intersect(& self, & Self)-> Self","2:21":"fn Bounds::intersect(& self, & Self)-> Self","2:22":"fn Bounds::intersect(& self, & Self)-> Self","2:23":"fn Bounds::intersect(& self, & Self)-> Self","2:24":"fn Bounds::intersect(& self, & Self)-> Self","2:25":"fn Bounds::intersect(& self, & Self)-> Self","2:26":"fn Bounds::intersect(& self, & Self)-> Self","2:27":"fn Bounds::intersect(& self, & Self)-> Self","2:28":"fn Bounds::intersect(& self, & Self)-> Self","2:29":"fn Bounds::intersect(& self, & Self)-> Self","2:3":"fn Bounds::intersect(& self, & Self)-> Self","2:30":"fn Bounds::intersect(& self, & Self)-> Self","2:31":"fn Bounds::intersect(& self, & Self)-> Self","2:32":"fn Bounds::intersect(& self, & Self)-> Self","2:33":"fn Bounds::intersect(& self, & Self)-> Self","2:34":"fn Bounds::intersect(& self, & Self)-> Self","2:35":"fn Bounds::intersect(& self, & Self)-> Self","2:36":"fn Bounds::intersect(& self, & Self)-> Self","2:37":"fn Bounds::intersect(& self, & Self)-> Self","2:38":"fn Bounds::intersect(& self, & Self)-> Self","2:39":"fn Bounds::intersect(& self, & Self)-> Self","2:4":"fn Bounds::intersect(& self, & Self)-> Self","2:40":"fn Bounds::intersect(& self, & Self)-> Self","2:41":"fn Bounds::intersect(& self, & Self)-> Self","2:42":"fn Bounds::intersect(& self, & Self)-> Self","2:43":"fn Bounds::intersect(& self, & Self)-> Self","2:44":"fn Bounds::intersect(& self, & Self)-> Self","2:45":"fn Bounds::intersect(& self, & Self)-> Self","2:46":"fn Bounds::intersect(& self, & Self)-> Self","2:5":"fn Bounds::intersect(& self, & Self)-> Self","2:6":"fn Bounds::intersect(& self, & Self)-> Self","2:7":"fn Bounds::intersect(& self, & Self)-> Self","2:8":"fn Bounds::intersect(& self, & Self)-> Self","2:9":"fn Bounds::intersect(& self, & Self)-> Self","3:0":"fn Bounds::intersect(& self, & Self)-> Self","3:1":"fn Bounds::intersect(& self, & Self)-> Self","3:2":"fn Bounds::intersect(& self, & Self)-> Self","3:3":"fn Bounds::intersect(& self, & Self)-> Self","3:4":"fn Bounds::intersect(& self, & Self)-> Self","4:0":"fn Bounds::intersect(& self, & Self)-> Self","4:1":"fn Bounds::intersect(& self, & Self)-> Self","4:2":"fn Bounds::intersect(& self, & Self)-> Self","4:3":"fn Bounds::intersect(& self, & Self)-> Self","4:4":"fn Bounds::intersect(& self, & Self)-> Self","4:5":"fn Bounds::intersect(& self, & Self)-> Self","4:6":"fn Bounds::intersect(& self, & Self)-> Self","5:0":"fn Bounds::intersect(& self, & Self)-> Self","5:1":"fn Bounds::intersect(& self, & Self)-> Self"},"l":{"0:1":[["geometry.rs",1080,1117]],"0:2":[["geometry.rs",1080,1117]],"0:3":[["geometry.rs",1080,1117]],"0:4":[["geometry.rs",1080,1117]],"0:5":[["geometry.rs",1080,1117]],"0:6":[["geometry.rs",1080,1117]],"1:0":[["geometry.rs",1080,1117]],"1:1":[["geometry.rs",1080,1117]],"1:2":[["geometry.rs",1122,1162]],"2:0":[["geometry.rs",1049,1089]],"2:1":[["geometry.rs",1049,1089]],"2:10":[["geometry.rs",1049,1089]],"2:11":[["geometry.rs",1049,1089]],"2:12":[["geometry.rs",1107,1147]],"2:13":[["geometry.rs",1107,1147]],"2:14":[["geometry.rs",1107,1147]],"2:15":[["geometry.rs",1107,1147]],"2:16":[["geometry.rs",1107,1147]],"2:17":[["geometry.rs",1107,1147]],"2:18":[["geometry.rs",1107,1147]],"2:19":[["geometry.rs",1107,1147]],"2:2":[["geometry.rs",1049,1089]],"2:20":[["geometry.rs",1107,1147]],"2:21":[["geometry.rs",1107,1147]],"2:22":[["geometry.rs",1107,1147]],"2:23":[["geometry.rs",1107,1147]],"2:24":[["geometry.rs",1107,1147]],"2:25":[["geometry.rs",1107,1147]],"2:26":[["geometry.rs",1107,1147]],"2:27":[["geometry.rs",1107,1147]],"2:28":[["geometry.rs",1107,1147]],"2:29":[["geometry.rs",1107,1147]],"2:3":[["geometry.rs",1049,1089]],"2:30":[["geometry.rs",1107,1147]],"2:31":[["geometry.rs",1107,1147]],"2:32":[["geometry.rs",1107,1147]],"2:33":[["geometry.rs",1107,1147]],"2:34":[["geometry.rs",1107,1147]],"2:35":[["geometry.rs",1107,1147]],"2:36":[["geometry.rs",1107,1147]],"2:37":[["geometry.rs",1107,1147]],"2:38":[["geometry.rs",1107,1147]],"2:39":[["geometry.rs",1107,1147]],"2:4":[["geometry.rs",1049,1089]],"2:40":[["geometry.rs",1107,1147]],"2:41":[["geometry.rs",1107,1147]],"2:42":[["geometry.rs",1107,1147]],"2:43":[["geometry.rs",1107,1147]],"2:44":[["geometry.rs",1107,1147]],"2:45":[["geometry.rs",1107,1147]],"2:46":[["geometry.rs",1107,1147]],"2:5":[["geometry.rs",1049,1089]],"2:6":[["geometry.rs",1049,1089]],"2:7":[["geometry.rs",1049,1089]],"2:8":[["geometry.rs",1049,1089]],"2:9":[["geometry.rs",1049,1089]],"3:0":[["geometry.rs",1107,1147]],"3:1":[["geometry.rs",1107,1147]],"3:2":[["geometry.rs",1107,1147]],"3:3":[["geometry.rs",1107,1147]],"3:4":[["geometry.rs",1107,1147]],"4:0":[["geometry.rs",1093,1130]],"4:1":[["geometry.rs",1093,1130]],"4:2":[["geometry.rs",1093,1130]],"4:3":[["geometry.rs",1093,1130]],"4:4":[["geometry.rs",1093,1130]],"4:5":[["geometry.rs",1093,1130]],"4:6":[["geometry.rs",1093,1130]],"5:0":[["geometry.rs",1107,1147]],"5:1":[["geometry.rs",1107,1147]]}},{"k":"fn:Bounds::intersects","d":{"0:1":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","0:2":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","0:3":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","0:4":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","0:5":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","0:6":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","1:0":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","1:1":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","1:2":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:0":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:1":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:10":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:11":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:12":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:13":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:14":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:15":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:16":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:17":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:18":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:19":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:2":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:20":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:21":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:22":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:23":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:24":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:25":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:26":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:27":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:28":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:29":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:3":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:30":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:31":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:32":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:33":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:34":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:35":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:36":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:37":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:38":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:39":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:4":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:40":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:41":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:42":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:43":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:44":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:45":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:46":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:5":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:6":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:7":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:8":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","2:9":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","3:0":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","3:1":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","3:2":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","3:3":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","3:4":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","4:0":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","4:1":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","4:2":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","4:3":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","4:4":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","4:5":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","4:6":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","5:0":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```","5:1":"Checks if this `Bounds` intersects with another `Bounds`.\n\nTwo `Bounds` instances intersect if they overlap in the 2D space they occupy.\nThis method checks if there is any overlapping area between the two bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to check for intersection with.\n\n# Returns\n\nReturns `true` if there is any intersection between the two bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds3 = Bounds {\n    origin: Point { x: 20, y: 20 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert_eq!(bounds1.intersects(&bounds2), true); // Overlapping bounds\nassert_eq!(bounds1.intersects(&bounds3), false); // Non-overlapping bounds\n```"},"s":{"0:1":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","0:2":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","0:3":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","0:4":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","0:5":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","0:6":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","1:0":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","1:1":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","1:2":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:0":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:1":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:10":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:11":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:12":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:13":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:14":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:15":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:16":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:17":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:18":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:19":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:2":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:20":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:21":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:22":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:23":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:24":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:25":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:26":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:27":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:28":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:29":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:3":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:30":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:31":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:32":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:33":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:34":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:35":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:36":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:37":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:38":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:39":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:4":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:40":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:41":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:42":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:43":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:44":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:45":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:46":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:5":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:6":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:7":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:8":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","2:9":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","3:0":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","3:1":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","3:2":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","3:3":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","3:4":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","4:0":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","4:1":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","4:2":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","4:3":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","4:4":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","4:5":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","4:6":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","5:0":"fn Bounds::intersects(& self, & Bounds < T >)-> bool","5:1":"fn Bounds::intersects(& self, & Bounds < T >)-> bool"},"l":{"0:1":[["geometry.rs",902,943]],"0:2":[["geometry.rs",902,943]],"0:3":[["geometry.rs",902,943]],"0:4":[["geometry.rs",902,943]],"0:5":[["geometry.rs",902,943]],"0:6":[["geometry.rs",902,943]],"1:0":[["geometry.rs",902,943]],"1:1":[["geometry.rs",902,943]],"1:2":[["geometry.rs",944,985]],"2:0":[["geometry.rs",871,912]],"2:1":[["geometry.rs",871,912]],"2:10":[["geometry.rs",871,912]],"2:11":[["geometry.rs",871,912]],"2:12":[["geometry.rs",929,970]],"2:13":[["geometry.rs",929,970]],"2:14":[["geometry.rs",929,970]],"2:15":[["geometry.rs",929,970]],"2:16":[["geometry.rs",929,970]],"2:17":[["geometry.rs",929,970]],"2:18":[["geometry.rs",929,970]],"2:19":[["geometry.rs",929,970]],"2:2":[["geometry.rs",871,912]],"2:20":[["geometry.rs",929,970]],"2:21":[["geometry.rs",929,970]],"2:22":[["geometry.rs",929,970]],"2:23":[["geometry.rs",929,970]],"2:24":[["geometry.rs",929,970]],"2:25":[["geometry.rs",929,970]],"2:26":[["geometry.rs",929,970]],"2:27":[["geometry.rs",929,970]],"2:28":[["geometry.rs",929,970]],"2:29":[["geometry.rs",929,970]],"2:3":[["geometry.rs",871,912]],"2:30":[["geometry.rs",929,970]],"2:31":[["geometry.rs",929,970]],"2:32":[["geometry.rs",929,970]],"2:33":[["geometry.rs",929,970]],"2:34":[["geometry.rs",929,970]],"2:35":[["geometry.rs",929,970]],"2:36":[["geometry.rs",929,970]],"2:37":[["geometry.rs",929,970]],"2:38":[["geometry.rs",929,970]],"2:39":[["geometry.rs",929,970]],"2:4":[["geometry.rs",871,912]],"2:40":[["geometry.rs",929,970]],"2:41":[["geometry.rs",929,970]],"2:42":[["geometry.rs",929,970]],"2:43":[["geometry.rs",929,970]],"2:44":[["geometry.rs",929,970]],"2:45":[["geometry.rs",929,970]],"2:46":[["geometry.rs",929,970]],"2:5":[["geometry.rs",871,912]],"2:6":[["geometry.rs",871,912]],"2:7":[["geometry.rs",871,912]],"2:8":[["geometry.rs",871,912]],"2:9":[["geometry.rs",871,912]],"3:0":[["geometry.rs",929,970]],"3:1":[["geometry.rs",929,970]],"3:2":[["geometry.rs",929,970]],"3:3":[["geometry.rs",929,970]],"3:4":[["geometry.rs",929,970]],"4:0":[["geometry.rs",902,943]],"4:1":[["geometry.rs",902,943]],"4:2":[["geometry.rs",902,943]],"4:3":[["geometry.rs",902,943]],"4:4":[["geometry.rs",902,943]],"4:5":[["geometry.rs",902,943]],"4:6":[["geometry.rs",902,943]],"5:0":[["geometry.rs",929,970]],"5:1":[["geometry.rs",929,970]]}},{"k":"fn:Bounds::is_contained_within","d":{"0:1":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","0:2":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","0:3":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","0:4":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","0:5":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","0:6":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","1:0":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","1:1":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","1:2":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:0":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:1":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:10":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:11":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:12":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:13":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:14":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:15":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:16":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:17":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:18":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:19":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:2":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:20":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:21":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:22":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:23":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:24":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:25":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:26":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:27":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:28":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:29":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:3":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:30":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:31":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:32":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:33":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:34":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:35":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:36":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:37":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:38":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:39":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:4":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:40":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:41":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:42":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:43":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:44":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:45":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:46":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:5":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:6":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:7":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:8":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","2:9":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","3:0":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","3:1":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","3:2":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","3:3":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","3:4":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","4:0":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","4:1":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","4:2":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","4:3":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","4:4":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","4:5":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","4:6":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","5:0":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```","5:1":"Checks if this bounds is completely contained within another bounds.\n\nThis method determines whether the current bounds is entirely enclosed by the given bounds.\nA bounds is considered to be contained within another if its origin (top-left corner) and\nits bottom-right corner are both contained within the other bounds.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` that might contain this bounds.\n\n# Returns\n\nReturns `true` if this bounds is completely inside the other bounds, `false` otherwise.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet outer_bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n};\nlet inner_bounds = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 10, height: 10 },\n};\nlet overlapping_bounds = Bounds {\n    origin: Point { x: 15, y: 15 },\n    size: Size { width: 10, height: 10 },\n};\n\nassert!(inner_bounds.is_contained_within(&outer_bounds));\nassert!(!overlapping_bounds.is_contained_within(&outer_bounds));\n```"},"s":{"0:1":"fn Bounds::is_contained_within(& self, & Self)-> bool","0:2":"fn Bounds::is_contained_within(& self, & Self)-> bool","0:3":"fn Bounds::is_contained_within(& self, & Self)-> bool","0:4":"fn Bounds::is_contained_within(& self, & Self)-> bool","0:5":"fn Bounds::is_contained_within(& self, & Self)-> bool","0:6":"fn Bounds::is_contained_within(& self, & Self)-> bool","1:0":"fn Bounds::is_contained_within(& self, & Self)-> bool","1:1":"fn Bounds::is_contained_within(& self, & Self)-> bool","1:2":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:0":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:1":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:10":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:11":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:12":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:13":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:14":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:15":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:16":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:17":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:18":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:19":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:2":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:20":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:21":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:22":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:23":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:24":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:25":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:26":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:27":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:28":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:29":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:3":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:30":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:31":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:32":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:33":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:34":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:35":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:36":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:37":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:38":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:39":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:4":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:40":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:41":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:42":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:43":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:44":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:45":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:46":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:5":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:6":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:7":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:8":"fn Bounds::is_contained_within(& self, & Self)-> bool","2:9":"fn Bounds::is_contained_within(& self, & Self)-> bool","3:0":"fn Bounds::is_contained_within(& self, & Self)-> bool","3:1":"fn Bounds::is_contained_within(& self, & Self)-> bool","3:2":"fn Bounds::is_contained_within(& self, & Self)-> bool","3:3":"fn Bounds::is_contained_within(& self, & Self)-> bool","3:4":"fn Bounds::is_contained_within(& self, & Self)-> bool","4:0":"fn Bounds::is_contained_within(& self, & Self)-> bool","4:1":"fn Bounds::is_contained_within(& self, & Self)-> bool","4:2":"fn Bounds::is_contained_within(& self, & Self)-> bool","4:3":"fn Bounds::is_contained_within(& self, & Self)-> bool","4:4":"fn Bounds::is_contained_within(& self, & Self)-> bool","4:5":"fn Bounds::is_contained_within(& self, & Self)-> bool","4:6":"fn Bounds::is_contained_within(& self, & Self)-> bool","5:0":"fn Bounds::is_contained_within(& self, & Self)-> bool","5:1":"fn Bounds::is_contained_within(& self, & Self)-> bool"},"l":{"0:1":[["geometry.rs",1424,1460]],"0:2":[["geometry.rs",1424,1460]],"0:3":[["geometry.rs",1424,1460]],"0:4":[["geometry.rs",1424,1460]],"0:5":[["geometry.rs",1424,1460]],"0:6":[["geometry.rs",1424,1460]],"1:0":[["geometry.rs",1424,1460]],"1:1":[["geometry.rs",1424,1460]],"1:2":[["geometry.rs",1487,1523]],"2:0":[["geometry.rs",1405,1441]],"2:1":[["geometry.rs",1405,1441]],"2:10":[["geometry.rs",1405,1441]],"2:11":[["geometry.rs",1405,1441]],"2:12":[["geometry.rs",1472,1508]],"2:13":[["geometry.rs",1472,1508]],"2:14":[["geometry.rs",1472,1508]],"2:15":[["geometry.rs",1472,1508]],"2:16":[["geometry.rs",1472,1508]],"2:17":[["geometry.rs",1472,1508]],"2:18":[["geometry.rs",1472,1508]],"2:19":[["geometry.rs",1472,1508]],"2:2":[["geometry.rs",1405,1441]],"2:20":[["geometry.rs",1472,1508]],"2:21":[["geometry.rs",1472,1508]],"2:22":[["geometry.rs",1472,1508]],"2:23":[["geometry.rs",1472,1508]],"2:24":[["geometry.rs",1472,1508]],"2:25":[["geometry.rs",1472,1508]],"2:26":[["geometry.rs",1472,1508]],"2:27":[["geometry.rs",1472,1508]],"2:28":[["geometry.rs",1472,1508]],"2:29":[["geometry.rs",1472,1508]],"2:3":[["geometry.rs",1405,1441]],"2:30":[["geometry.rs",1472,1508]],"2:31":[["geometry.rs",1472,1508]],"2:32":[["geometry.rs",1472,1508]],"2:33":[["geometry.rs",1472,1508]],"2:34":[["geometry.rs",1472,1508]],"2:35":[["geometry.rs",1472,1508]],"2:36":[["geometry.rs",1472,1508]],"2:37":[["geometry.rs",1472,1508]],"2:38":[["geometry.rs",1472,1508]],"2:39":[["geometry.rs",1472,1508]],"2:4":[["geometry.rs",1405,1441]],"2:40":[["geometry.rs",1472,1508]],"2:41":[["geometry.rs",1472,1508]],"2:42":[["geometry.rs",1472,1508]],"2:43":[["geometry.rs",1472,1508]],"2:44":[["geometry.rs",1472,1508]],"2:45":[["geometry.rs",1472,1508]],"2:46":[["geometry.rs",1472,1508]],"2:5":[["geometry.rs",1405,1441]],"2:6":[["geometry.rs",1405,1441]],"2:7":[["geometry.rs",1405,1441]],"2:8":[["geometry.rs",1405,1441]],"2:9":[["geometry.rs",1405,1441]],"3:0":[["geometry.rs",1472,1508]],"3:1":[["geometry.rs",1472,1508]],"3:2":[["geometry.rs",1472,1508]],"3:3":[["geometry.rs",1472,1508]],"3:4":[["geometry.rs",1472,1508]],"4:0":[["geometry.rs",1437,1473]],"4:1":[["geometry.rs",1437,1473]],"4:2":[["geometry.rs",1437,1473]],"4:3":[["geometry.rs",1437,1473]],"4:4":[["geometry.rs",1437,1473]],"4:5":[["geometry.rs",1437,1473]],"4:6":[["geometry.rs",1437,1473]],"5:0":[["geometry.rs",1472,1508]],"5:1":[["geometry.rs",1472,1508]]}},{"k":"fn:Bounds::is_empty","d":{"0:1":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","0:2":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","0:3":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","0:4":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","0:5":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","0:6":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","1:0":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","1:1":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","1:2":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:0":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:1":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:10":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:11":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:12":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:13":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:14":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:15":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:16":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:17":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:18":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:19":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:2":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:20":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:21":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:22":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:23":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:24":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:25":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:26":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:27":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:28":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:29":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:3":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:30":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:31":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:32":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:33":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:34":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:35":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:36":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:37":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:38":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:39":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:4":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:40":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:41":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:42":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:43":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:44":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:45":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:46":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:5":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:6":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:7":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:8":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","2:9":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","3:0":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","3:1":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","3:2":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","3:3":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","3:4":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","4:0":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","4:1":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","4:2":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","4:3":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","4:4":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","4:5":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","4:6":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","5:0":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area.","5:1":"Checks if the bounds represent an empty area.\n\n# Returns\n\nReturns `true` if either the width or the height of the bounds is less than or equal to zero, indicating an empty area."},"s":{"0:1":"fn Bounds::is_empty(& self)-> bool","0:2":"fn Bounds::is_empty(& self)-> bool","0:3":"fn Bounds::is_empty(& self)-> bool","0:4":"fn Bounds::is_empty(& self)-> bool","0:5":"fn Bounds::is_empty(& self)-> bool","0:6":"fn Bounds::is_empty(& self)-> bool","1:0":"fn Bounds::is_empty(& self)-> bool","1:1":"fn Bounds::is_empty(& self)-> bool","1:2":"fn Bounds::is_empty(& self)-> bool","2:0":"fn Bounds::is_empty(& self)-> bool","2:1":"fn Bounds::is_empty(& self)-> bool","2:10":"fn Bounds::is_empty(& self)-> bool","2:11":"fn Bounds::is_empty(& self)-> bool","2:12":"fn Bounds::is_empty(& self)-> bool","2:13":"fn Bounds::is_empty(& self)-> bool","2:14":"fn Bounds::is_empty(& self)-> bool","2:15":"fn Bounds::is_empty(& self)-> bool","2:16":"fn Bounds::is_empty(& self)-> bool","2:17":"fn Bounds::is_empty(& self)-> bool","2:18":"fn Bounds::is_empty(& self)-> bool","2:19":"fn Bounds::is_empty(& self)-> bool","2:2":"fn Bounds::is_empty(& self)-> bool","2:20":"fn Bounds::is_empty(& self)-> bool","2:21":"fn Bounds::is_empty(& self)-> bool","2:22":"fn Bounds::is_empty(& self)-> bool","2:23":"fn Bounds::is_empty(& self)-> bool","2:24":"fn Bounds::is_empty(& self)-> bool","2:25":"fn Bounds::is_empty(& self)-> bool","2:26":"fn Bounds::is_empty(& self)-> bool","2:27":"fn Bounds::is_empty(& self)-> bool","2:28":"fn Bounds::is_empty(& self)-> bool","2:29":"fn Bounds::is_empty(& self)-> bool","2:3":"fn Bounds::is_empty(& self)-> bool","2:30":"fn Bounds::is_empty(& self)-> bool","2:31":"fn Bounds::is_empty(& self)-> bool","2:32":"fn Bounds::is_empty(& self)-> bool","2:33":"fn Bounds::is_empty(& self)-> bool","2:34":"fn Bounds::is_empty(& self)-> bool","2:35":"fn Bounds::is_empty(& self)-> bool","2:36":"fn Bounds::is_empty(& self)-> bool","2:37":"fn Bounds::is_empty(& self)-> bool","2:38":"fn Bounds::is_empty(& self)-> bool","2:39":"fn Bounds::is_empty(& self)-> bool","2:4":"fn Bounds::is_empty(& self)-> bool","2:40":"fn Bounds::is_empty(& self)-> bool","2:41":"fn Bounds::is_empty(& self)-> bool","2:42":"fn Bounds::is_empty(& self)-> bool","2:43":"fn Bounds::is_empty(& self)-> bool","2:44":"fn Bounds::is_empty(& self)-> bool","2:45":"fn Bounds::is_empty(& self)-> bool","2:46":"fn Bounds::is_empty(& self)-> bool","2:5":"fn Bounds::is_empty(& self)-> bool","2:6":"fn Bounds::is_empty(& self)-> bool","2:7":"fn Bounds::is_empty(& self)-> bool","2:8":"fn Bounds::is_empty(& self)-> bool","2:9":"fn Bounds::is_empty(& self)-> bool","3:0":"fn Bounds::is_empty(& self)-> bool","3:1":"fn Bounds::is_empty(& self)-> bool","3:2":"fn Bounds::is_empty(& self)-> bool","3:3":"fn Bounds::is_empty(& self)-> bool","3:4":"fn Bounds::is_empty(& self)-> bool","4:0":"fn Bounds::is_empty(& self)-> bool","4:1":"fn Bounds::is_empty(& self)-> bool","4:2":"fn Bounds::is_empty(& self)-> bool","4:3":"fn Bounds::is_empty(& self)-> bool","4:4":"fn Bounds::is_empty(& self)-> bool","4:5":"fn Bounds::is_empty(& self)-> bool","4:6":"fn Bounds::is_empty(& self)-> bool","5:0":"fn Bounds::is_empty(& self)-> bool","5:1":"fn Bounds::is_empty(& self)-> bool"},"l":{"0:1":[["geometry.rs",1567,1575]],"0:2":[["geometry.rs",1567,1575]],"0:3":[["geometry.rs",1567,1575]],"0:4":[["geometry.rs",1567,1575]],"0:5":[["geometry.rs",1567,1575]],"0:6":[["geometry.rs",1567,1575]],"1:0":[["geometry.rs",1567,1575]],"1:1":[["geometry.rs",1567,1575]],"1:2":[["geometry.rs",1630,1638]],"2:0":[["geometry.rs",1548,1556]],"2:1":[["geometry.rs",1548,1556]],"2:10":[["geometry.rs",1548,1556]],"2:11":[["geometry.rs",1548,1556]],"2:12":[["geometry.rs",1615,1623]],"2:13":[["geometry.rs",1615,1623]],"2:14":[["geometry.rs",1615,1623]],"2:15":[["geometry.rs",1615,1623]],"2:16":[["geometry.rs",1615,1623]],"2:17":[["geometry.rs",1615,1623]],"2:18":[["geometry.rs",1615,1623]],"2:19":[["geometry.rs",1615,1623]],"2:2":[["geometry.rs",1548,1556]],"2:20":[["geometry.rs",1615,1623]],"2:21":[["geometry.rs",1615,1623]],"2:22":[["geometry.rs",1615,1623]],"2:23":[["geometry.rs",1615,1623]],"2:24":[["geometry.rs",1615,1623]],"2:25":[["geometry.rs",1615,1623]],"2:26":[["geometry.rs",1615,1623]],"2:27":[["geometry.rs",1615,1623]],"2:28":[["geometry.rs",1615,1623]],"2:29":[["geometry.rs",1615,1623]],"2:3":[["geometry.rs",1548,1556]],"2:30":[["geometry.rs",1615,1623]],"2:31":[["geometry.rs",1615,1623]],"2:32":[["geometry.rs",1615,1623]],"2:33":[["geometry.rs",1615,1623]],"2:34":[["geometry.rs",1615,1623]],"2:35":[["geometry.rs",1615,1623]],"2:36":[["geometry.rs",1615,1623]],"2:37":[["geometry.rs",1615,1623]],"2:38":[["geometry.rs",1615,1623]],"2:39":[["geometry.rs",1615,1623]],"2:4":[["geometry.rs",1548,1556]],"2:40":[["geometry.rs",1615,1623]],"2:41":[["geometry.rs",1615,1623]],"2:42":[["geometry.rs",1615,1623]],"2:43":[["geometry.rs",1615,1623]],"2:44":[["geometry.rs",1615,1623]],"2:45":[["geometry.rs",1615,1623]],"2:46":[["geometry.rs",1615,1623]],"2:5":[["geometry.rs",1548,1556]],"2:6":[["geometry.rs",1548,1556]],"2:7":[["geometry.rs",1548,1556]],"2:8":[["geometry.rs",1548,1556]],"2:9":[["geometry.rs",1548,1556]],"3:0":[["geometry.rs",1615,1623]],"3:1":[["geometry.rs",1615,1623]],"3:2":[["geometry.rs",1615,1623]],"3:3":[["geometry.rs",1615,1623]],"3:4":[["geometry.rs",1615,1623]],"4:0":[["geometry.rs",1580,1588]],"4:1":[["geometry.rs",1580,1588]],"4:2":[["geometry.rs",1580,1588]],"4:3":[["geometry.rs",1580,1588]],"4:4":[["geometry.rs",1580,1588]],"4:5":[["geometry.rs",1580,1588]],"4:6":[["geometry.rs",1580,1588]],"5:0":[["geometry.rs",1615,1623]],"5:1":[["geometry.rs",1615,1623]]}},{"k":"fn:Bounds::left","d":{"0:1":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","0:2":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","0:3":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","0:4":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","0:5":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","0:6":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","1:0":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","1:1":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","1:2":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:0":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:1":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:10":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:11":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:12":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:13":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:14":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:15":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:16":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:17":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:18":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:19":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:2":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:20":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:21":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:22":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:23":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:24":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:25":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:26":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:27":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:28":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:29":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:3":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:30":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:31":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:32":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:33":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:34":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:35":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:36":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:37":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:38":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:39":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:4":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:40":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:41":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:42":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:43":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:44":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:45":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:46":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:5":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:6":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:7":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:8":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","2:9":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","3:0":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","3:1":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","3:2":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","3:3":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","3:4":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","4:0":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","4:1":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","4:2":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","4:3":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","4:4":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","4:5":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","4:6":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","5:0":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds.","5:1":"Returns the left edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the left edge of the bounds."},"s":{"0:1":"fn Bounds::left(& self)-> T","0:2":"fn Bounds::left(& self)-> T","0:3":"fn Bounds::left(& self)-> T","0:4":"fn Bounds::left(& self)-> T","0:5":"fn Bounds::left(& self)-> T","0:6":"fn Bounds::left(& self)-> T","1:0":"fn Bounds::left(& self)-> T","1:1":"fn Bounds::left(& self)-> T","1:2":"fn Bounds::left(& self)-> T","2:0":"fn Bounds::left(& self)-> T","2:1":"fn Bounds::left(& self)-> T","2:10":"fn Bounds::left(& self)-> T","2:11":"fn Bounds::left(& self)-> T","2:12":"fn Bounds::left(& self)-> T","2:13":"fn Bounds::left(& self)-> T","2:14":"fn Bounds::left(& self)-> T","2:15":"fn Bounds::left(& self)-> T","2:16":"fn Bounds::left(& self)-> T","2:17":"fn Bounds::left(& self)-> T","2:18":"fn Bounds::left(& self)-> T","2:19":"fn Bounds::left(& self)-> T","2:2":"fn Bounds::left(& self)-> T","2:20":"fn Bounds::left(& self)-> T","2:21":"fn Bounds::left(& self)-> T","2:22":"fn Bounds::left(& self)-> T","2:23":"fn Bounds::left(& self)-> T","2:24":"fn Bounds::left(& self)-> T","2:25":"fn Bounds::left(& self)-> T","2:26":"fn Bounds::left(& self)-> T","2:27":"fn Bounds::left(& self)-> T","2:28":"fn Bounds::left(& self)-> T","2:29":"fn Bounds::left(& self)-> T","2:3":"fn Bounds::left(& self)-> T","2:30":"fn Bounds::left(& self)-> T","2:31":"fn Bounds::left(& self)-> T","2:32":"fn Bounds::left(& self)-> T","2:33":"fn Bounds::left(& self)-> T","2:34":"fn Bounds::left(& self)-> T","2:35":"fn Bounds::left(& self)-> T","2:36":"fn Bounds::left(& self)-> T","2:37":"fn Bounds::left(& self)-> T","2:38":"fn Bounds::left(& self)-> T","2:39":"fn Bounds::left(& self)-> T","2:4":"fn Bounds::left(& self)-> T","2:40":"fn Bounds::left(& self)-> T","2:41":"fn Bounds::left(& self)-> T","2:42":"fn Bounds::left(& self)-> T","2:43":"fn Bounds::left(& self)-> T","2:44":"fn Bounds::left(& self)-> T","2:45":"fn Bounds::left(& self)-> T","2:46":"fn Bounds::left(& self)-> T","2:5":"fn Bounds::left(& self)-> T","2:6":"fn Bounds::left(& self)-> T","2:7":"fn Bounds::left(& self)-> T","2:8":"fn Bounds::left(& self)-> T","2:9":"fn Bounds::left(& self)-> T","3:0":"fn Bounds::left(& self)-> T","3:1":"fn Bounds::left(& self)-> T","3:2":"fn Bounds::left(& self)-> T","3:3":"fn Bounds::left(& self)-> T","3:4":"fn Bounds::left(& self)-> T","4:0":"fn Bounds::left(& self)-> T","4:1":"fn Bounds::left(& self)-> T","4:2":"fn Bounds::left(& self)-> T","4:3":"fn Bounds::left(& self)-> T","4:4":"fn Bounds::left(& self)-> T","4:5":"fn Bounds::left(& self)-> T","4:6":"fn Bounds::left(& self)-> T","5:0":"fn Bounds::left(& self)-> T","5:1":"fn Bounds::left(& self)-> T"},"l":{"0:1":[["geometry.rs",1267,1274]],"0:2":[["geometry.rs",1267,1274]],"0:3":[["geometry.rs",1267,1274]],"0:4":[["geometry.rs",1267,1274]],"0:5":[["geometry.rs",1267,1274]],"0:6":[["geometry.rs",1267,1274]],"1:0":[["geometry.rs",1267,1274]],"1:1":[["geometry.rs",1267,1274]],"1:2":[["geometry.rs",1321,1328]],"2:0":[["geometry.rs",1248,1255]],"2:1":[["geometry.rs",1248,1255]],"2:10":[["geometry.rs",1248,1255]],"2:11":[["geometry.rs",1248,1255]],"2:12":[["geometry.rs",1306,1313]],"2:13":[["geometry.rs",1306,1313]],"2:14":[["geometry.rs",1306,1313]],"2:15":[["geometry.rs",1306,1313]],"2:16":[["geometry.rs",1306,1313]],"2:17":[["geometry.rs",1306,1313]],"2:18":[["geometry.rs",1306,1313]],"2:19":[["geometry.rs",1306,1313]],"2:2":[["geometry.rs",1248,1255]],"2:20":[["geometry.rs",1306,1313]],"2:21":[["geometry.rs",1306,1313]],"2:22":[["geometry.rs",1306,1313]],"2:23":[["geometry.rs",1306,1313]],"2:24":[["geometry.rs",1306,1313]],"2:25":[["geometry.rs",1306,1313]],"2:26":[["geometry.rs",1306,1313]],"2:27":[["geometry.rs",1306,1313]],"2:28":[["geometry.rs",1306,1313]],"2:29":[["geometry.rs",1306,1313]],"2:3":[["geometry.rs",1248,1255]],"2:30":[["geometry.rs",1306,1313]],"2:31":[["geometry.rs",1306,1313]],"2:32":[["geometry.rs",1306,1313]],"2:33":[["geometry.rs",1306,1313]],"2:34":[["geometry.rs",1306,1313]],"2:35":[["geometry.rs",1306,1313]],"2:36":[["geometry.rs",1306,1313]],"2:37":[["geometry.rs",1306,1313]],"2:38":[["geometry.rs",1306,1313]],"2:39":[["geometry.rs",1306,1313]],"2:4":[["geometry.rs",1248,1255]],"2:40":[["geometry.rs",1306,1313]],"2:41":[["geometry.rs",1306,1313]],"2:42":[["geometry.rs",1306,1313]],"2:43":[["geometry.rs",1306,1313]],"2:44":[["geometry.rs",1306,1313]],"2:45":[["geometry.rs",1306,1313]],"2:46":[["geometry.rs",1306,1313]],"2:5":[["geometry.rs",1248,1255]],"2:6":[["geometry.rs",1248,1255]],"2:7":[["geometry.rs",1248,1255]],"2:8":[["geometry.rs",1248,1255]],"2:9":[["geometry.rs",1248,1255]],"3:0":[["geometry.rs",1306,1313]],"3:1":[["geometry.rs",1306,1313]],"3:2":[["geometry.rs",1306,1313]],"3:3":[["geometry.rs",1306,1313]],"3:4":[["geometry.rs",1306,1313]],"4:0":[["geometry.rs",1280,1287]],"4:1":[["geometry.rs",1280,1287]],"4:2":[["geometry.rs",1280,1287]],"4:3":[["geometry.rs",1280,1287]],"4:4":[["geometry.rs",1280,1287]],"4:5":[["geometry.rs",1280,1287]],"4:6":[["geometry.rs",1280,1287]],"5:0":[["geometry.rs",1306,1313]],"5:1":[["geometry.rs",1306,1313]]}},{"k":"fn:Bounds::left_center","d":{"1:2":"Returns the left center point of the bounds.","2:12":"Returns the left center point of the bounds.","2:13":"Returns the left center point of the bounds.","2:14":"Returns the left center point of the bounds.","2:15":"Returns the left center point of the bounds.","2:16":"Returns the left center point of the bounds.","2:17":"Returns the left center point of the bounds.","2:18":"Returns the left center point of the bounds.","2:19":"Returns the left center point of the bounds.","2:20":"Returns the left center point of the bounds.","2:21":"Returns the left center point of the bounds.","2:22":"Returns the left center point of the bounds.","2:23":"Returns the left center point of the bounds.","2:24":"Returns the left center point of the bounds.","2:25":"Returns the left center point of the bounds.","2:26":"Returns the left center point of the bounds.","2:27":"Returns the left center point of the bounds.","2:28":"Returns the left center point of the bounds.","2:29":"Returns the left center point of the bounds.","2:30":"Returns the left center point of the bounds.","2:31":"Returns the left center point of the bounds.","2:32":"Returns the left center point of the bounds.","2:33":"Returns the left center point of the bounds.","2:34":"Returns the left center point of the bounds.","2:35":"Returns the left center point of the bounds.","2:36":"Returns the left center point of the bounds.","2:37":"Returns the left center point of the bounds.","2:38":"Returns the left center point of the bounds.","2:39":"Returns the left center point of the bounds.","2:40":"Returns the left center point of the bounds.","2:41":"Returns the left center point of the bounds.","2:42":"Returns the left center point of the bounds.","2:43":"Returns the left center point of the bounds.","2:44":"Returns the left center point of the bounds.","2:45":"Returns the left center point of the bounds.","2:46":"Returns the left center point of the bounds.","3:0":"Returns the left center point of the bounds.","3:1":"Returns the left center point of the bounds.","3:2":"Returns the left center point of the bounds.","3:3":"Returns the left center point of the bounds.","3:4":"Returns the left center point of the bounds.","5:0":"Returns the left center point of the bounds.","5:1":"Returns the left center point of the bounds."},"s":{"1:2":"fn Bounds::left_center(& self)-> Point < T >","2:12":"fn Bounds::left_center(& self)-> Point < T >","2:13":"fn Bounds::left_center(& self)-> Point < T >","2:14":"fn Bounds::left_center(& self)-> Point < T >","2:15":"fn Bounds::left_center(& self)-> Point < T >","2:16":"fn Bounds::left_center(& self)-> Point < T >","2:17":"fn Bounds::left_center(& self)-> Point < T >","2:18":"fn Bounds::left_center(& self)-> Point < T >","2:19":"fn Bounds::left_center(& self)-> Point < T >","2:20":"fn Bounds::left_center(& self)-> Point < T >","2:21":"fn Bounds::left_center(& self)-> Point < T >","2:22":"fn Bounds::left_center(& self)-> Point < T >","2:23":"fn Bounds::left_center(& self)-> Point < T >","2:24":"fn Bounds::left_center(& self)-> Point < T >","2:25":"fn Bounds::left_center(& self)-> Point < T >","2:26":"fn Bounds::left_center(& self)-> Point < T >","2:27":"fn Bounds::left_center(& self)-> Point < T >","2:28":"fn Bounds::left_center(& self)-> Point < T >","2:29":"fn Bounds::left_center(& self)-> Point < T >","2:30":"fn Bounds::left_center(& self)-> Point < T >","2:31":"fn Bounds::left_center(& self)-> Point < T >","2:32":"fn Bounds::left_center(& self)-> Point < T >","2:33":"fn Bounds::left_center(& self)-> Point < T >","2:34":"fn Bounds::left_center(& self)-> Point < T >","2:35":"fn Bounds::left_center(& self)-> Point < T >","2:36":"fn Bounds::left_center(& self)-> Point < T >","2:37":"fn Bounds::left_center(& self)-> Point < T >","2:38":"fn Bounds::left_center(& self)-> Point < T >","2:39":"fn Bounds::left_center(& self)-> Point < T >","2:40":"fn Bounds::left_center(& self)-> Point < T >","2:41":"fn Bounds::left_center(& self)-> Point < T >","2:42":"fn Bounds::left_center(& self)-> Point < T >","2:43":"fn Bounds::left_center(& self)-> Point < T >","2:44":"fn Bounds::left_center(& self)-> Point < T >","2:45":"fn Bounds::left_center(& self)-> Point < T >","2:46":"fn Bounds::left_center(& self)-> Point < T >","3:0":"fn Bounds::left_center(& self)-> Point < T >","3:1":"fn Bounds::left_center(& self)-> Point < T >","3:2":"fn Bounds::left_center(& self)-> Point < T >","3:3":"fn Bounds::left_center(& self)-> Point < T >","3:4":"fn Bounds::left_center(& self)-> Point < T >","5:0":"fn Bounds::left_center(& self)-> Point < T >","5:1":"fn Bounds::left_center(& self)-> Point < T >"},"l":{"1:2":[["geometry.rs",923,929]],"2:12":[["geometry.rs",908,914]],"2:13":[["geometry.rs",908,914]],"2:14":[["geometry.rs",908,914]],"2:15":[["geometry.rs",908,914]],"2:16":[["geometry.rs",908,914]],"2:17":[["geometry.rs",908,914]],"2:18":[["geometry.rs",908,914]],"2:19":[["geometry.rs",908,914]],"2:20":[["geometry.rs",908,914]],"2:21":[["geometry.rs",908,914]],"2:22":[["geometry.rs",908,914]],"2:23":[["geometry.rs",908,914]],"2:24":[["geometry.rs",908,914]],"2:25":[["geometry.rs",908,914]],"2:26":[["geometry.rs",908,914]],"2:27":[["geometry.rs",908,914]],"2:28":[["geometry.rs",908,914]],"2:29":[["geometry.rs",908,914]],"2:30":[["geometry.rs",908,914]],"2:31":[["geometry.rs",908,914]],"2:32":[["geometry.rs",908,914]],"2:33":[["geometry.rs",908,914]],"2:34":[["geometry.rs",908,914]],"2:35":[["geometry.rs",908,914]],"2:36":[["geometry.rs",908,914]],"2:37":[["geometry.rs",908,914]],"2:38":[["geometry.rs",908,914]],"2:39":[["geometry.rs",908,914]],"2:40":[["geometry.rs",908,914]],"2:41":[["geometry.rs",908,914]],"2:42":[["geometry.rs",908,914]],"2:43":[["geometry.rs",908,914]],"2:44":[["geometry.rs",908,914]],"2:45":[["geometry.rs",908,914]],"2:46":[["geometry.rs",908,914]],"3:0":[["geometry.rs",908,914]],"3:1":[["geometry.rs",908,914]],"3:2":[["geometry.rs",908,914]],"3:3":[["geometry.rs",908,914]],"3:4":[["geometry.rs",908,914]],"5:0":[["geometry.rs",908,914]],"5:1":[["geometry.rs",908,914]]}},{"k":"fn:Bounds::localize","d":{"0:1":"Convert a point to the coordinate space defined by this Bounds","0:2":"Convert a point to the coordinate space defined by this Bounds","0:3":"Convert a point to the coordinate space defined by this Bounds","0:4":"Convert a point to the coordinate space defined by this Bounds","0:5":"Convert a point to the coordinate space defined by this Bounds","0:6":"Convert a point to the coordinate space defined by this Bounds","1:0":"Convert a point to the coordinate space defined by this Bounds","1:1":"Convert a point to the coordinate space defined by this Bounds","1:2":"Convert a point to the coordinate space defined by this Bounds","2:0":"Convert a point to the coordinate space defined by this Bounds","2:1":"Convert a point to the coordinate space defined by this Bounds","2:10":"Convert a point to the coordinate space defined by this Bounds","2:11":"Convert a point to the coordinate space defined by this Bounds","2:12":"Convert a point to the coordinate space defined by this Bounds","2:13":"Convert a point to the coordinate space defined by this Bounds","2:14":"Convert a point to the coordinate space defined by this Bounds","2:15":"Convert a point to the coordinate space defined by this Bounds","2:16":"Convert a point to the coordinate space defined by this Bounds","2:17":"Convert a point to the coordinate space defined by this Bounds","2:18":"Convert a point to the coordinate space defined by this Bounds","2:19":"Convert a point to the coordinate space defined by this Bounds","2:2":"Convert a point to the coordinate space defined by this Bounds","2:20":"Convert a point to the coordinate space defined by this Bounds","2:21":"Convert a point to the coordinate space defined by this Bounds","2:22":"Convert a point to the coordinate space defined by this Bounds","2:23":"Convert a point to the coordinate space defined by this Bounds","2:24":"Convert a point to the coordinate space defined by this Bounds","2:25":"Convert a point to the coordinate space defined by this Bounds","2:26":"Convert a point to the coordinate space defined by this Bounds","2:27":"Convert a point to the coordinate space defined by this Bounds","2:28":"Convert a point to the coordinate space defined by this Bounds","2:29":"Convert a point to the coordinate space defined by this Bounds","2:3":"Convert a point to the coordinate space defined by this Bounds","2:30":"Convert a point to the coordinate space defined by this Bounds","2:31":"Convert a point to the coordinate space defined by this Bounds","2:32":"Convert a point to the coordinate space defined by this Bounds","2:33":"Convert a point to the coordinate space defined by this Bounds","2:34":"Convert a point to the coordinate space defined by this Bounds","2:35":"Convert a point to the coordinate space defined by this Bounds","2:36":"Convert a point to the coordinate space defined by this Bounds","2:37":"Convert a point to the coordinate space defined by this Bounds","2:38":"Convert a point to the coordinate space defined by this Bounds","2:39":"Convert a point to the coordinate space defined by this Bounds","2:4":"Convert a point to the coordinate space defined by this Bounds","2:40":"Convert a point to the coordinate space defined by this Bounds","2:41":"Convert a point to the coordinate space defined by this Bounds","2:42":"Convert a point to the coordinate space defined by this Bounds","2:43":"Convert a point to the coordinate space defined by this Bounds","2:44":"Convert a point to the coordinate space defined by this Bounds","2:45":"Convert a point to the coordinate space defined by this Bounds","2:46":"Convert a point to the coordinate space defined by this Bounds","2:5":"Convert a point to the coordinate space defined by this Bounds","2:6":"Convert a point to the coordinate space defined by this Bounds","2:7":"Convert a point to the coordinate space defined by this Bounds","2:8":"Convert a point to the coordinate space defined by this Bounds","2:9":"Convert a point to the coordinate space defined by this Bounds","3:0":"Convert a point to the coordinate space defined by this Bounds","3:1":"Convert a point to the coordinate space defined by this Bounds","3:2":"Convert a point to the coordinate space defined by this Bounds","3:3":"Convert a point to the coordinate space defined by this Bounds","3:4":"Convert a point to the coordinate space defined by this Bounds","4:0":"Convert a point to the coordinate space defined by this Bounds","4:1":"Convert a point to the coordinate space defined by this Bounds","4:2":"Convert a point to the coordinate space defined by this Bounds","4:3":"Convert a point to the coordinate space defined by this Bounds","4:4":"Convert a point to the coordinate space defined by this Bounds","4:5":"Convert a point to the coordinate space defined by this Bounds","4:6":"Convert a point to the coordinate space defined by this Bounds","5:0":"Convert a point to the coordinate space defined by this Bounds","5:1":"Convert a point to the coordinate space defined by this Bounds"},"s":{"0:1":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","0:2":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","0:3":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","0:4":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","0:5":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","0:6":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","1:0":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","1:1":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","1:2":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:0":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:1":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:10":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:11":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:12":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:13":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:14":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:15":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:16":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:17":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:18":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:19":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:2":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:20":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:21":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:22":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:23":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:24":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:25":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:26":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:27":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:28":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:29":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:3":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:30":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:31":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:32":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:33":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:34":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:35":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:36":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:37":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:38":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:39":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:4":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:40":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:41":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:42":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:43":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:44":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:45":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:46":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:5":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:6":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:7":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:8":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","2:9":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","3:0":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","3:1":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","3:2":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","3:3":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","3:4":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","4:0":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","4:1":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","4:2":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","4:3":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","4:4":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","4:5":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","4:6":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","5:0":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >","5:1":"fn Bounds::localize(& self, & Point < T >)-> Option < Point < T > >"},"l":{"0:1":[["geometry.rs",1554,1558]],"0:2":[["geometry.rs",1554,1558]],"0:3":[["geometry.rs",1554,1558]],"0:4":[["geometry.rs",1554,1558]],"0:5":[["geometry.rs",1554,1558]],"0:6":[["geometry.rs",1554,1558]],"1:0":[["geometry.rs",1554,1558]],"1:1":[["geometry.rs",1554,1558]],"1:2":[["geometry.rs",1617,1621]],"2:0":[["geometry.rs",1535,1539]],"2:1":[["geometry.rs",1535,1539]],"2:10":[["geometry.rs",1535,1539]],"2:11":[["geometry.rs",1535,1539]],"2:12":[["geometry.rs",1602,1606]],"2:13":[["geometry.rs",1602,1606]],"2:14":[["geometry.rs",1602,1606]],"2:15":[["geometry.rs",1602,1606]],"2:16":[["geometry.rs",1602,1606]],"2:17":[["geometry.rs",1602,1606]],"2:18":[["geometry.rs",1602,1606]],"2:19":[["geometry.rs",1602,1606]],"2:2":[["geometry.rs",1535,1539]],"2:20":[["geometry.rs",1602,1606]],"2:21":[["geometry.rs",1602,1606]],"2:22":[["geometry.rs",1602,1606]],"2:23":[["geometry.rs",1602,1606]],"2:24":[["geometry.rs",1602,1606]],"2:25":[["geometry.rs",1602,1606]],"2:26":[["geometry.rs",1602,1606]],"2:27":[["geometry.rs",1602,1606]],"2:28":[["geometry.rs",1602,1606]],"2:29":[["geometry.rs",1602,1606]],"2:3":[["geometry.rs",1535,1539]],"2:30":[["geometry.rs",1602,1606]],"2:31":[["geometry.rs",1602,1606]],"2:32":[["geometry.rs",1602,1606]],"2:33":[["geometry.rs",1602,1606]],"2:34":[["geometry.rs",1602,1606]],"2:35":[["geometry.rs",1602,1606]],"2:36":[["geometry.rs",1602,1606]],"2:37":[["geometry.rs",1602,1606]],"2:38":[["geometry.rs",1602,1606]],"2:39":[["geometry.rs",1602,1606]],"2:4":[["geometry.rs",1535,1539]],"2:40":[["geometry.rs",1602,1606]],"2:41":[["geometry.rs",1602,1606]],"2:42":[["geometry.rs",1602,1606]],"2:43":[["geometry.rs",1602,1606]],"2:44":[["geometry.rs",1602,1606]],"2:45":[["geometry.rs",1602,1606]],"2:46":[["geometry.rs",1602,1606]],"2:5":[["geometry.rs",1535,1539]],"2:6":[["geometry.rs",1535,1539]],"2:7":[["geometry.rs",1535,1539]],"2:8":[["geometry.rs",1535,1539]],"2:9":[["geometry.rs",1535,1539]],"3:0":[["geometry.rs",1602,1606]],"3:1":[["geometry.rs",1602,1606]],"3:2":[["geometry.rs",1602,1606]],"3:3":[["geometry.rs",1602,1606]],"3:4":[["geometry.rs",1602,1606]],"4:0":[["geometry.rs",1567,1571]],"4:1":[["geometry.rs",1567,1571]],"4:2":[["geometry.rs",1567,1571]],"4:3":[["geometry.rs",1567,1571]],"4:4":[["geometry.rs",1567,1571]],"4:5":[["geometry.rs",1567,1571]],"4:6":[["geometry.rs",1567,1571]],"5:0":[["geometry.rs",1602,1606]],"5:1":[["geometry.rs",1602,1606]]}},{"k":"fn:Bounds::map","d":{"0:1":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","0:2":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","0:3":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","0:4":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","0:5":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","0:6":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","1:0":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","1:1":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","1:2":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:0":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:1":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:10":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:11":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:12":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:13":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:14":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:15":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:16":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:17":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:18":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:19":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:2":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:20":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:21":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:22":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:23":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:24":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:25":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:26":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:27":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:28":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:29":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:3":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:30":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:31":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:32":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:33":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:34":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:35":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:36":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:37":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:38":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:39":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:4":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:40":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:41":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:42":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:43":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:44":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:45":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:46":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:5":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:6":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:7":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:8":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:9":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","3:0":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","3:1":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","3:2":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","3:3":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","3:4":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","4:0":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","4:1":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","4:2":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","4:3":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","4:4":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","4:5":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","4:6":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","5:0":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","5:1":"Applies a function to the origin and size of the bounds, producing a new `Bounds<U>`.\n\nThis method allows for converting a `Bounds<T>` to a `Bounds<U>` by specifying a closure\nthat defines how to convert between the two types. The closure is applied to the `origin` and\n`size` fields, resulting in new bounds of the desired type.\n\n# Arguments\n\n* `f` - A closure that takes a value of type `T` and returns a value of type `U`.\n\n# Returns\n\nReturns a new `Bounds<U>` with the origin and size mapped by the provided function.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map(|value| value as f64 * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```"},"s":{"0:1":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","0:2":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","0:3":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","0:4":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","0:5":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","0:6":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","1:0":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","1:1":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","1:2":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:0":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:1":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:10":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:11":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:12":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:13":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:14":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:15":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:16":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:17":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:18":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:19":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:2":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:20":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:21":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:22":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:23":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:24":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:25":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:26":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:27":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:28":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:29":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:3":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:30":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:31":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:32":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:33":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:34":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:35":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:36":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:37":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:38":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:39":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:4":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:40":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:41":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:42":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:43":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:44":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:45":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:46":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:5":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:6":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:7":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:8":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","2:9":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","3:0":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","3:1":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","3:2":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","3:3":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","3:4":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","4:0":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","4:1":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","4:2":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","4:3":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","4:4":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","4:5":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","4:6":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","5:0":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,","5:1":"fn Bounds::map<U>(& self, impl Fn (T) -> U)-> Bounds < U >where U : Clone + Debug + Default + PartialEq ,"},"l":{"0:1":[["geometry.rs",1462,1499]],"0:2":[["geometry.rs",1462,1499]],"0:3":[["geometry.rs",1462,1499]],"0:4":[["geometry.rs",1462,1499]],"0:5":[["geometry.rs",1462,1499]],"0:6":[["geometry.rs",1462,1499]],"1:0":[["geometry.rs",1462,1499]],"1:1":[["geometry.rs",1462,1499]],"1:2":[["geometry.rs",1525,1562]],"2:0":[["geometry.rs",1443,1480]],"2:1":[["geometry.rs",1443,1480]],"2:10":[["geometry.rs",1443,1480]],"2:11":[["geometry.rs",1443,1480]],"2:12":[["geometry.rs",1510,1547]],"2:13":[["geometry.rs",1510,1547]],"2:14":[["geometry.rs",1510,1547]],"2:15":[["geometry.rs",1510,1547]],"2:16":[["geometry.rs",1510,1547]],"2:17":[["geometry.rs",1510,1547]],"2:18":[["geometry.rs",1510,1547]],"2:19":[["geometry.rs",1510,1547]],"2:2":[["geometry.rs",1443,1480]],"2:20":[["geometry.rs",1510,1547]],"2:21":[["geometry.rs",1510,1547]],"2:22":[["geometry.rs",1510,1547]],"2:23":[["geometry.rs",1510,1547]],"2:24":[["geometry.rs",1510,1547]],"2:25":[["geometry.rs",1510,1547]],"2:26":[["geometry.rs",1510,1547]],"2:27":[["geometry.rs",1510,1547]],"2:28":[["geometry.rs",1510,1547]],"2:29":[["geometry.rs",1510,1547]],"2:3":[["geometry.rs",1443,1480]],"2:30":[["geometry.rs",1510,1547]],"2:31":[["geometry.rs",1510,1547]],"2:32":[["geometry.rs",1510,1547]],"2:33":[["geometry.rs",1510,1547]],"2:34":[["geometry.rs",1510,1547]],"2:35":[["geometry.rs",1510,1547]],"2:36":[["geometry.rs",1510,1547]],"2:37":[["geometry.rs",1510,1547]],"2:38":[["geometry.rs",1510,1547]],"2:39":[["geometry.rs",1510,1547]],"2:4":[["geometry.rs",1443,1480]],"2:40":[["geometry.rs",1510,1547]],"2:41":[["geometry.rs",1510,1547]],"2:42":[["geometry.rs",1510,1547]],"2:43":[["geometry.rs",1510,1547]],"2:44":[["geometry.rs",1510,1547]],"2:45":[["geometry.rs",1510,1547]],"2:46":[["geometry.rs",1510,1547]],"2:5":[["geometry.rs",1443,1480]],"2:6":[["geometry.rs",1443,1480]],"2:7":[["geometry.rs",1443,1480]],"2:8":[["geometry.rs",1443,1480]],"2:9":[["geometry.rs",1443,1480]],"3:0":[["geometry.rs",1510,1547]],"3:1":[["geometry.rs",1510,1547]],"3:2":[["geometry.rs",1510,1547]],"3:3":[["geometry.rs",1510,1547]],"3:4":[["geometry.rs",1510,1547]],"4:0":[["geometry.rs",1475,1512]],"4:1":[["geometry.rs",1475,1512]],"4:2":[["geometry.rs",1475,1512]],"4:3":[["geometry.rs",1475,1512]],"4:4":[["geometry.rs",1475,1512]],"4:5":[["geometry.rs",1475,1512]],"4:6":[["geometry.rs",1475,1512]],"5:0":[["geometry.rs",1510,1547]],"5:1":[["geometry.rs",1510,1547]]}},{"k":"fn:Bounds::map_origin","d":{"0:1":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","0:2":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","0:3":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","0:4":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","0:5":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","0:6":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","1:0":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","1:1":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","1:2":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:0":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:1":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:10":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:11":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:12":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:13":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:14":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:15":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:16":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:17":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:18":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:19":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:2":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:20":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:21":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:22":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:23":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:24":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:25":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:26":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:27":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:28":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:29":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:3":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:30":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:31":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:32":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:33":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:34":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:35":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:36":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:37":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:38":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:39":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:4":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:40":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:41":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:42":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:43":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:44":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:45":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:46":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:5":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:6":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:7":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:8":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","2:9":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","3:0":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","3:1":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","3:2":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","3:3":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","3:4":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","4:0":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","4:1":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","4:2":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","4:3":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","4:4":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","4:5":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","4:6":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","5:0":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```","5:1":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_origin(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 15.0, y: 15.0 },\n    size: Size { width: 10.0, height: 20.0 },\n});\n```"},"s":{"0:1":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","0:2":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","0:3":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","0:4":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","0:5":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","0:6":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","1:0":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","1:1":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","1:2":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:0":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:1":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:10":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:11":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:12":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:13":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:14":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:15":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:16":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:17":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:18":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:19":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:2":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:20":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:21":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:22":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:23":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:24":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:25":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:26":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:27":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:28":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:29":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:3":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:30":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:31":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:32":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:33":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:34":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:35":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:36":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:37":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:38":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:39":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:4":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:40":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:41":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:42":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:43":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:44":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:45":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:46":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:5":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:6":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:7":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:8":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","2:9":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","3:0":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","3:1":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","3:2":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","3:3":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","3:4":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","4:0":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","4:1":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","4:2":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","4:3":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","4:4":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","4:5":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","4:6":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","5:0":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >","5:1":"fn Bounds::map_origin(self, impl Fn (T) -> T)-> Bounds < T >"},"l":{"0:1":[["geometry.rs",1501,1523]],"0:2":[["geometry.rs",1501,1523]],"0:3":[["geometry.rs",1501,1523]],"0:4":[["geometry.rs",1501,1523]],"0:5":[["geometry.rs",1501,1523]],"0:6":[["geometry.rs",1501,1523]],"1:0":[["geometry.rs",1501,1523]],"1:1":[["geometry.rs",1501,1523]],"1:2":[["geometry.rs",1564,1586]],"2:0":[["geometry.rs",1482,1504]],"2:1":[["geometry.rs",1482,1504]],"2:10":[["geometry.rs",1482,1504]],"2:11":[["geometry.rs",1482,1504]],"2:12":[["geometry.rs",1549,1571]],"2:13":[["geometry.rs",1549,1571]],"2:14":[["geometry.rs",1549,1571]],"2:15":[["geometry.rs",1549,1571]],"2:16":[["geometry.rs",1549,1571]],"2:17":[["geometry.rs",1549,1571]],"2:18":[["geometry.rs",1549,1571]],"2:19":[["geometry.rs",1549,1571]],"2:2":[["geometry.rs",1482,1504]],"2:20":[["geometry.rs",1549,1571]],"2:21":[["geometry.rs",1549,1571]],"2:22":[["geometry.rs",1549,1571]],"2:23":[["geometry.rs",1549,1571]],"2:24":[["geometry.rs",1549,1571]],"2:25":[["geometry.rs",1549,1571]],"2:26":[["geometry.rs",1549,1571]],"2:27":[["geometry.rs",1549,1571]],"2:28":[["geometry.rs",1549,1571]],"2:29":[["geometry.rs",1549,1571]],"2:3":[["geometry.rs",1482,1504]],"2:30":[["geometry.rs",1549,1571]],"2:31":[["geometry.rs",1549,1571]],"2:32":[["geometry.rs",1549,1571]],"2:33":[["geometry.rs",1549,1571]],"2:34":[["geometry.rs",1549,1571]],"2:35":[["geometry.rs",1549,1571]],"2:36":[["geometry.rs",1549,1571]],"2:37":[["geometry.rs",1549,1571]],"2:38":[["geometry.rs",1549,1571]],"2:39":[["geometry.rs",1549,1571]],"2:4":[["geometry.rs",1482,1504]],"2:40":[["geometry.rs",1549,1571]],"2:41":[["geometry.rs",1549,1571]],"2:42":[["geometry.rs",1549,1571]],"2:43":[["geometry.rs",1549,1571]],"2:44":[["geometry.rs",1549,1571]],"2:45":[["geometry.rs",1549,1571]],"2:46":[["geometry.rs",1549,1571]],"2:5":[["geometry.rs",1482,1504]],"2:6":[["geometry.rs",1482,1504]],"2:7":[["geometry.rs",1482,1504]],"2:8":[["geometry.rs",1482,1504]],"2:9":[["geometry.rs",1482,1504]],"3:0":[["geometry.rs",1549,1571]],"3:1":[["geometry.rs",1549,1571]],"3:2":[["geometry.rs",1549,1571]],"3:3":[["geometry.rs",1549,1571]],"3:4":[["geometry.rs",1549,1571]],"4:0":[["geometry.rs",1514,1536]],"4:1":[["geometry.rs",1514,1536]],"4:2":[["geometry.rs",1514,1536]],"4:3":[["geometry.rs",1514,1536]],"4:4":[["geometry.rs",1514,1536]],"4:5":[["geometry.rs",1514,1536]],"4:6":[["geometry.rs",1514,1536]],"5:0":[["geometry.rs",1549,1571]],"5:1":[["geometry.rs",1549,1571]]}},{"k":"fn:Bounds::map_size","d":{"0:1":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","0:2":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","0:3":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","0:4":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","0:5":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","0:6":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","1:0":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","1:1":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","1:2":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:0":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:1":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:10":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:11":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:12":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:13":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:14":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:15":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:16":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:17":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:18":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:19":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:2":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:20":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:21":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:22":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:23":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:24":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:25":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:26":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:27":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:28":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:29":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:3":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:30":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:31":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:32":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:33":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:34":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:35":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:36":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:37":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:38":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:39":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:4":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:40":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:41":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:42":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:43":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:44":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:45":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:46":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:5":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:6":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:7":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:8":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","2:9":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","3:0":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","3:1":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","3:2":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","3:3":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","3:4":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","4:0":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","4:1":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","4:2":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","4:3":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","4:4":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","4:5":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","4:6":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","5:0":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```","5:1":"Applies a function to the origin  of the bounds, producing a new `Bounds` with the new origin\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 10.0, height: 20.0 },\n};\nlet new_bounds = bounds.map_size(|value| value * 1.5);\n\nassert_eq!(new_bounds, Bounds {\n    origin: Point { x: 10.0, y: 10.0 },\n    size: Size { width: 15.0, height: 30.0 },\n});\n```"},"s":{"0:1":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","0:2":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","0:3":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","0:4":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","0:5":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","0:6":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","1:0":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","1:1":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","1:2":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:0":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:1":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:10":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:11":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:12":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:13":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:14":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:15":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:16":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:17":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:18":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:19":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:2":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:20":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:21":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:22":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:23":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:24":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:25":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:26":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:27":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:28":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:29":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:3":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:30":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:31":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:32":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:33":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:34":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:35":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:36":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:37":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:38":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:39":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:4":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:40":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:41":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:42":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:43":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:44":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:45":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:46":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:5":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:6":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:7":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:8":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","2:9":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","3:0":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","3:1":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","3:2":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","3:3":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","3:4":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","4:0":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","4:1":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","4:2":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","4:3":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","4:4":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","4:5":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","4:6":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","5:0":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >","5:1":"fn Bounds::map_size(self, impl Fn (T) -> T)-> Bounds < T >"},"l":{"0:1":[["geometry.rs",1525,1547]],"0:2":[["geometry.rs",1525,1547]],"0:3":[["geometry.rs",1525,1547]],"0:4":[["geometry.rs",1525,1547]],"0:5":[["geometry.rs",1525,1547]],"0:6":[["geometry.rs",1525,1547]],"1:0":[["geometry.rs",1525,1547]],"1:1":[["geometry.rs",1525,1547]],"1:2":[["geometry.rs",1588,1610]],"2:0":[["geometry.rs",1506,1528]],"2:1":[["geometry.rs",1506,1528]],"2:10":[["geometry.rs",1506,1528]],"2:11":[["geometry.rs",1506,1528]],"2:12":[["geometry.rs",1573,1595]],"2:13":[["geometry.rs",1573,1595]],"2:14":[["geometry.rs",1573,1595]],"2:15":[["geometry.rs",1573,1595]],"2:16":[["geometry.rs",1573,1595]],"2:17":[["geometry.rs",1573,1595]],"2:18":[["geometry.rs",1573,1595]],"2:19":[["geometry.rs",1573,1595]],"2:2":[["geometry.rs",1506,1528]],"2:20":[["geometry.rs",1573,1595]],"2:21":[["geometry.rs",1573,1595]],"2:22":[["geometry.rs",1573,1595]],"2:23":[["geometry.rs",1573,1595]],"2:24":[["geometry.rs",1573,1595]],"2:25":[["geometry.rs",1573,1595]],"2:26":[["geometry.rs",1573,1595]],"2:27":[["geometry.rs",1573,1595]],"2:28":[["geometry.rs",1573,1595]],"2:29":[["geometry.rs",1573,1595]],"2:3":[["geometry.rs",1506,1528]],"2:30":[["geometry.rs",1573,1595]],"2:31":[["geometry.rs",1573,1595]],"2:32":[["geometry.rs",1573,1595]],"2:33":[["geometry.rs",1573,1595]],"2:34":[["geometry.rs",1573,1595]],"2:35":[["geometry.rs",1573,1595]],"2:36":[["geometry.rs",1573,1595]],"2:37":[["geometry.rs",1573,1595]],"2:38":[["geometry.rs",1573,1595]],"2:39":[["geometry.rs",1573,1595]],"2:4":[["geometry.rs",1506,1528]],"2:40":[["geometry.rs",1573,1595]],"2:41":[["geometry.rs",1573,1595]],"2:42":[["geometry.rs",1573,1595]],"2:43":[["geometry.rs",1573,1595]],"2:44":[["geometry.rs",1573,1595]],"2:45":[["geometry.rs",1573,1595]],"2:46":[["geometry.rs",1573,1595]],"2:5":[["geometry.rs",1506,1528]],"2:6":[["geometry.rs",1506,1528]],"2:7":[["geometry.rs",1506,1528]],"2:8":[["geometry.rs",1506,1528]],"2:9":[["geometry.rs",1506,1528]],"3:0":[["geometry.rs",1573,1595]],"3:1":[["geometry.rs",1573,1595]],"3:2":[["geometry.rs",1573,1595]],"3:3":[["geometry.rs",1573,1595]],"3:4":[["geometry.rs",1573,1595]],"4:0":[["geometry.rs",1538,1560]],"4:1":[["geometry.rs",1538,1560]],"4:2":[["geometry.rs",1538,1560]],"4:3":[["geometry.rs",1538,1560]],"4:4":[["geometry.rs",1538,1560]],"4:5":[["geometry.rs",1538,1560]],"4:6":[["geometry.rs",1538,1560]],"5:0":[["geometry.rs",1573,1595]],"5:1":[["geometry.rs",1573,1595]]}},{"k":"fn:Bounds::maximized","d":{"0:1":"Generate maximized bounds for the given display or primary display if none is provided","0:2":"Generate maximized bounds for the given display or primary display if none is provided","0:3":"Generate maximized bounds for the given display or primary display if none is provided","0:4":"Generate maximized bounds for the given display or primary display if none is provided","0:5":"Generate maximized bounds for the given display or primary display if none is provided","0:6":"Generate maximized bounds for the given display or primary display if none is provided","1:0":"Generate maximized bounds for the given display or primary display if none is provided","1:1":"Generate maximized bounds for the given display or primary display if none is provided","1:2":"Generate maximized bounds for the given display or primary display if none is provided","2:0":"Generate maximized bounds for the given display or primary display if none is provided","2:1":"Generate maximized bounds for the given display or primary display if none is provided","2:10":"Generate maximized bounds for the given display or primary display if none is provided","2:11":"Generate maximized bounds for the given display or primary display if none is provided","2:12":"Generate maximized bounds for the given display or primary display if none is provided","2:13":"Generate maximized bounds for the given display or primary display if none is provided","2:14":"Generate maximized bounds for the given display or primary display if none is provided","2:15":"Generate maximized bounds for the given display or primary display if none is provided","2:16":"Generate maximized bounds for the given display or primary display if none is provided","2:17":"Generate maximized bounds for the given display or primary display if none is provided","2:18":"Generate maximized bounds for the given display or primary display if none is provided","2:19":"Generate maximized bounds for the given display or primary display if none is provided","2:2":"Generate maximized bounds for the given display or primary display if none is provided","2:20":"Generate maximized bounds for the given display or primary display if none is provided","2:21":"Generate maximized bounds for the given display or primary display if none is provided","2:22":"Generate maximized bounds for the given display or primary display if none is provided","2:23":"Generate maximized bounds for the given display or primary display if none is provided","2:24":"Generate maximized bounds for the given display or primary display if none is provided","2:25":"Generate maximized bounds for the given display or primary display if none is provided","2:26":"Generate maximized bounds for the given display or primary display if none is provided","2:27":"Generate maximized bounds for the given display or primary display if none is provided","2:28":"Generate maximized bounds for the given display or primary display if none is provided","2:29":"Generate maximized bounds for the given display or primary display if none is provided","2:3":"Generate maximized bounds for the given display or primary display if none is provided","2:30":"Generate maximized bounds for the given display or primary display if none is provided","2:31":"Generate maximized bounds for the given display or primary display if none is provided","2:32":"Generate maximized bounds for the given display or primary display if none is provided","2:33":"Generate maximized bounds for the given display or primary display if none is provided","2:34":"Generate maximized bounds for the given display or primary display if none is provided","2:35":"Generate maximized bounds for the given display or primary display if none is provided","2:36":"Generate maximized bounds for the given display or primary display if none is provided","2:37":"Generate maximized bounds for the given display or primary display if none is provided","2:38":"Generate maximized bounds for the given display or primary display if none is provided","2:39":"Generate maximized bounds for the given display or primary display if none is provided","2:4":"Generate maximized bounds for the given display or primary display if none is provided","2:40":"Generate maximized bounds for the given display or primary display if none is provided","2:41":"Generate maximized bounds for the given display or primary display if none is provided","2:42":"Generate maximized bounds for the given display or primary display if none is provided","2:43":"Generate maximized bounds for the given display or primary display if none is provided","2:44":"Generate maximized bounds for the given display or primary display if none is provided","2:45":"Generate maximized bounds for the given display or primary display if none is provided","2:46":"Generate maximized bounds for the given display or primary display if none is provided","2:5":"Generate maximized bounds for the given display or primary display if none is provided","2:6":"Generate maximized bounds for the given display or primary display if none is provided","2:7":"Generate maximized bounds for the given display or primary display if none is provided","2:8":"Generate maximized bounds for the given display or primary display if none is provided","2:9":"Generate maximized bounds for the given display or primary display if none is provided","3:0":"Generate maximized bounds for the given display or primary display if none is provided","3:1":"Generate maximized bounds for the given display or primary display if none is provided","3:2":"Generate maximized bounds for the given display or primary display if none is provided","3:3":"Generate maximized bounds for the given display or primary display if none is provided","3:4":"Generate maximized bounds for the given display or primary display if none is provided","4:0":"Generate maximized bounds for the given display or primary display if none is provided","4:1":"Generate maximized bounds for the given display or primary display if none is provided","4:2":"Generate maximized bounds for the given display or primary display if none is provided","4:3":"Generate maximized bounds for the given display or primary display if none is provided","4:4":"Generate maximized bounds for the given display or primary display if none is provided","4:5":"Generate maximized bounds for the given display or primary display if none is provided","4:6":"Generate maximized bounds for the given display or primary display if none is provided","5:0":"Generate maximized bounds for the given display or primary display if none is provided","5:1":"Generate maximized bounds for the given display or primary display if none is provided"},"s":{"0:1":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","0:2":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","0:3":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","0:4":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","0:5":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","0:6":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","1:0":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","1:1":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","1:2":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:0":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:1":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:10":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:11":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:12":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:13":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:14":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:15":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:16":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:17":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:18":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:19":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:2":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:20":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:21":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:22":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:23":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:24":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:25":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:26":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:27":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:28":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:29":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:3":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:30":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:31":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:32":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:33":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:34":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:35":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:36":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:37":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:38":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:39":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:4":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:40":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:41":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:42":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:43":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:44":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:45":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:46":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:5":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:6":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:7":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:8":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","2:9":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","3:0":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","3:1":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","3:2":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","3:3":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","3:4":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","4:0":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","4:1":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","4:2":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","4:3":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","4:4":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","4:5":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","4:6":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","5:0":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self","5:1":"fn Bounds::maximized(Option < DisplayId >, & App)-> Self"},"l":{"0:1":[["geometry.rs",784,796]],"0:2":[["geometry.rs",784,796]],"0:3":[["geometry.rs",784,796]],"0:4":[["geometry.rs",784,796]],"0:5":[["geometry.rs",784,796]],"0:6":[["geometry.rs",784,796]],"1:0":[["geometry.rs",784,796]],"1:1":[["geometry.rs",784,796]],"1:2":[["geometry.rs",768,780]],"2:0":[["geometry.rs",753,765]],"2:1":[["geometry.rs",753,765]],"2:10":[["geometry.rs",753,765]],"2:11":[["geometry.rs",753,765]],"2:12":[["geometry.rs",753,765]],"2:13":[["geometry.rs",753,765]],"2:14":[["geometry.rs",753,765]],"2:15":[["geometry.rs",753,765]],"2:16":[["geometry.rs",753,765]],"2:17":[["geometry.rs",753,765]],"2:18":[["geometry.rs",753,765]],"2:19":[["geometry.rs",753,765]],"2:2":[["geometry.rs",753,765]],"2:20":[["geometry.rs",753,765]],"2:21":[["geometry.rs",753,765]],"2:22":[["geometry.rs",753,765]],"2:23":[["geometry.rs",753,765]],"2:24":[["geometry.rs",753,765]],"2:25":[["geometry.rs",753,765]],"2:26":[["geometry.rs",753,765]],"2:27":[["geometry.rs",753,765]],"2:28":[["geometry.rs",753,765]],"2:29":[["geometry.rs",753,765]],"2:3":[["geometry.rs",753,765]],"2:30":[["geometry.rs",753,765]],"2:31":[["geometry.rs",753,765]],"2:32":[["geometry.rs",753,765]],"2:33":[["geometry.rs",753,765]],"2:34":[["geometry.rs",753,765]],"2:35":[["geometry.rs",753,765]],"2:36":[["geometry.rs",753,765]],"2:37":[["geometry.rs",753,765]],"2:38":[["geometry.rs",753,765]],"2:39":[["geometry.rs",753,765]],"2:4":[["geometry.rs",753,765]],"2:40":[["geometry.rs",753,765]],"2:41":[["geometry.rs",753,765]],"2:42":[["geometry.rs",753,765]],"2:43":[["geometry.rs",753,765]],"2:44":[["geometry.rs",753,765]],"2:45":[["geometry.rs",753,765]],"2:46":[["geometry.rs",753,765]],"2:5":[["geometry.rs",753,765]],"2:6":[["geometry.rs",753,765]],"2:7":[["geometry.rs",753,765]],"2:8":[["geometry.rs",753,765]],"2:9":[["geometry.rs",753,765]],"3:0":[["geometry.rs",753,765]],"3:1":[["geometry.rs",753,765]],"3:2":[["geometry.rs",753,765]],"3:3":[["geometry.rs",753,765]],"3:4":[["geometry.rs",753,765]],"4:0":[["geometry.rs",784,796]],"4:1":[["geometry.rs",784,796]],"4:2":[["geometry.rs",784,796]],"4:3":[["geometry.rs",784,796]],"4:4":[["geometry.rs",784,796]],"4:5":[["geometry.rs",784,796]],"4:6":[["geometry.rs",784,796]],"5:0":[["geometry.rs",753,765]],"5:1":[["geometry.rs",753,765]]}},{"k":"fn:Bounds::new","d":{"0:1":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","0:2":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","0:3":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","0:4":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","0:5":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","0:6":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","1:0":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","1:1":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","1:2":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:0":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:1":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:10":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:11":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:12":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:13":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:14":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:15":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:16":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:17":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:18":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:19":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:2":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:20":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:21":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:22":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:23":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:24":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:25":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:26":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:27":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:28":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:29":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:3":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:30":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:31":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:32":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:33":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:34":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:35":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:36":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:37":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:38":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:39":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:4":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:40":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:41":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:42":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:43":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:44":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:45":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:46":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:5":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:6":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:7":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:8":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","2:9":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","3:0":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","3:1":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","3:2":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","3:3":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","3:4":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","4:0":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","4:1":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","4:2":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","4:3":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","4:4":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","4:5":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","4:6":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","5:0":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size.","5:1":"Creates a new `Bounds` with the specified origin and size.\n\n# Arguments\n\n* `origin` - A `Point<T>` representing the origin of the bounds.\n* `size` - A `Size<T>` representing the size of the bounds.\n\n# Returns\n\nReturns a `Bounds<T>` that has the given origin and size."},"s":{"0:1":"fn Bounds::new(Point < T >, Size < T >)-> Self","0:2":"fn Bounds::new(Point < T >, Size < T >)-> Self","0:3":"fn Bounds::new(Point < T >, Size < T >)-> Self","0:4":"fn Bounds::new(Point < T >, Size < T >)-> Self","0:5":"fn Bounds::new(Point < T >, Size < T >)-> Self","0:6":"fn Bounds::new(Point < T >, Size < T >)-> Self","1:0":"fn Bounds::new(Point < T >, Size < T >)-> Self","1:1":"fn Bounds::new(Point < T >, Size < T >)-> Self","1:2":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:0":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:1":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:10":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:11":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:12":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:13":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:14":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:15":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:16":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:17":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:18":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:19":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:2":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:20":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:21":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:22":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:23":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:24":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:25":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:26":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:27":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:28":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:29":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:3":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:30":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:31":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:32":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:33":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:34":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:35":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:36":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:37":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:38":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:39":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:4":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:40":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:41":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:42":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:43":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:44":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:45":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:46":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:5":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:6":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:7":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:8":"fn Bounds::new(Point < T >, Size < T >)-> Self","2:9":"fn Bounds::new(Point < T >, Size < T >)-> Self","3:0":"fn Bounds::new(Point < T >, Size < T >)-> Self","3:1":"fn Bounds::new(Point < T >, Size < T >)-> Self","3:2":"fn Bounds::new(Point < T >, Size < T >)-> Self","3:3":"fn Bounds::new(Point < T >, Size < T >)-> Self","3:4":"fn Bounds::new(Point < T >, Size < T >)-> Self","4:0":"fn Bounds::new(Point < T >, Size < T >)-> Self","4:1":"fn Bounds::new(Point < T >, Size < T >)-> Self","4:2":"fn Bounds::new(Point < T >, Size < T >)-> Self","4:3":"fn Bounds::new(Point < T >, Size < T >)-> Self","4:4":"fn Bounds::new(Point < T >, Size < T >)-> Self","4:5":"fn Bounds::new(Point < T >, Size < T >)-> Self","4:6":"fn Bounds::new(Point < T >, Size < T >)-> Self","5:0":"fn Bounds::new(Point < T >, Size < T >)-> Self","5:1":"fn Bounds::new(Point < T >, Size < T >)-> Self"},"l":{"0:1":[["geometry.rs",803,815]],"0:2":[["geometry.rs",803,815]],"0:3":[["geometry.rs",803,815]],"0:4":[["geometry.rs",803,815]],"0:5":[["geometry.rs",803,815]],"0:6":[["geometry.rs",803,815]],"1:0":[["geometry.rs",803,815]],"1:1":[["geometry.rs",803,815]],"1:2":[["geometry.rs",787,799]],"2:0":[["geometry.rs",772,784]],"2:1":[["geometry.rs",772,784]],"2:10":[["geometry.rs",772,784]],"2:11":[["geometry.rs",772,784]],"2:12":[["geometry.rs",772,784]],"2:13":[["geometry.rs",772,784]],"2:14":[["geometry.rs",772,784]],"2:15":[["geometry.rs",772,784]],"2:16":[["geometry.rs",772,784]],"2:17":[["geometry.rs",772,784]],"2:18":[["geometry.rs",772,784]],"2:19":[["geometry.rs",772,784]],"2:2":[["geometry.rs",772,784]],"2:20":[["geometry.rs",772,784]],"2:21":[["geometry.rs",772,784]],"2:22":[["geometry.rs",772,784]],"2:23":[["geometry.rs",772,784]],"2:24":[["geometry.rs",772,784]],"2:25":[["geometry.rs",772,784]],"2:26":[["geometry.rs",772,784]],"2:27":[["geometry.rs",772,784]],"2:28":[["geometry.rs",772,784]],"2:29":[["geometry.rs",772,784]],"2:3":[["geometry.rs",772,784]],"2:30":[["geometry.rs",772,784]],"2:31":[["geometry.rs",772,784]],"2:32":[["geometry.rs",772,784]],"2:33":[["geometry.rs",772,784]],"2:34":[["geometry.rs",772,784]],"2:35":[["geometry.rs",772,784]],"2:36":[["geometry.rs",772,784]],"2:37":[["geometry.rs",772,784]],"2:38":[["geometry.rs",772,784]],"2:39":[["geometry.rs",772,784]],"2:4":[["geometry.rs",772,784]],"2:40":[["geometry.rs",772,784]],"2:41":[["geometry.rs",772,784]],"2:42":[["geometry.rs",772,784]],"2:43":[["geometry.rs",772,784]],"2:44":[["geometry.rs",772,784]],"2:45":[["geometry.rs",772,784]],"2:46":[["geometry.rs",772,784]],"2:5":[["geometry.rs",772,784]],"2:6":[["geometry.rs",772,784]],"2:7":[["geometry.rs",772,784]],"2:8":[["geometry.rs",772,784]],"2:9":[["geometry.rs",772,784]],"3:0":[["geometry.rs",772,784]],"3:1":[["geometry.rs",772,784]],"3:2":[["geometry.rs",772,784]],"3:3":[["geometry.rs",772,784]],"3:4":[["geometry.rs",772,784]],"4:0":[["geometry.rs",803,815]],"4:1":[["geometry.rs",803,815]],"4:2":[["geometry.rs",803,815]],"4:3":[["geometry.rs",803,815]],"4:4":[["geometry.rs",803,815]],"4:5":[["geometry.rs",803,815]],"4:6":[["geometry.rs",803,815]],"5:0":[["geometry.rs",772,784]],"5:1":[["geometry.rs",772,784]]}},{"k":"fn:Bounds::right","d":{"0:1":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","0:2":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","0:3":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","0:4":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","0:5":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","0:6":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","1:0":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","1:1":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","1:2":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:0":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:1":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:10":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:11":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:12":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:13":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:14":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:15":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:16":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:17":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:18":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:19":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:2":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:20":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:21":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:22":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:23":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:24":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:25":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:26":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:27":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:28":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:29":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:3":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:30":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:31":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:32":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:33":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:34":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:35":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:36":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:37":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:38":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:39":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:4":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:40":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:41":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:42":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:43":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:44":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:45":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:46":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:5":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:6":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:7":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:8":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","2:9":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","3:0":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","3:1":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","3:2":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","3:3":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","3:4":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","4:0":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","4:1":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","4:2":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","4:3":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","4:4":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","4:5":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","4:6":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","5:0":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds.","5:1":"Returns the right edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the x-coordinate of the right edge of the bounds."},"s":{"0:1":"fn Bounds::right(& self)-> T","0:2":"fn Bounds::right(& self)-> T","0:3":"fn Bounds::right(& self)-> T","0:4":"fn Bounds::right(& self)-> T","0:5":"fn Bounds::right(& self)-> T","0:6":"fn Bounds::right(& self)-> T","1:0":"fn Bounds::right(& self)-> T","1:1":"fn Bounds::right(& self)-> T","1:2":"fn Bounds::right(& self)-> T","2:0":"fn Bounds::right(& self)-> T","2:1":"fn Bounds::right(& self)-> T","2:10":"fn Bounds::right(& self)-> T","2:11":"fn Bounds::right(& self)-> T","2:12":"fn Bounds::right(& self)-> T","2:13":"fn Bounds::right(& self)-> T","2:14":"fn Bounds::right(& self)-> T","2:15":"fn Bounds::right(& self)-> T","2:16":"fn Bounds::right(& self)-> T","2:17":"fn Bounds::right(& self)-> T","2:18":"fn Bounds::right(& self)-> T","2:19":"fn Bounds::right(& self)-> T","2:2":"fn Bounds::right(& self)-> T","2:20":"fn Bounds::right(& self)-> T","2:21":"fn Bounds::right(& self)-> T","2:22":"fn Bounds::right(& self)-> T","2:23":"fn Bounds::right(& self)-> T","2:24":"fn Bounds::right(& self)-> T","2:25":"fn Bounds::right(& self)-> T","2:26":"fn Bounds::right(& self)-> T","2:27":"fn Bounds::right(& self)-> T","2:28":"fn Bounds::right(& self)-> T","2:29":"fn Bounds::right(& self)-> T","2:3":"fn Bounds::right(& self)-> T","2:30":"fn Bounds::right(& self)-> T","2:31":"fn Bounds::right(& self)-> T","2:32":"fn Bounds::right(& self)-> T","2:33":"fn Bounds::right(& self)-> T","2:34":"fn Bounds::right(& self)-> T","2:35":"fn Bounds::right(& self)-> T","2:36":"fn Bounds::right(& self)-> T","2:37":"fn Bounds::right(& self)-> T","2:38":"fn Bounds::right(& self)-> T","2:39":"fn Bounds::right(& self)-> T","2:4":"fn Bounds::right(& self)-> T","2:40":"fn Bounds::right(& self)-> T","2:41":"fn Bounds::right(& self)-> T","2:42":"fn Bounds::right(& self)-> T","2:43":"fn Bounds::right(& self)-> T","2:44":"fn Bounds::right(& self)-> T","2:45":"fn Bounds::right(& self)-> T","2:46":"fn Bounds::right(& self)-> T","2:5":"fn Bounds::right(& self)-> T","2:6":"fn Bounds::right(& self)-> T","2:7":"fn Bounds::right(& self)-> T","2:8":"fn Bounds::right(& self)-> T","2:9":"fn Bounds::right(& self)-> T","3:0":"fn Bounds::right(& self)-> T","3:1":"fn Bounds::right(& self)-> T","3:2":"fn Bounds::right(& self)-> T","3:3":"fn Bounds::right(& self)-> T","3:4":"fn Bounds::right(& self)-> T","4:0":"fn Bounds::right(& self)-> T","4:1":"fn Bounds::right(& self)-> T","4:2":"fn Bounds::right(& self)-> T","4:3":"fn Bounds::right(& self)-> T","4:4":"fn Bounds::right(& self)-> T","4:5":"fn Bounds::right(& self)-> T","4:6":"fn Bounds::right(& self)-> T","5:0":"fn Bounds::right(& self)-> T","5:1":"fn Bounds::right(& self)-> T"},"l":{"0:1":[["geometry.rs",1276,1283]],"0:2":[["geometry.rs",1276,1283]],"0:3":[["geometry.rs",1276,1283]],"0:4":[["geometry.rs",1276,1283]],"0:5":[["geometry.rs",1276,1283]],"0:6":[["geometry.rs",1276,1283]],"1:0":[["geometry.rs",1276,1283]],"1:1":[["geometry.rs",1276,1283]],"1:2":[["geometry.rs",1330,1337]],"2:0":[["geometry.rs",1257,1264]],"2:1":[["geometry.rs",1257,1264]],"2:10":[["geometry.rs",1257,1264]],"2:11":[["geometry.rs",1257,1264]],"2:12":[["geometry.rs",1315,1322]],"2:13":[["geometry.rs",1315,1322]],"2:14":[["geometry.rs",1315,1322]],"2:15":[["geometry.rs",1315,1322]],"2:16":[["geometry.rs",1315,1322]],"2:17":[["geometry.rs",1315,1322]],"2:18":[["geometry.rs",1315,1322]],"2:19":[["geometry.rs",1315,1322]],"2:2":[["geometry.rs",1257,1264]],"2:20":[["geometry.rs",1315,1322]],"2:21":[["geometry.rs",1315,1322]],"2:22":[["geometry.rs",1315,1322]],"2:23":[["geometry.rs",1315,1322]],"2:24":[["geometry.rs",1315,1322]],"2:25":[["geometry.rs",1315,1322]],"2:26":[["geometry.rs",1315,1322]],"2:27":[["geometry.rs",1315,1322]],"2:28":[["geometry.rs",1315,1322]],"2:29":[["geometry.rs",1315,1322]],"2:3":[["geometry.rs",1257,1264]],"2:30":[["geometry.rs",1315,1322]],"2:31":[["geometry.rs",1315,1322]],"2:32":[["geometry.rs",1315,1322]],"2:33":[["geometry.rs",1315,1322]],"2:34":[["geometry.rs",1315,1322]],"2:35":[["geometry.rs",1315,1322]],"2:36":[["geometry.rs",1315,1322]],"2:37":[["geometry.rs",1315,1322]],"2:38":[["geometry.rs",1315,1322]],"2:39":[["geometry.rs",1315,1322]],"2:4":[["geometry.rs",1257,1264]],"2:40":[["geometry.rs",1315,1322]],"2:41":[["geometry.rs",1315,1322]],"2:42":[["geometry.rs",1315,1322]],"2:43":[["geometry.rs",1315,1322]],"2:44":[["geometry.rs",1315,1322]],"2:45":[["geometry.rs",1315,1322]],"2:46":[["geometry.rs",1315,1322]],"2:5":[["geometry.rs",1257,1264]],"2:6":[["geometry.rs",1257,1264]],"2:7":[["geometry.rs",1257,1264]],"2:8":[["geometry.rs",1257,1264]],"2:9":[["geometry.rs",1257,1264]],"3:0":[["geometry.rs",1315,1322]],"3:1":[["geometry.rs",1315,1322]],"3:2":[["geometry.rs",1315,1322]],"3:3":[["geometry.rs",1315,1322]],"3:4":[["geometry.rs",1315,1322]],"4:0":[["geometry.rs",1289,1296]],"4:1":[["geometry.rs",1289,1296]],"4:2":[["geometry.rs",1289,1296]],"4:3":[["geometry.rs",1289,1296]],"4:4":[["geometry.rs",1289,1296]],"4:5":[["geometry.rs",1289,1296]],"4:6":[["geometry.rs",1289,1296]],"5:0":[["geometry.rs",1315,1322]],"5:1":[["geometry.rs",1315,1322]]}},{"k":"fn:Bounds::right_center","d":{"1:2":"Returns the right center point of the bounds.","2:12":"Returns the right center point of the bounds.","2:13":"Returns the right center point of the bounds.","2:14":"Returns the right center point of the bounds.","2:15":"Returns the right center point of the bounds.","2:16":"Returns the right center point of the bounds.","2:17":"Returns the right center point of the bounds.","2:18":"Returns the right center point of the bounds.","2:19":"Returns the right center point of the bounds.","2:20":"Returns the right center point of the bounds.","2:21":"Returns the right center point of the bounds.","2:22":"Returns the right center point of the bounds.","2:23":"Returns the right center point of the bounds.","2:24":"Returns the right center point of the bounds.","2:25":"Returns the right center point of the bounds.","2:26":"Returns the right center point of the bounds.","2:27":"Returns the right center point of the bounds.","2:28":"Returns the right center point of the bounds.","2:29":"Returns the right center point of the bounds.","2:30":"Returns the right center point of the bounds.","2:31":"Returns the right center point of the bounds.","2:32":"Returns the right center point of the bounds.","2:33":"Returns the right center point of the bounds.","2:34":"Returns the right center point of the bounds.","2:35":"Returns the right center point of the bounds.","2:36":"Returns the right center point of the bounds.","2:37":"Returns the right center point of the bounds.","2:38":"Returns the right center point of the bounds.","2:39":"Returns the right center point of the bounds.","2:40":"Returns the right center point of the bounds.","2:41":"Returns the right center point of the bounds.","2:42":"Returns the right center point of the bounds.","2:43":"Returns the right center point of the bounds.","2:44":"Returns the right center point of the bounds.","2:45":"Returns the right center point of the bounds.","2:46":"Returns the right center point of the bounds.","3:0":"Returns the right center point of the bounds.","3:1":"Returns the right center point of the bounds.","3:2":"Returns the right center point of the bounds.","3:3":"Returns the right center point of the bounds.","3:4":"Returns the right center point of the bounds.","5:0":"Returns the right center point of the bounds.","5:1":"Returns the right center point of the bounds."},"s":{"1:2":"fn Bounds::right_center(& self)-> Point < T >","2:12":"fn Bounds::right_center(& self)-> Point < T >","2:13":"fn Bounds::right_center(& self)-> Point < T >","2:14":"fn Bounds::right_center(& self)-> Point < T >","2:15":"fn Bounds::right_center(& self)-> Point < T >","2:16":"fn Bounds::right_center(& self)-> Point < T >","2:17":"fn Bounds::right_center(& self)-> Point < T >","2:18":"fn Bounds::right_center(& self)-> Point < T >","2:19":"fn Bounds::right_center(& self)-> Point < T >","2:20":"fn Bounds::right_center(& self)-> Point < T >","2:21":"fn Bounds::right_center(& self)-> Point < T >","2:22":"fn Bounds::right_center(& self)-> Point < T >","2:23":"fn Bounds::right_center(& self)-> Point < T >","2:24":"fn Bounds::right_center(& self)-> Point < T >","2:25":"fn Bounds::right_center(& self)-> Point < T >","2:26":"fn Bounds::right_center(& self)-> Point < T >","2:27":"fn Bounds::right_center(& self)-> Point < T >","2:28":"fn Bounds::right_center(& self)-> Point < T >","2:29":"fn Bounds::right_center(& self)-> Point < T >","2:30":"fn Bounds::right_center(& self)-> Point < T >","2:31":"fn Bounds::right_center(& self)-> Point < T >","2:32":"fn Bounds::right_center(& self)-> Point < T >","2:33":"fn Bounds::right_center(& self)-> Point < T >","2:34":"fn Bounds::right_center(& self)-> Point < T >","2:35":"fn Bounds::right_center(& self)-> Point < T >","2:36":"fn Bounds::right_center(& self)-> Point < T >","2:37":"fn Bounds::right_center(& self)-> Point < T >","2:38":"fn Bounds::right_center(& self)-> Point < T >","2:39":"fn Bounds::right_center(& self)-> Point < T >","2:40":"fn Bounds::right_center(& self)-> Point < T >","2:41":"fn Bounds::right_center(& self)-> Point < T >","2:42":"fn Bounds::right_center(& self)-> Point < T >","2:43":"fn Bounds::right_center(& self)-> Point < T >","2:44":"fn Bounds::right_center(& self)-> Point < T >","2:45":"fn Bounds::right_center(& self)-> Point < T >","2:46":"fn Bounds::right_center(& self)-> Point < T >","3:0":"fn Bounds::right_center(& self)-> Point < T >","3:1":"fn Bounds::right_center(& self)-> Point < T >","3:2":"fn Bounds::right_center(& self)-> Point < T >","3:3":"fn Bounds::right_center(& self)-> Point < T >","3:4":"fn Bounds::right_center(& self)-> Point < T >","5:0":"fn Bounds::right_center(& self)-> Point < T >","5:1":"fn Bounds::right_center(& self)-> Point < T >"},"l":{"1:2":[["geometry.rs",931,937]],"2:12":[["geometry.rs",916,922]],"2:13":[["geometry.rs",916,922]],"2:14":[["geometry.rs",916,922]],"2:15":[["geometry.rs",916,922]],"2:16":[["geometry.rs",916,922]],"2:17":[["geometry.rs",916,922]],"2:18":[["geometry.rs",916,922]],"2:19":[["geometry.rs",916,922]],"2:20":[["geometry.rs",916,922]],"2:21":[["geometry.rs",916,922]],"2:22":[["geometry.rs",916,922]],"2:23":[["geometry.rs",916,922]],"2:24":[["geometry.rs",916,922]],"2:25":[["geometry.rs",916,922]],"2:26":[["geometry.rs",916,922]],"2:27":[["geometry.rs",916,922]],"2:28":[["geometry.rs",916,922]],"2:29":[["geometry.rs",916,922]],"2:30":[["geometry.rs",916,922]],"2:31":[["geometry.rs",916,922]],"2:32":[["geometry.rs",916,922]],"2:33":[["geometry.rs",916,922]],"2:34":[["geometry.rs",916,922]],"2:35":[["geometry.rs",916,922]],"2:36":[["geometry.rs",916,922]],"2:37":[["geometry.rs",916,922]],"2:38":[["geometry.rs",916,922]],"2:39":[["geometry.rs",916,922]],"2:40":[["geometry.rs",916,922]],"2:41":[["geometry.rs",916,922]],"2:42":[["geometry.rs",916,922]],"2:43":[["geometry.rs",916,922]],"2:44":[["geometry.rs",916,922]],"2:45":[["geometry.rs",916,922]],"2:46":[["geometry.rs",916,922]],"3:0":[["geometry.rs",916,922]],"3:1":[["geometry.rs",916,922]],"3:2":[["geometry.rs",916,922]],"3:3":[["geometry.rs",916,922]],"3:4":[["geometry.rs",916,922]],"5:0":[["geometry.rs",916,922]],"5:1":[["geometry.rs",916,922]]}},{"k":"fn:Bounds::scale","d":{"0:1":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels(10.0), y: Pixels(20.0) },\n    size: Size { width: Pixels(30.0), height: Pixels(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels(20.0),\n        y: ScaledPixels(40.0),\n    },\n    size: Size {\n        width: ScaledPixels(60.0),\n        height: ScaledPixels(80.0)\n    },\n});\n```","0:2":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","0:3":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","0:4":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","0:5":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","0:6":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","1:0":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","1:1":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","1:2":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:0":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:1":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:10":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:11":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:12":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:13":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:14":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:15":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:16":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:17":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:18":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:19":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:2":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:20":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:21":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:22":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:23":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:24":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:25":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:26":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:27":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:28":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:29":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:3":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:30":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:31":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:32":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:33":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:34":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:35":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:36":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:37":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:38":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:39":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:4":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:40":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:41":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:42":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:43":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:44":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:45":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:46":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:5":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:6":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:7":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:8":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","2:9":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","3:0":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","3:1":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","3:2":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","3:3":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","3:4":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","4:0":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","4:1":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","4:2":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","4:3":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","4:4":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","4:5":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","4:6":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","5:0":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```","5:1":"Scales the bounds by a given factor, typically used to adjust for display scaling.\n\nThis method multiplies the origin and size of the bounds by the provided scaling factor,\nresulting in a new `Bounds<ScaledPixels>` that is proportionally larger or smaller\ndepending on the scaling factor. This can be used to ensure that the bounds are properly\nscaled for different display densities.\n\n# Arguments\n\n* `factor` - The scaling factor to apply to the origin and size, typically the display's scaling factor.\n\n# Returns\n\nReturns a new `Bounds<ScaledPixels>` that represents the scaled bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size, Pixels, ScaledPixels, DevicePixels};\nlet bounds = Bounds {\n    origin: Point { x: Pixels::from(10.0), y: Pixels::from(20.0) },\n    size: Size { width: Pixels::from(30.0), height: Pixels::from(40.0) },\n};\nlet display_scale_factor = 2.0;\nlet scaled_bounds = bounds.scale(display_scale_factor);\nassert_eq!(scaled_bounds, Bounds {\n    origin: Point {\n        x: ScaledPixels::from(20.0),\n        y: ScaledPixels::from(40.0),\n    },\n    size: Size {\n        width: ScaledPixels::from(60.0),\n        height: ScaledPixels::from(80.0)\n    },\n});\n```"},"s":{"0:1":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","0:2":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","0:3":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","0:4":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","0:5":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","0:6":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","1:0":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","1:1":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","1:2":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:0":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:1":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:10":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:11":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:12":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:13":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:14":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:15":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:16":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:17":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:18":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:19":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:2":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:20":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:21":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:22":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:23":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:24":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:25":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:26":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:27":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:28":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:29":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:3":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:30":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:31":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:32":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:33":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:34":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:35":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:36":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:37":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:38":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:39":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:4":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:40":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:41":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:42":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:43":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:44":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:45":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:46":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:5":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:6":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:7":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:8":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","2:9":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","3:0":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","3:1":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","3:2":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","3:3":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","3:4":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","4:0":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","4:1":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","4:2":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","4:3":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","4:4":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","4:5":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","4:6":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","5:0":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >","5:1":"fn Bounds::scale(& self, f32)-> Bounds < ScaledPixels >"},"l":{"0:1":[["geometry.rs",1611,1652]],"0:2":[["geometry.rs",1611,1652]],"0:3":[["geometry.rs",1611,1652]],"0:4":[["geometry.rs",1611,1652]],"0:5":[["geometry.rs",1611,1652]],"0:6":[["geometry.rs",1611,1652]],"1:0":[["geometry.rs",1611,1652]],"1:1":[["geometry.rs",1611,1652]],"1:2":[["geometry.rs",1674,1715]],"2:0":[["geometry.rs",1592,1633]],"2:1":[["geometry.rs",1592,1633]],"2:10":[["geometry.rs",1592,1633]],"2:11":[["geometry.rs",1592,1633]],"2:12":[["geometry.rs",1659,1700]],"2:13":[["geometry.rs",1659,1700]],"2:14":[["geometry.rs",1659,1700]],"2:15":[["geometry.rs",1659,1700]],"2:16":[["geometry.rs",1659,1700]],"2:17":[["geometry.rs",1659,1700]],"2:18":[["geometry.rs",1659,1700]],"2:19":[["geometry.rs",1659,1700]],"2:2":[["geometry.rs",1592,1633]],"2:20":[["geometry.rs",1659,1700]],"2:21":[["geometry.rs",1659,1700]],"2:22":[["geometry.rs",1659,1700]],"2:23":[["geometry.rs",1659,1700]],"2:24":[["geometry.rs",1659,1700]],"2:25":[["geometry.rs",1659,1700]],"2:26":[["geometry.rs",1659,1700]],"2:27":[["geometry.rs",1659,1700]],"2:28":[["geometry.rs",1659,1700]],"2:29":[["geometry.rs",1659,1700]],"2:3":[["geometry.rs",1592,1633]],"2:30":[["geometry.rs",1659,1700]],"2:31":[["geometry.rs",1659,1700]],"2:32":[["geometry.rs",1659,1700]],"2:33":[["geometry.rs",1659,1700]],"2:34":[["geometry.rs",1659,1700]],"2:35":[["geometry.rs",1659,1700]],"2:36":[["geometry.rs",1659,1700]],"2:37":[["geometry.rs",1659,1700]],"2:38":[["geometry.rs",1659,1700]],"2:39":[["geometry.rs",1659,1700]],"2:4":[["geometry.rs",1592,1633]],"2:40":[["geometry.rs",1659,1700]],"2:41":[["geometry.rs",1659,1700]],"2:42":[["geometry.rs",1659,1700]],"2:43":[["geometry.rs",1659,1700]],"2:44":[["geometry.rs",1659,1700]],"2:45":[["geometry.rs",1659,1700]],"2:46":[["geometry.rs",1659,1700]],"2:5":[["geometry.rs",1592,1633]],"2:6":[["geometry.rs",1592,1633]],"2:7":[["geometry.rs",1592,1633]],"2:8":[["geometry.rs",1592,1633]],"2:9":[["geometry.rs",1592,1633]],"3:0":[["geometry.rs",1659,1700]],"3:1":[["geometry.rs",1659,1700]],"3:2":[["geometry.rs",1659,1700]],"3:3":[["geometry.rs",1659,1700]],"3:4":[["geometry.rs",1659,1700]],"4:0":[["geometry.rs",1624,1665]],"4:1":[["geometry.rs",1624,1665]],"4:2":[["geometry.rs",1624,1665]],"4:3":[["geometry.rs",1624,1665]],"4:4":[["geometry.rs",1624,1665]],"4:5":[["geometry.rs",1624,1665]],"4:6":[["geometry.rs",1624,1665]],"5:0":[["geometry.rs",1659,1700]],"5:1":[["geometry.rs",1659,1700]]}},{"k":"fn:Bounds::scale_and_snap","d":{"4:0":"Scale bounds to device coordinates and snap to the device pixel grid.\n\nThe origin and far edge are rounded independently so adjacent elements\nshare exact pixel boundaries without gaps or overlaps.","4:1":"Scale bounds to device coordinates and snap to the device pixel grid.\n\nThe origin and far edge are rounded independently so adjacent elements\nshare exact pixel boundaries without gaps or overlaps.","4:2":"Scale bounds to device coordinates and snap to the device pixel grid.\n\nThe origin and far edge are rounded independently so adjacent elements\nshare exact pixel boundaries without gaps or overlaps.","4:3":"Scale bounds to device coordinates and snap to the device pixel grid.\n\nThe origin and far edge are rounded independently so adjacent elements\nshare exact pixel boundaries without gaps or overlaps.","4:4":"Scale bounds to device coordinates and snap to the device pixel grid.\n\nThe origin and far edge are rounded independently so adjacent elements\nshare exact pixel boundaries without gaps or overlaps.","4:5":"Scale bounds to device coordinates and snap to the device pixel grid.\n\nThe origin and far edge are rounded independently so adjacent elements\nshare exact pixel boundaries without gaps or overlaps.","4:6":"Scale bounds to device coordinates and snap to the device pixel grid.\n\nThe origin and far edge are rounded independently so adjacent elements\nshare exact pixel boundaries without gaps or overlaps."},"s":{"4:0":"fn Bounds::scale_and_snap(& self, f32)-> Bounds < ScaledPixels >","4:1":"fn Bounds::scale_and_snap(& self, f32)-> Bounds < ScaledPixels >","4:2":"fn Bounds::scale_and_snap(& self, f32)-> Bounds < ScaledPixels >","4:3":"fn Bounds::scale_and_snap(& self, f32)-> Bounds < ScaledPixels >","4:4":"fn Bounds::scale_and_snap(& self, f32)-> Bounds < ScaledPixels >","4:5":"fn Bounds::scale_and_snap(& self, f32)-> Bounds < ScaledPixels >","4:6":"fn Bounds::scale_and_snap(& self, f32)-> Bounds < ScaledPixels >"},"l":{"4:0":[["geometry.rs",1667,1692]],"4:1":[["geometry.rs",1667,1692]],"4:2":[["geometry.rs",1667,1692]],"4:3":[["geometry.rs",1667,1692]],"4:4":[["geometry.rs",1667,1692]],"4:5":[["geometry.rs",1667,1692]],"4:6":[["geometry.rs",1667,1692]]}},{"k":"fn:Bounds::scale_and_snap_conservative","d":{"4:0":"Scale bounds to device coordinates while conservatively covering the logical bounds.","4:1":"Scale bounds to device coordinates while conservatively covering the logical bounds.","4:2":"Scale bounds to device coordinates while conservatively covering the logical bounds.","4:3":"Scale bounds to device coordinates while conservatively covering the logical bounds.","4:4":"Scale bounds to device coordinates while conservatively covering the logical bounds.","4:5":"Scale bounds to device coordinates while conservatively covering the logical bounds.","4:6":"Scale bounds to device coordinates while conservatively covering the logical bounds."},"s":{"4:0":"fn Bounds::scale_and_snap_conservative(& self, f32)-> Bounds < ScaledPixels >","4:1":"fn Bounds::scale_and_snap_conservative(& self, f32)-> Bounds < ScaledPixels >","4:2":"fn Bounds::scale_and_snap_conservative(& self, f32)-> Bounds < ScaledPixels >","4:3":"fn Bounds::scale_and_snap_conservative(& self, f32)-> Bounds < ScaledPixels >","4:4":"fn Bounds::scale_and_snap_conservative(& self, f32)-> Bounds < ScaledPixels >","4:5":"fn Bounds::scale_and_snap_conservative(& self, f32)-> Bounds < ScaledPixels >","4:6":"fn Bounds::scale_and_snap_conservative(& self, f32)-> Bounds < ScaledPixels >"},"l":{"4:0":[["geometry.rs",1694,1716]],"4:1":[["geometry.rs",1694,1716]],"4:2":[["geometry.rs",1694,1716]],"4:3":[["geometry.rs",1694,1716]],"4:4":[["geometry.rs",1694,1716]],"4:5":[["geometry.rs",1694,1716]],"4:6":[["geometry.rs",1694,1716]]}},{"k":"fn:Bounds::space_within","d":{"0:1":"Computes the space available within outer bounds.","0:2":"Computes the space available within outer bounds.","0:3":"Computes the space available within outer bounds.","0:4":"Computes the space available within outer bounds.","0:5":"Computes the space available within outer bounds.","0:6":"Computes the space available within outer bounds.","1:0":"Computes the space available within outer bounds.","1:1":"Computes the space available within outer bounds.","1:2":"Computes the space available within outer bounds.","2:0":"Computes the space available within outer bounds.","2:1":"Computes the space available within outer bounds.","2:10":"Computes the space available within outer bounds.","2:11":"Computes the space available within outer bounds.","2:12":"Computes the space available within outer bounds.","2:13":"Computes the space available within outer bounds.","2:14":"Computes the space available within outer bounds.","2:15":"Computes the space available within outer bounds.","2:16":"Computes the space available within outer bounds.","2:17":"Computes the space available within outer bounds.","2:18":"Computes the space available within outer bounds.","2:19":"Computes the space available within outer bounds.","2:2":"Computes the space available within outer bounds.","2:20":"Computes the space available within outer bounds.","2:21":"Computes the space available within outer bounds.","2:22":"Computes the space available within outer bounds.","2:23":"Computes the space available within outer bounds.","2:24":"Computes the space available within outer bounds.","2:25":"Computes the space available within outer bounds.","2:26":"Computes the space available within outer bounds.","2:27":"Computes the space available within outer bounds.","2:28":"Computes the space available within outer bounds.","2:29":"Computes the space available within outer bounds.","2:3":"Computes the space available within outer bounds.","2:30":"Computes the space available within outer bounds.","2:31":"Computes the space available within outer bounds.","2:32":"Computes the space available within outer bounds.","2:33":"Computes the space available within outer bounds.","2:34":"Computes the space available within outer bounds.","2:35":"Computes the space available within outer bounds.","2:36":"Computes the space available within outer bounds.","2:37":"Computes the space available within outer bounds.","2:38":"Computes the space available within outer bounds.","2:39":"Computes the space available within outer bounds.","2:4":"Computes the space available within outer bounds.","2:40":"Computes the space available within outer bounds.","2:41":"Computes the space available within outer bounds.","2:42":"Computes the space available within outer bounds.","2:43":"Computes the space available within outer bounds.","2:44":"Computes the space available within outer bounds.","2:45":"Computes the space available within outer bounds.","2:46":"Computes the space available within outer bounds.","2:5":"Computes the space available within outer bounds.","2:6":"Computes the space available within outer bounds.","2:7":"Computes the space available within outer bounds.","2:8":"Computes the space available within outer bounds.","2:9":"Computes the space available within outer bounds.","3:0":"Computes the space available within outer bounds.","3:1":"Computes the space available within outer bounds.","3:2":"Computes the space available within outer bounds.","3:3":"Computes the space available within outer bounds.","3:4":"Computes the space available within outer bounds.","4:0":"Computes the space available within outer bounds.","4:1":"Computes the space available within outer bounds.","4:2":"Computes the space available within outer bounds.","4:3":"Computes the space available within outer bounds.","4:4":"Computes the space available within outer bounds.","4:5":"Computes the space available within outer bounds.","4:6":"Computes the space available within outer bounds.","5:0":"Computes the space available within outer bounds.","5:1":"Computes the space available within outer bounds."},"s":{"0:1":"fn Bounds::space_within(& self, & Self)-> Edges < T >","0:2":"fn Bounds::space_within(& self, & Self)-> Edges < T >","0:3":"fn Bounds::space_within(& self, & Self)-> Edges < T >","0:4":"fn Bounds::space_within(& self, & Self)-> Edges < T >","0:5":"fn Bounds::space_within(& self, & Self)-> Edges < T >","0:6":"fn Bounds::space_within(& self, & Self)-> Edges < T >","1:0":"fn Bounds::space_within(& self, & Self)-> Edges < T >","1:1":"fn Bounds::space_within(& self, & Self)-> Edges < T >","1:2":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:0":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:1":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:10":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:11":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:12":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:13":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:14":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:15":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:16":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:17":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:18":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:19":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:2":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:20":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:21":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:22":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:23":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:24":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:25":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:26":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:27":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:28":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:29":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:3":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:30":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:31":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:32":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:33":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:34":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:35":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:36":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:37":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:38":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:39":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:4":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:40":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:41":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:42":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:43":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:44":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:45":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:46":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:5":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:6":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:7":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:8":"fn Bounds::space_within(& self, & Self)-> Edges < T >","2:9":"fn Bounds::space_within(& self, & Self)-> Edges < T >","3:0":"fn Bounds::space_within(& self, & Self)-> Edges < T >","3:1":"fn Bounds::space_within(& self, & Self)-> Edges < T >","3:2":"fn Bounds::space_within(& self, & Self)-> Edges < T >","3:3":"fn Bounds::space_within(& self, & Self)-> Edges < T >","3:4":"fn Bounds::space_within(& self, & Self)-> Edges < T >","4:0":"fn Bounds::space_within(& self, & Self)-> Edges < T >","4:1":"fn Bounds::space_within(& self, & Self)-> Edges < T >","4:2":"fn Bounds::space_within(& self, & Self)-> Edges < T >","4:3":"fn Bounds::space_within(& self, & Self)-> Edges < T >","4:4":"fn Bounds::space_within(& self, & Self)-> Edges < T >","4:5":"fn Bounds::space_within(& self, & Self)-> Edges < T >","4:6":"fn Bounds::space_within(& self, & Self)-> Edges < T >","5:0":"fn Bounds::space_within(& self, & Self)-> Edges < T >","5:1":"fn Bounds::space_within(& self, & Self)-> Edges < T >"},"l":{"0:1":[["geometry.rs",1163,1171]],"0:2":[["geometry.rs",1163,1171]],"0:3":[["geometry.rs",1163,1171]],"0:4":[["geometry.rs",1163,1171]],"0:5":[["geometry.rs",1163,1171]],"0:6":[["geometry.rs",1163,1171]],"1:0":[["geometry.rs",1163,1171]],"1:1":[["geometry.rs",1163,1171]],"1:2":[["geometry.rs",1208,1216]],"2:0":[["geometry.rs",1135,1143]],"2:1":[["geometry.rs",1135,1143]],"2:10":[["geometry.rs",1135,1143]],"2:11":[["geometry.rs",1135,1143]],"2:12":[["geometry.rs",1193,1201]],"2:13":[["geometry.rs",1193,1201]],"2:14":[["geometry.rs",1193,1201]],"2:15":[["geometry.rs",1193,1201]],"2:16":[["geometry.rs",1193,1201]],"2:17":[["geometry.rs",1193,1201]],"2:18":[["geometry.rs",1193,1201]],"2:19":[["geometry.rs",1193,1201]],"2:2":[["geometry.rs",1135,1143]],"2:20":[["geometry.rs",1193,1201]],"2:21":[["geometry.rs",1193,1201]],"2:22":[["geometry.rs",1193,1201]],"2:23":[["geometry.rs",1193,1201]],"2:24":[["geometry.rs",1193,1201]],"2:25":[["geometry.rs",1193,1201]],"2:26":[["geometry.rs",1193,1201]],"2:27":[["geometry.rs",1193,1201]],"2:28":[["geometry.rs",1193,1201]],"2:29":[["geometry.rs",1193,1201]],"2:3":[["geometry.rs",1135,1143]],"2:30":[["geometry.rs",1193,1201]],"2:31":[["geometry.rs",1193,1201]],"2:32":[["geometry.rs",1193,1201]],"2:33":[["geometry.rs",1193,1201]],"2:34":[["geometry.rs",1193,1201]],"2:35":[["geometry.rs",1193,1201]],"2:36":[["geometry.rs",1193,1201]],"2:37":[["geometry.rs",1193,1201]],"2:38":[["geometry.rs",1193,1201]],"2:39":[["geometry.rs",1193,1201]],"2:4":[["geometry.rs",1135,1143]],"2:40":[["geometry.rs",1193,1201]],"2:41":[["geometry.rs",1193,1201]],"2:42":[["geometry.rs",1193,1201]],"2:43":[["geometry.rs",1193,1201]],"2:44":[["geometry.rs",1193,1201]],"2:45":[["geometry.rs",1193,1201]],"2:46":[["geometry.rs",1193,1201]],"2:5":[["geometry.rs",1135,1143]],"2:6":[["geometry.rs",1135,1143]],"2:7":[["geometry.rs",1135,1143]],"2:8":[["geometry.rs",1135,1143]],"2:9":[["geometry.rs",1135,1143]],"3:0":[["geometry.rs",1193,1201]],"3:1":[["geometry.rs",1193,1201]],"3:2":[["geometry.rs",1193,1201]],"3:3":[["geometry.rs",1193,1201]],"3:4":[["geometry.rs",1193,1201]],"4:0":[["geometry.rs",1176,1184]],"4:1":[["geometry.rs",1176,1184]],"4:2":[["geometry.rs",1176,1184]],"4:3":[["geometry.rs",1176,1184]],"4:4":[["geometry.rs",1176,1184]],"4:5":[["geometry.rs",1176,1184]],"4:6":[["geometry.rs",1176,1184]],"5:0":[["geometry.rs",1193,1201]],"5:1":[["geometry.rs",1193,1201]]}},{"k":"fn:Bounds::to_device_pixels","d":{"0:1":"Convert the bounds from logical pixels to physical pixels","0:2":"Convert the bounds from logical pixels to physical pixels","0:3":"Convert the bounds from logical pixels to physical pixels","0:4":"Convert the bounds from logical pixels to physical pixels","0:5":"Convert the bounds from logical pixels to physical pixels","0:6":"Convert the bounds from logical pixels to physical pixels","1:0":"Convert the bounds from logical pixels to physical pixels","1:1":"Convert the bounds from logical pixels to physical pixels","1:2":"Convert the bounds from logical pixels to physical pixels","2:0":"Convert the bounds from logical pixels to physical pixels","2:1":"Convert the bounds from logical pixels to physical pixels","2:10":"Convert the bounds from logical pixels to physical pixels","2:11":"Convert the bounds from logical pixels to physical pixels","2:12":"Convert the bounds from logical pixels to physical pixels","2:13":"Convert the bounds from logical pixels to physical pixels","2:14":"Convert the bounds from logical pixels to physical pixels","2:15":"Convert the bounds from logical pixels to physical pixels","2:16":"Convert the bounds from logical pixels to physical pixels","2:17":"Convert the bounds from logical pixels to physical pixels","2:18":"Convert the bounds from logical pixels to physical pixels","2:19":"Convert the bounds from logical pixels to physical pixels","2:2":"Convert the bounds from logical pixels to physical pixels","2:20":"Convert the bounds from logical pixels to physical pixels","2:21":"Convert the bounds from logical pixels to physical pixels","2:22":"Convert the bounds from logical pixels to physical pixels","2:23":"Convert the bounds from logical pixels to physical pixels","2:24":"Convert the bounds from logical pixels to physical pixels","2:25":"Convert the bounds from logical pixels to physical pixels","2:26":"Convert the bounds from logical pixels to physical pixels","2:27":"Convert the bounds from logical pixels to physical pixels","2:28":"Convert the bounds from logical pixels to physical pixels","2:29":"Convert the bounds from logical pixels to physical pixels","2:3":"Convert the bounds from logical pixels to physical pixels","2:30":"Convert the bounds from logical pixels to physical pixels","2:31":"Convert the bounds from logical pixels to physical pixels","2:32":"Convert the bounds from logical pixels to physical pixels","2:33":"Convert the bounds from logical pixels to physical pixels","2:34":"Convert the bounds from logical pixels to physical pixels","2:35":"Convert the bounds from logical pixels to physical pixels","2:36":"Convert the bounds from logical pixels to physical pixels","2:37":"Convert the bounds from logical pixels to physical pixels","2:38":"Convert the bounds from logical pixels to physical pixels","2:39":"Convert the bounds from logical pixels to physical pixels","2:4":"Convert the bounds from logical pixels to physical pixels","2:40":"Convert the bounds from logical pixels to physical pixels","2:41":"Convert the bounds from logical pixels to physical pixels","2:42":"Convert the bounds from logical pixels to physical pixels","2:43":"Convert the bounds from logical pixels to physical pixels","2:44":"Convert the bounds from logical pixels to physical pixels","2:45":"Convert the bounds from logical pixels to physical pixels","2:46":"Convert the bounds from logical pixels to physical pixels","2:5":"Convert the bounds from logical pixels to physical pixels","2:6":"Convert the bounds from logical pixels to physical pixels","2:7":"Convert the bounds from logical pixels to physical pixels","2:8":"Convert the bounds from logical pixels to physical pixels","2:9":"Convert the bounds from logical pixels to physical pixels","3:0":"Convert the bounds from logical pixels to physical pixels","3:1":"Convert the bounds from logical pixels to physical pixels","3:2":"Convert the bounds from logical pixels to physical pixels","3:3":"Convert the bounds from logical pixels to physical pixels","3:4":"Convert the bounds from logical pixels to physical pixels","4:0":"Convert the bounds from logical pixels to physical pixels","4:1":"Convert the bounds from logical pixels to physical pixels","4:2":"Convert the bounds from logical pixels to physical pixels","4:3":"Convert the bounds from logical pixels to physical pixels","4:4":"Convert the bounds from logical pixels to physical pixels","4:5":"Convert the bounds from logical pixels to physical pixels","4:6":"Convert the bounds from logical pixels to physical pixels","5:0":"Convert the bounds from logical pixels to physical pixels","5:1":"Convert the bounds from logical pixels to physical pixels"},"s":{"0:1":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","0:2":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","0:3":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","0:4":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","0:5":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","0:6":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","1:0":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","1:1":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","1:2":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:0":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:1":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:10":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:11":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:12":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:13":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:14":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:15":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:16":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:17":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:18":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:19":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:2":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:20":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:21":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:22":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:23":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:24":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:25":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:26":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:27":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:28":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:29":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:3":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:30":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:31":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:32":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:33":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:34":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:35":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:36":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:37":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:38":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:39":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:4":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:40":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:41":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:42":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:43":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:44":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:45":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:46":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:5":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:6":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:7":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:8":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","2:9":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","3:0":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","3:1":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","3:2":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","3:3":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","3:4":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","4:0":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","4:1":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","4:2":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","4:3":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","4:4":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","4:5":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","4:6":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","5:0":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >","5:1":"fn Bounds::to_device_pixels(self, f32)-> Bounds < DevicePixels >"},"l":{"0:1":[["geometry.rs",1654,1663]],"0:2":[["geometry.rs",1654,1663]],"0:3":[["geometry.rs",1654,1663]],"0:4":[["geometry.rs",1654,1663]],"0:5":[["geometry.rs",1654,1663]],"0:6":[["geometry.rs",1654,1663]],"1:0":[["geometry.rs",1654,1663]],"1:1":[["geometry.rs",1654,1663]],"1:2":[["geometry.rs",1717,1726]],"2:0":[["geometry.rs",1635,1644]],"2:1":[["geometry.rs",1635,1644]],"2:10":[["geometry.rs",1635,1644]],"2:11":[["geometry.rs",1635,1644]],"2:12":[["geometry.rs",1702,1711]],"2:13":[["geometry.rs",1702,1711]],"2:14":[["geometry.rs",1702,1711]],"2:15":[["geometry.rs",1702,1711]],"2:16":[["geometry.rs",1702,1711]],"2:17":[["geometry.rs",1702,1711]],"2:18":[["geometry.rs",1702,1711]],"2:19":[["geometry.rs",1702,1711]],"2:2":[["geometry.rs",1635,1644]],"2:20":[["geometry.rs",1702,1711]],"2:21":[["geometry.rs",1702,1711]],"2:22":[["geometry.rs",1702,1711]],"2:23":[["geometry.rs",1702,1711]],"2:24":[["geometry.rs",1702,1711]],"2:25":[["geometry.rs",1702,1711]],"2:26":[["geometry.rs",1702,1711]],"2:27":[["geometry.rs",1702,1711]],"2:28":[["geometry.rs",1702,1711]],"2:29":[["geometry.rs",1702,1711]],"2:3":[["geometry.rs",1635,1644]],"2:30":[["geometry.rs",1702,1711]],"2:31":[["geometry.rs",1702,1711]],"2:32":[["geometry.rs",1702,1711]],"2:33":[["geometry.rs",1702,1711]],"2:34":[["geometry.rs",1702,1711]],"2:35":[["geometry.rs",1702,1711]],"2:36":[["geometry.rs",1702,1711]],"2:37":[["geometry.rs",1702,1711]],"2:38":[["geometry.rs",1702,1711]],"2:39":[["geometry.rs",1702,1711]],"2:4":[["geometry.rs",1635,1644]],"2:40":[["geometry.rs",1702,1711]],"2:41":[["geometry.rs",1702,1711]],"2:42":[["geometry.rs",1702,1711]],"2:43":[["geometry.rs",1702,1711]],"2:44":[["geometry.rs",1702,1711]],"2:45":[["geometry.rs",1702,1711]],"2:46":[["geometry.rs",1702,1711]],"2:5":[["geometry.rs",1635,1644]],"2:6":[["geometry.rs",1635,1644]],"2:7":[["geometry.rs",1635,1644]],"2:8":[["geometry.rs",1635,1644]],"2:9":[["geometry.rs",1635,1644]],"3:0":[["geometry.rs",1702,1711]],"3:1":[["geometry.rs",1702,1711]],"3:2":[["geometry.rs",1702,1711]],"3:3":[["geometry.rs",1702,1711]],"3:4":[["geometry.rs",1702,1711]],"4:0":[["geometry.rs",1718,1727]],"4:1":[["geometry.rs",1718,1727]],"4:2":[["geometry.rs",1718,1727]],"4:3":[["geometry.rs",1718,1727]],"4:4":[["geometry.rs",1718,1727]],"4:5":[["geometry.rs",1718,1727]],"4:6":[["geometry.rs",1718,1727]],"5:0":[["geometry.rs",1702,1711]],"5:1":[["geometry.rs",1702,1711]]}},{"k":"fn:Bounds::to_pixels","d":{"0:1":"Convert the bounds from physical pixels to logical pixels","0:2":"Convert the bounds from physical pixels to logical pixels","0:3":"Convert the bounds from physical pixels to logical pixels","0:4":"Convert the bounds from physical pixels to logical pixels","0:5":"Convert the bounds from physical pixels to logical pixels","0:6":"Convert the bounds from physical pixels to logical pixels","1:0":"Convert the bounds from physical pixels to logical pixels","1:1":"Convert the bounds from physical pixels to logical pixels","1:2":"Convert the bounds from physical pixels to logical pixels","2:0":"Convert the bounds from physical pixels to logical pixels","2:1":"Convert the bounds from physical pixels to logical pixels","2:10":"Convert the bounds from physical pixels to logical pixels","2:11":"Convert the bounds from physical pixels to logical pixels","2:12":"Convert the bounds from physical pixels to logical pixels","2:13":"Convert the bounds from physical pixels to logical pixels","2:14":"Convert the bounds from physical pixels to logical pixels","2:15":"Convert the bounds from physical pixels to logical pixels","2:16":"Convert the bounds from physical pixels to logical pixels","2:17":"Convert the bounds from physical pixels to logical pixels","2:18":"Convert the bounds from physical pixels to logical pixels","2:19":"Convert the bounds from physical pixels to logical pixels","2:2":"Convert the bounds from physical pixels to logical pixels","2:20":"Convert the bounds from physical pixels to logical pixels","2:21":"Convert the bounds from physical pixels to logical pixels","2:22":"Convert the bounds from physical pixels to logical pixels","2:23":"Convert the bounds from physical pixels to logical pixels","2:24":"Convert the bounds from physical pixels to logical pixels","2:25":"Convert the bounds from physical pixels to logical pixels","2:26":"Convert the bounds from physical pixels to logical pixels","2:27":"Convert the bounds from physical pixels to logical pixels","2:28":"Convert the bounds from physical pixels to logical pixels","2:29":"Convert the bounds from physical pixels to logical pixels","2:3":"Convert the bounds from physical pixels to logical pixels","2:30":"Convert the bounds from physical pixels to logical pixels","2:31":"Convert the bounds from physical pixels to logical pixels","2:32":"Convert the bounds from physical pixels to logical pixels","2:33":"Convert the bounds from physical pixels to logical pixels","2:34":"Convert the bounds from physical pixels to logical pixels","2:35":"Convert the bounds from physical pixels to logical pixels","2:36":"Convert the bounds from physical pixels to logical pixels","2:37":"Convert the bounds from physical pixels to logical pixels","2:38":"Convert the bounds from physical pixels to logical pixels","2:39":"Convert the bounds from physical pixels to logical pixels","2:4":"Convert the bounds from physical pixels to logical pixels","2:40":"Convert the bounds from physical pixels to logical pixels","2:41":"Convert the bounds from physical pixels to logical pixels","2:42":"Convert the bounds from physical pixels to logical pixels","2:43":"Convert the bounds from physical pixels to logical pixels","2:44":"Convert the bounds from physical pixels to logical pixels","2:45":"Convert the bounds from physical pixels to logical pixels","2:46":"Convert the bounds from physical pixels to logical pixels","2:5":"Convert the bounds from physical pixels to logical pixels","2:6":"Convert the bounds from physical pixels to logical pixels","2:7":"Convert the bounds from physical pixels to logical pixels","2:8":"Convert the bounds from physical pixels to logical pixels","2:9":"Convert the bounds from physical pixels to logical pixels","3:0":"Convert the bounds from physical pixels to logical pixels","3:1":"Convert the bounds from physical pixels to logical pixels","3:2":"Convert the bounds from physical pixels to logical pixels","3:3":"Convert the bounds from physical pixels to logical pixels","3:4":"Convert the bounds from physical pixels to logical pixels","4:0":"Convert the bounds from physical pixels to logical pixels","4:1":"Convert the bounds from physical pixels to logical pixels","4:2":"Convert the bounds from physical pixels to logical pixels","4:3":"Convert the bounds from physical pixels to logical pixels","4:4":"Convert the bounds from physical pixels to logical pixels","4:5":"Convert the bounds from physical pixels to logical pixels","4:6":"Convert the bounds from physical pixels to logical pixels","5:0":"Convert the bounds from physical pixels to logical pixels","5:1":"Convert the bounds from physical pixels to logical pixels"},"s":{"0:1":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","0:2":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","0:3":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","0:4":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","0:5":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","0:6":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","1:0":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","1:1":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","1:2":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:0":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:1":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:10":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:11":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:12":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:13":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:14":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:15":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:16":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:17":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:18":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:19":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:2":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:20":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:21":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:22":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:23":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:24":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:25":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:26":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:27":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:28":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:29":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:3":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:30":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:31":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:32":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:33":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:34":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:35":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:36":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:37":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:38":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:39":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:4":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:40":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:41":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:42":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:43":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:44":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:45":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:46":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:5":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:6":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:7":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:8":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","2:9":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","3:0":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","3:1":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","3:2":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","3:3":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","3:4":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","4:0":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","4:1":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","4:2":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","4:3":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","4:4":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","4:5":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","4:6":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","5:0":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >","5:1":"fn Bounds::to_pixels(self, f32)-> Bounds < Pixels >"},"l":{"0:1":[["geometry.rs",1667,1676]],"0:2":[["geometry.rs",1667,1676]],"0:3":[["geometry.rs",1667,1676]],"0:4":[["geometry.rs",1667,1676]],"0:5":[["geometry.rs",1667,1676]],"0:6":[["geometry.rs",1667,1676]],"1:0":[["geometry.rs",1667,1676]],"1:1":[["geometry.rs",1667,1676]],"1:2":[["geometry.rs",1730,1739]],"2:0":[["geometry.rs",1648,1657]],"2:1":[["geometry.rs",1648,1657]],"2:10":[["geometry.rs",1648,1657]],"2:11":[["geometry.rs",1648,1657]],"2:12":[["geometry.rs",1715,1724]],"2:13":[["geometry.rs",1715,1724]],"2:14":[["geometry.rs",1715,1724]],"2:15":[["geometry.rs",1715,1724]],"2:16":[["geometry.rs",1715,1724]],"2:17":[["geometry.rs",1715,1724]],"2:18":[["geometry.rs",1715,1724]],"2:19":[["geometry.rs",1715,1724]],"2:2":[["geometry.rs",1648,1657]],"2:20":[["geometry.rs",1715,1724]],"2:21":[["geometry.rs",1715,1724]],"2:22":[["geometry.rs",1715,1724]],"2:23":[["geometry.rs",1715,1724]],"2:24":[["geometry.rs",1715,1724]],"2:25":[["geometry.rs",1715,1724]],"2:26":[["geometry.rs",1715,1724]],"2:27":[["geometry.rs",1715,1724]],"2:28":[["geometry.rs",1715,1724]],"2:29":[["geometry.rs",1715,1724]],"2:3":[["geometry.rs",1648,1657]],"2:30":[["geometry.rs",1715,1724]],"2:31":[["geometry.rs",1715,1724]],"2:32":[["geometry.rs",1715,1724]],"2:33":[["geometry.rs",1715,1724]],"2:34":[["geometry.rs",1715,1724]],"2:35":[["geometry.rs",1715,1724]],"2:36":[["geometry.rs",1715,1724]],"2:37":[["geometry.rs",1715,1724]],"2:38":[["geometry.rs",1715,1724]],"2:39":[["geometry.rs",1715,1724]],"2:4":[["geometry.rs",1648,1657]],"2:40":[["geometry.rs",1715,1724]],"2:41":[["geometry.rs",1715,1724]],"2:42":[["geometry.rs",1715,1724]],"2:43":[["geometry.rs",1715,1724]],"2:44":[["geometry.rs",1715,1724]],"2:45":[["geometry.rs",1715,1724]],"2:46":[["geometry.rs",1715,1724]],"2:5":[["geometry.rs",1648,1657]],"2:6":[["geometry.rs",1648,1657]],"2:7":[["geometry.rs",1648,1657]],"2:8":[["geometry.rs",1648,1657]],"2:9":[["geometry.rs",1648,1657]],"3:0":[["geometry.rs",1715,1724]],"3:1":[["geometry.rs",1715,1724]],"3:2":[["geometry.rs",1715,1724]],"3:3":[["geometry.rs",1715,1724]],"3:4":[["geometry.rs",1715,1724]],"4:0":[["geometry.rs",1731,1740]],"4:1":[["geometry.rs",1731,1740]],"4:2":[["geometry.rs",1731,1740]],"4:3":[["geometry.rs",1731,1740]],"4:4":[["geometry.rs",1731,1740]],"4:5":[["geometry.rs",1731,1740]],"4:6":[["geometry.rs",1731,1740]],"5:0":[["geometry.rs",1715,1724]],"5:1":[["geometry.rs",1715,1724]]}},{"k":"fn:Bounds::top","d":{"0:1":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","0:2":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","0:3":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","0:4":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","0:5":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","0:6":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","1:0":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","1:1":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","1:2":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:0":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:1":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:10":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:11":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:12":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:13":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:14":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:15":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:16":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:17":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:18":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:19":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:2":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:20":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:21":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:22":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:23":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:24":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:25":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:26":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:27":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:28":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:29":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:3":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:30":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:31":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:32":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:33":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:34":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:35":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:36":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:37":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:38":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:39":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:4":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:40":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:41":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:42":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:43":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:44":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:45":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:46":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:5":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:6":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:7":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:8":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","2:9":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","3:0":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","3:1":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","3:2":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","3:3":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","3:4":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","4:0":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","4:1":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","4:2":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","4:3":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","4:4":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","4:5":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","4:6":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","5:0":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds.","5:1":"Returns the top edge of the bounds.\n\n# Returns\n\nA value of type `T` representing the y-coordinate of the top edge of the bounds."},"s":{"0:1":"fn Bounds::top(& self)-> T","0:2":"fn Bounds::top(& self)-> T","0:3":"fn Bounds::top(& self)-> T","0:4":"fn Bounds::top(& self)-> T","0:5":"fn Bounds::top(& self)-> T","0:6":"fn Bounds::top(& self)-> T","1:0":"fn Bounds::top(& self)-> T","1:1":"fn Bounds::top(& self)-> T","1:2":"fn Bounds::top(& self)-> T","2:0":"fn Bounds::top(& self)-> T","2:1":"fn Bounds::top(& self)-> T","2:10":"fn Bounds::top(& self)-> T","2:11":"fn Bounds::top(& self)-> T","2:12":"fn Bounds::top(& self)-> T","2:13":"fn Bounds::top(& self)-> T","2:14":"fn Bounds::top(& self)-> T","2:15":"fn Bounds::top(& self)-> T","2:16":"fn Bounds::top(& self)-> T","2:17":"fn Bounds::top(& self)-> T","2:18":"fn Bounds::top(& self)-> T","2:19":"fn Bounds::top(& self)-> T","2:2":"fn Bounds::top(& self)-> T","2:20":"fn Bounds::top(& self)-> T","2:21":"fn Bounds::top(& self)-> T","2:22":"fn Bounds::top(& self)-> T","2:23":"fn Bounds::top(& self)-> T","2:24":"fn Bounds::top(& self)-> T","2:25":"fn Bounds::top(& self)-> T","2:26":"fn Bounds::top(& self)-> T","2:27":"fn Bounds::top(& self)-> T","2:28":"fn Bounds::top(& self)-> T","2:29":"fn Bounds::top(& self)-> T","2:3":"fn Bounds::top(& self)-> T","2:30":"fn Bounds::top(& self)-> T","2:31":"fn Bounds::top(& self)-> T","2:32":"fn Bounds::top(& self)-> T","2:33":"fn Bounds::top(& self)-> T","2:34":"fn Bounds::top(& self)-> T","2:35":"fn Bounds::top(& self)-> T","2:36":"fn Bounds::top(& self)-> T","2:37":"fn Bounds::top(& self)-> T","2:38":"fn Bounds::top(& self)-> T","2:39":"fn Bounds::top(& self)-> T","2:4":"fn Bounds::top(& self)-> T","2:40":"fn Bounds::top(& self)-> T","2:41":"fn Bounds::top(& self)-> T","2:42":"fn Bounds::top(& self)-> T","2:43":"fn Bounds::top(& self)-> T","2:44":"fn Bounds::top(& self)-> T","2:45":"fn Bounds::top(& self)-> T","2:46":"fn Bounds::top(& self)-> T","2:5":"fn Bounds::top(& self)-> T","2:6":"fn Bounds::top(& self)-> T","2:7":"fn Bounds::top(& self)-> T","2:8":"fn Bounds::top(& self)-> T","2:9":"fn Bounds::top(& self)-> T","3:0":"fn Bounds::top(& self)-> T","3:1":"fn Bounds::top(& self)-> T","3:2":"fn Bounds::top(& self)-> T","3:3":"fn Bounds::top(& self)-> T","3:4":"fn Bounds::top(& self)-> T","4:0":"fn Bounds::top(& self)-> T","4:1":"fn Bounds::top(& self)-> T","4:2":"fn Bounds::top(& self)-> T","4:3":"fn Bounds::top(& self)-> T","4:4":"fn Bounds::top(& self)-> T","4:5":"fn Bounds::top(& self)-> T","4:6":"fn Bounds::top(& self)-> T","5:0":"fn Bounds::top(& self)-> T","5:1":"fn Bounds::top(& self)-> T"},"l":{"0:1":[["geometry.rs",1249,1256]],"0:2":[["geometry.rs",1249,1256]],"0:3":[["geometry.rs",1249,1256]],"0:4":[["geometry.rs",1249,1256]],"0:5":[["geometry.rs",1249,1256]],"0:6":[["geometry.rs",1249,1256]],"1:0":[["geometry.rs",1249,1256]],"1:1":[["geometry.rs",1249,1256]],"1:2":[["geometry.rs",1303,1310]],"2:0":[["geometry.rs",1230,1237]],"2:1":[["geometry.rs",1230,1237]],"2:10":[["geometry.rs",1230,1237]],"2:11":[["geometry.rs",1230,1237]],"2:12":[["geometry.rs",1288,1295]],"2:13":[["geometry.rs",1288,1295]],"2:14":[["geometry.rs",1288,1295]],"2:15":[["geometry.rs",1288,1295]],"2:16":[["geometry.rs",1288,1295]],"2:17":[["geometry.rs",1288,1295]],"2:18":[["geometry.rs",1288,1295]],"2:19":[["geometry.rs",1288,1295]],"2:2":[["geometry.rs",1230,1237]],"2:20":[["geometry.rs",1288,1295]],"2:21":[["geometry.rs",1288,1295]],"2:22":[["geometry.rs",1288,1295]],"2:23":[["geometry.rs",1288,1295]],"2:24":[["geometry.rs",1288,1295]],"2:25":[["geometry.rs",1288,1295]],"2:26":[["geometry.rs",1288,1295]],"2:27":[["geometry.rs",1288,1295]],"2:28":[["geometry.rs",1288,1295]],"2:29":[["geometry.rs",1288,1295]],"2:3":[["geometry.rs",1230,1237]],"2:30":[["geometry.rs",1288,1295]],"2:31":[["geometry.rs",1288,1295]],"2:32":[["geometry.rs",1288,1295]],"2:33":[["geometry.rs",1288,1295]],"2:34":[["geometry.rs",1288,1295]],"2:35":[["geometry.rs",1288,1295]],"2:36":[["geometry.rs",1288,1295]],"2:37":[["geometry.rs",1288,1295]],"2:38":[["geometry.rs",1288,1295]],"2:39":[["geometry.rs",1288,1295]],"2:4":[["geometry.rs",1230,1237]],"2:40":[["geometry.rs",1288,1295]],"2:41":[["geometry.rs",1288,1295]],"2:42":[["geometry.rs",1288,1295]],"2:43":[["geometry.rs",1288,1295]],"2:44":[["geometry.rs",1288,1295]],"2:45":[["geometry.rs",1288,1295]],"2:46":[["geometry.rs",1288,1295]],"2:5":[["geometry.rs",1230,1237]],"2:6":[["geometry.rs",1230,1237]],"2:7":[["geometry.rs",1230,1237]],"2:8":[["geometry.rs",1230,1237]],"2:9":[["geometry.rs",1230,1237]],"3:0":[["geometry.rs",1288,1295]],"3:1":[["geometry.rs",1288,1295]],"3:2":[["geometry.rs",1288,1295]],"3:3":[["geometry.rs",1288,1295]],"3:4":[["geometry.rs",1288,1295]],"4:0":[["geometry.rs",1262,1269]],"4:1":[["geometry.rs",1262,1269]],"4:2":[["geometry.rs",1262,1269]],"4:3":[["geometry.rs",1262,1269]],"4:4":[["geometry.rs",1262,1269]],"4:5":[["geometry.rs",1262,1269]],"4:6":[["geometry.rs",1262,1269]],"5:0":[["geometry.rs",1288,1295]],"5:1":[["geometry.rs",1288,1295]]}},{"k":"fn:Bounds::top_center","d":{"1:2":"Returns the top center point of the bounds.","2:12":"Returns the top center point of the bounds.","2:13":"Returns the top center point of the bounds.","2:14":"Returns the top center point of the bounds.","2:15":"Returns the top center point of the bounds.","2:16":"Returns the top center point of the bounds.","2:17":"Returns the top center point of the bounds.","2:18":"Returns the top center point of the bounds.","2:19":"Returns the top center point of the bounds.","2:20":"Returns the top center point of the bounds.","2:21":"Returns the top center point of the bounds.","2:22":"Returns the top center point of the bounds.","2:23":"Returns the top center point of the bounds.","2:24":"Returns the top center point of the bounds.","2:25":"Returns the top center point of the bounds.","2:26":"Returns the top center point of the bounds.","2:27":"Returns the top center point of the bounds.","2:28":"Returns the top center point of the bounds.","2:29":"Returns the top center point of the bounds.","2:30":"Returns the top center point of the bounds.","2:31":"Returns the top center point of the bounds.","2:32":"Returns the top center point of the bounds.","2:33":"Returns the top center point of the bounds.","2:34":"Returns the top center point of the bounds.","2:35":"Returns the top center point of the bounds.","2:36":"Returns the top center point of the bounds.","2:37":"Returns the top center point of the bounds.","2:38":"Returns the top center point of the bounds.","2:39":"Returns the top center point of the bounds.","2:40":"Returns the top center point of the bounds.","2:41":"Returns the top center point of the bounds.","2:42":"Returns the top center point of the bounds.","2:43":"Returns the top center point of the bounds.","2:44":"Returns the top center point of the bounds.","2:45":"Returns the top center point of the bounds.","2:46":"Returns the top center point of the bounds.","3:0":"Returns the top center point of the bounds.","3:1":"Returns the top center point of the bounds.","3:2":"Returns the top center point of the bounds.","3:3":"Returns the top center point of the bounds.","3:4":"Returns the top center point of the bounds.","5:0":"Returns the top center point of the bounds.","5:1":"Returns the top center point of the bounds."},"s":{"1:2":"fn Bounds::top_center(& self)-> Point < T >","2:12":"fn Bounds::top_center(& self)-> Point < T >","2:13":"fn Bounds::top_center(& self)-> Point < T >","2:14":"fn Bounds::top_center(& self)-> Point < T >","2:15":"fn Bounds::top_center(& self)-> Point < T >","2:16":"fn Bounds::top_center(& self)-> Point < T >","2:17":"fn Bounds::top_center(& self)-> Point < T >","2:18":"fn Bounds::top_center(& self)-> Point < T >","2:19":"fn Bounds::top_center(& self)-> Point < T >","2:20":"fn Bounds::top_center(& self)-> Point < T >","2:21":"fn Bounds::top_center(& self)-> Point < T >","2:22":"fn Bounds::top_center(& self)-> Point < T >","2:23":"fn Bounds::top_center(& self)-> Point < T >","2:24":"fn Bounds::top_center(& self)-> Point < T >","2:25":"fn Bounds::top_center(& self)-> Point < T >","2:26":"fn Bounds::top_center(& self)-> Point < T >","2:27":"fn Bounds::top_center(& self)-> Point < T >","2:28":"fn Bounds::top_center(& self)-> Point < T >","2:29":"fn Bounds::top_center(& self)-> Point < T >","2:30":"fn Bounds::top_center(& self)-> Point < T >","2:31":"fn Bounds::top_center(& self)-> Point < T >","2:32":"fn Bounds::top_center(& self)-> Point < T >","2:33":"fn Bounds::top_center(& self)-> Point < T >","2:34":"fn Bounds::top_center(& self)-> Point < T >","2:35":"fn Bounds::top_center(& self)-> Point < T >","2:36":"fn Bounds::top_center(& self)-> Point < T >","2:37":"fn Bounds::top_center(& self)-> Point < T >","2:38":"fn Bounds::top_center(& self)-> Point < T >","2:39":"fn Bounds::top_center(& self)-> Point < T >","2:40":"fn Bounds::top_center(& self)-> Point < T >","2:41":"fn Bounds::top_center(& self)-> Point < T >","2:42":"fn Bounds::top_center(& self)-> Point < T >","2:43":"fn Bounds::top_center(& self)-> Point < T >","2:44":"fn Bounds::top_center(& self)-> Point < T >","2:45":"fn Bounds::top_center(& self)-> Point < T >","2:46":"fn Bounds::top_center(& self)-> Point < T >","3:0":"fn Bounds::top_center(& self)-> Point < T >","3:1":"fn Bounds::top_center(& self)-> Point < T >","3:2":"fn Bounds::top_center(& self)-> Point < T >","3:3":"fn Bounds::top_center(& self)-> Point < T >","3:4":"fn Bounds::top_center(& self)-> Point < T >","5:0":"fn Bounds::top_center(& self)-> Point < T >","5:1":"fn Bounds::top_center(& self)-> Point < T >"},"l":{"1:2":[["geometry.rs",907,913]],"2:12":[["geometry.rs",892,898]],"2:13":[["geometry.rs",892,898]],"2:14":[["geometry.rs",892,898]],"2:15":[["geometry.rs",892,898]],"2:16":[["geometry.rs",892,898]],"2:17":[["geometry.rs",892,898]],"2:18":[["geometry.rs",892,898]],"2:19":[["geometry.rs",892,898]],"2:20":[["geometry.rs",892,898]],"2:21":[["geometry.rs",892,898]],"2:22":[["geometry.rs",892,898]],"2:23":[["geometry.rs",892,898]],"2:24":[["geometry.rs",892,898]],"2:25":[["geometry.rs",892,898]],"2:26":[["geometry.rs",892,898]],"2:27":[["geometry.rs",892,898]],"2:28":[["geometry.rs",892,898]],"2:29":[["geometry.rs",892,898]],"2:30":[["geometry.rs",892,898]],"2:31":[["geometry.rs",892,898]],"2:32":[["geometry.rs",892,898]],"2:33":[["geometry.rs",892,898]],"2:34":[["geometry.rs",892,898]],"2:35":[["geometry.rs",892,898]],"2:36":[["geometry.rs",892,898]],"2:37":[["geometry.rs",892,898]],"2:38":[["geometry.rs",892,898]],"2:39":[["geometry.rs",892,898]],"2:40":[["geometry.rs",892,898]],"2:41":[["geometry.rs",892,898]],"2:42":[["geometry.rs",892,898]],"2:43":[["geometry.rs",892,898]],"2:44":[["geometry.rs",892,898]],"2:45":[["geometry.rs",892,898]],"2:46":[["geometry.rs",892,898]],"3:0":[["geometry.rs",892,898]],"3:1":[["geometry.rs",892,898]],"3:2":[["geometry.rs",892,898]],"3:3":[["geometry.rs",892,898]],"3:4":[["geometry.rs",892,898]],"5:0":[["geometry.rs",892,898]],"5:1":[["geometry.rs",892,898]]}},{"k":"fn:Bounds::top_right","d":{"0:1":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","0:2":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","0:3":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","0:4":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","0:5":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","0:6":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","1:0":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","1:1":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","1:2":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:0":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:1":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:10":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:11":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:12":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:13":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:14":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:15":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:16":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:17":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:18":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:19":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:2":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:20":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:21":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:22":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:23":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:24":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:25":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:26":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:27":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:28":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:29":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:3":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:30":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:31":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:32":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:33":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:34":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:35":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:36":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:37":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:38":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:39":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:4":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:40":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:41":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:42":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:43":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:44":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:45":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:46":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:5":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:6":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:7":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:8":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","2:9":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","3:0":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","3:1":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","3:2":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","3:3":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","3:4":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","4:0":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","4:1":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","4:2":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","4:3":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","4:4":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","4:5":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","4:6":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","5:0":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```","5:1":"Returns the top right corner point of the bounds.\n\n# Returns\n\nA `Point<T>` representing the top right corner of the bounds.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 20 },\n};\nlet top_right = bounds.top_right();\nassert_eq!(top_right, Point { x: 10, y: 0 });\n```"},"s":{"0:1":"fn Bounds::top_right(& self)-> Point < T >","0:2":"fn Bounds::top_right(& self)-> Point < T >","0:3":"fn Bounds::top_right(& self)-> Point < T >","0:4":"fn Bounds::top_right(& self)-> Point < T >","0:5":"fn Bounds::top_right(& self)-> Point < T >","0:6":"fn Bounds::top_right(& self)-> Point < T >","1:0":"fn Bounds::top_right(& self)-> Point < T >","1:1":"fn Bounds::top_right(& self)-> Point < T >","1:2":"fn Bounds::top_right(& self)-> Point < T >","2:0":"fn Bounds::top_right(& self)-> Point < T >","2:1":"fn Bounds::top_right(& self)-> Point < T >","2:10":"fn Bounds::top_right(& self)-> Point < T >","2:11":"fn Bounds::top_right(& self)-> Point < T >","2:12":"fn Bounds::top_right(& self)-> Point < T >","2:13":"fn Bounds::top_right(& self)-> Point < T >","2:14":"fn Bounds::top_right(& self)-> Point < T >","2:15":"fn Bounds::top_right(& self)-> Point < T >","2:16":"fn Bounds::top_right(& self)-> Point < T >","2:17":"fn Bounds::top_right(& self)-> Point < T >","2:18":"fn Bounds::top_right(& self)-> Point < T >","2:19":"fn Bounds::top_right(& self)-> Point < T >","2:2":"fn Bounds::top_right(& self)-> Point < T >","2:20":"fn Bounds::top_right(& self)-> Point < T >","2:21":"fn Bounds::top_right(& self)-> Point < T >","2:22":"fn Bounds::top_right(& self)-> Point < T >","2:23":"fn Bounds::top_right(& self)-> Point < T >","2:24":"fn Bounds::top_right(& self)-> Point < T >","2:25":"fn Bounds::top_right(& self)-> Point < T >","2:26":"fn Bounds::top_right(& self)-> Point < T >","2:27":"fn Bounds::top_right(& self)-> Point < T >","2:28":"fn Bounds::top_right(& self)-> Point < T >","2:29":"fn Bounds::top_right(& self)-> Point < T >","2:3":"fn Bounds::top_right(& self)-> Point < T >","2:30":"fn Bounds::top_right(& self)-> Point < T >","2:31":"fn Bounds::top_right(& self)-> Point < T >","2:32":"fn Bounds::top_right(& self)-> Point < T >","2:33":"fn Bounds::top_right(& self)-> Point < T >","2:34":"fn Bounds::top_right(& self)-> Point < T >","2:35":"fn Bounds::top_right(& self)-> Point < T >","2:36":"fn Bounds::top_right(& self)-> Point < T >","2:37":"fn Bounds::top_right(& self)-> Point < T >","2:38":"fn Bounds::top_right(& self)-> Point < T >","2:39":"fn Bounds::top_right(& self)-> Point < T >","2:4":"fn Bounds::top_right(& self)-> Point < T >","2:40":"fn Bounds::top_right(& self)-> Point < T >","2:41":"fn Bounds::top_right(& self)-> Point < T >","2:42":"fn Bounds::top_right(& self)-> Point < T >","2:43":"fn Bounds::top_right(& self)-> Point < T >","2:44":"fn Bounds::top_right(& self)-> Point < T >","2:45":"fn Bounds::top_right(& self)-> Point < T >","2:46":"fn Bounds::top_right(& self)-> Point < T >","2:5":"fn Bounds::top_right(& self)-> Point < T >","2:6":"fn Bounds::top_right(& self)-> Point < T >","2:7":"fn Bounds::top_right(& self)-> Point < T >","2:8":"fn Bounds::top_right(& self)-> Point < T >","2:9":"fn Bounds::top_right(& self)-> Point < T >","3:0":"fn Bounds::top_right(& self)-> Point < T >","3:1":"fn Bounds::top_right(& self)-> Point < T >","3:2":"fn Bounds::top_right(& self)-> Point < T >","3:3":"fn Bounds::top_right(& self)-> Point < T >","3:4":"fn Bounds::top_right(& self)-> Point < T >","4:0":"fn Bounds::top_right(& self)-> Point < T >","4:1":"fn Bounds::top_right(& self)-> Point < T >","4:2":"fn Bounds::top_right(& self)-> Point < T >","4:3":"fn Bounds::top_right(& self)-> Point < T >","4:4":"fn Bounds::top_right(& self)-> Point < T >","4:5":"fn Bounds::top_right(& self)-> Point < T >","4:6":"fn Bounds::top_right(& self)-> Point < T >","5:0":"fn Bounds::top_right(& self)-> Point < T >","5:1":"fn Bounds::top_right(& self)-> Point < T >"},"l":{"0:1":[["geometry.rs",1285,1307]],"0:2":[["geometry.rs",1285,1307]],"0:3":[["geometry.rs",1285,1307]],"0:4":[["geometry.rs",1285,1307]],"0:5":[["geometry.rs",1285,1307]],"0:6":[["geometry.rs",1285,1307]],"1:0":[["geometry.rs",1285,1307]],"1:1":[["geometry.rs",1285,1307]],"1:2":[["geometry.rs",1339,1361]],"2:0":[["geometry.rs",1266,1288]],"2:1":[["geometry.rs",1266,1288]],"2:10":[["geometry.rs",1266,1288]],"2:11":[["geometry.rs",1266,1288]],"2:12":[["geometry.rs",1324,1346]],"2:13":[["geometry.rs",1324,1346]],"2:14":[["geometry.rs",1324,1346]],"2:15":[["geometry.rs",1324,1346]],"2:16":[["geometry.rs",1324,1346]],"2:17":[["geometry.rs",1324,1346]],"2:18":[["geometry.rs",1324,1346]],"2:19":[["geometry.rs",1324,1346]],"2:2":[["geometry.rs",1266,1288]],"2:20":[["geometry.rs",1324,1346]],"2:21":[["geometry.rs",1324,1346]],"2:22":[["geometry.rs",1324,1346]],"2:23":[["geometry.rs",1324,1346]],"2:24":[["geometry.rs",1324,1346]],"2:25":[["geometry.rs",1324,1346]],"2:26":[["geometry.rs",1324,1346]],"2:27":[["geometry.rs",1324,1346]],"2:28":[["geometry.rs",1324,1346]],"2:29":[["geometry.rs",1324,1346]],"2:3":[["geometry.rs",1266,1288]],"2:30":[["geometry.rs",1324,1346]],"2:31":[["geometry.rs",1324,1346]],"2:32":[["geometry.rs",1324,1346]],"2:33":[["geometry.rs",1324,1346]],"2:34":[["geometry.rs",1324,1346]],"2:35":[["geometry.rs",1324,1346]],"2:36":[["geometry.rs",1324,1346]],"2:37":[["geometry.rs",1324,1346]],"2:38":[["geometry.rs",1324,1346]],"2:39":[["geometry.rs",1324,1346]],"2:4":[["geometry.rs",1266,1288]],"2:40":[["geometry.rs",1324,1346]],"2:41":[["geometry.rs",1324,1346]],"2:42":[["geometry.rs",1324,1346]],"2:43":[["geometry.rs",1324,1346]],"2:44":[["geometry.rs",1324,1346]],"2:45":[["geometry.rs",1324,1346]],"2:46":[["geometry.rs",1324,1346]],"2:5":[["geometry.rs",1266,1288]],"2:6":[["geometry.rs",1266,1288]],"2:7":[["geometry.rs",1266,1288]],"2:8":[["geometry.rs",1266,1288]],"2:9":[["geometry.rs",1266,1288]],"3:0":[["geometry.rs",1324,1346]],"3:1":[["geometry.rs",1324,1346]],"3:2":[["geometry.rs",1324,1346]],"3:3":[["geometry.rs",1324,1346]],"3:4":[["geometry.rs",1324,1346]],"4:0":[["geometry.rs",1298,1320]],"4:1":[["geometry.rs",1298,1320]],"4:2":[["geometry.rs",1298,1320]],"4:3":[["geometry.rs",1298,1320]],"4:4":[["geometry.rs",1298,1320]],"4:5":[["geometry.rs",1298,1320]],"4:6":[["geometry.rs",1298,1320]],"5:0":[["geometry.rs",1324,1346]],"5:1":[["geometry.rs",1324,1346]]}},{"k":"fn:Bounds::union","d":{"0:1":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","0:2":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","0:3":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","0:4":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","0:5":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","0:6":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","1:0":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","1:1":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","1:2":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:0":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:1":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:10":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:11":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:12":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:13":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:14":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:15":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:16":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:17":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:18":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:19":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:2":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:20":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:21":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:22":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:23":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:24":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:25":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:26":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:27":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:28":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:29":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:3":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:30":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:31":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:32":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:33":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:34":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:35":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:36":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:37":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:38":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:39":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:4":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:40":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:41":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:42":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:43":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:44":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:45":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:46":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:5":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:6":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:7":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:8":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","2:9":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","3:0":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","3:1":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","3:2":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","3:3":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","3:4":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","4:0":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","4:1":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","4:2":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","4:3":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","4:4":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","4:5":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","4:6":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use kael::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","5:0":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```","5:1":"Computes the union of two `Bounds`.\n\nThis method calculates the smallest `Bounds` that contains both the current `Bounds` and the `other` `Bounds`.\nThe resulting `Bounds` will have an origin that is the minimum of the origins of the two `Bounds`,\nand a size that encompasses the furthest extents of both `Bounds`.\n\n# Arguments\n\n* `other` - A reference to another `Bounds` to create a union with.\n\n# Returns\n\nReturns a `Bounds` representing the union of the two `Bounds`.\n\n# Examples\n\n```\n# use gpui::{Bounds, Point, Size};\nlet bounds1 = Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 10, height: 10 },\n};\nlet bounds2 = Bounds {\n    origin: Point { x: 5, y: 5 },\n    size: Size { width: 15, height: 15 },\n};\nlet union_bounds = bounds1.union(&bounds2);\n\nassert_eq!(union_bounds, Bounds {\n    origin: Point { x: 0, y: 0 },\n    size: Size { width: 20, height: 20 },\n});\n```"},"s":{"0:1":"fn Bounds::union(& self, & Self)-> Self","0:2":"fn Bounds::union(& self, & Self)-> Self","0:3":"fn Bounds::union(& self, & Self)-> Self","0:4":"fn Bounds::union(& self, & Self)-> Self","0:5":"fn Bounds::union(& self, & Self)-> Self","0:6":"fn Bounds::union(& self, & Self)-> Self","1:0":"fn Bounds::union(& self, & Self)-> Self","1:1":"fn Bounds::union(& self, & Self)-> Self","1:2":"fn Bounds::union(& self, & Self)-> Self","2:0":"fn Bounds::union(& self, & Self)-> Self","2:1":"fn Bounds::union(& self, & Self)-> Self","2:10":"fn Bounds::union(& self, & Self)-> Self","2:11":"fn Bounds::union(& self, & Self)-> Self","2:12":"fn Bounds::union(& self, & Self)-> Self","2:13":"fn Bounds::union(& self, & Self)-> Self","2:14":"fn Bounds::union(& self, & Self)-> Self","2:15":"fn Bounds::union(& self, & Self)-> Self","2:16":"fn Bounds::union(& self, & Self)-> Self","2:17":"fn Bounds::union(& self, & Self)-> Self","2:18":"fn Bounds::union(& self, & Self)-> Self","2:19":"fn Bounds::union(& self, & Self)-> Self","2:2":"fn Bounds::union(& self, & Self)-> Self","2:20":"fn Bounds::union(& self, & Self)-> Self","2:21":"fn Bounds::union(& self, & Self)-> Self","2:22":"fn Bounds::union(& self, & Self)-> Self","2:23":"fn Bounds::union(& self, & Self)-> Self","2:24":"fn Bounds::union(& self, & Self)-> Self","2:25":"fn Bounds::union(& self, & Self)-> Self","2:26":"fn Bounds::union(& self, & Self)-> Self","2:27":"fn Bounds::union(& self, & Self)-> Self","2:28":"fn Bounds::union(& self, & Self)-> Self","2:29":"fn Bounds::union(& self, & Self)-> Self","2:3":"fn Bounds::union(& self, & Self)-> Self","2:30":"fn Bounds::union(& self, & Self)-> Self","2:31":"fn Bounds::union(& self, & Self)-> Self","2:32":"fn Bounds::union(& self, & Self)-> Self","2:33":"fn Bounds::union(& self, & Self)-> Self","2:34":"fn Bounds::union(& self, & Self)-> Self","2:35":"fn Bounds::union(& self, & Self)-> Self","2:36":"fn Bounds::union(& self, & Self)-> Self","2:37":"fn Bounds::union(& self, & Self)-> Self","2:38":"fn Bounds::union(& self, & Self)-> Self","2:39":"fn Bounds::union(& self, & Self)-> Self","2:4":"fn Bounds::union(& self, & Self)-> Self","2:40":"fn Bounds::union(& self, & Self)-> Self","2:41":"fn Bounds::union(& self, & Self)-> Self","2:42":"fn Bounds::union(& self, & Self)-> Self","2:43":"fn Bounds::union(& self, & Self)-> Self","2:44":"fn Bounds::union(& self, & Self)-> Self","2:45":"fn Bounds::union(& self, & Self)-> Self","2:46":"fn Bounds::union(& self, & Self)-> Self","2:5":"fn Bounds::union(& self, & Self)-> Self","2:6":"fn Bounds::union(& self, & Self)-> Self","2:7":"fn Bounds::union(& self, & Self)-> Self","2:8":"fn Bounds::union(& self, & Self)-> Self","2:9":"fn Bounds::union(& self, & Self)-> Self","3:0":"fn Bounds::union(& self, & Self)-> Self","3:1":"fn Bounds::union(& self, & Self)-> Self","3:2":"fn Bounds::union(& self, & Self)-> Self","3:3":"fn Bounds::union(& self, & Self)-> Self","3:4":"fn Bounds::union(& self, & Self)-> Self","4:0":"fn Bounds::union(& self, & Self)-> Self","4:1":"fn Bounds::union(& self, & Self)-> Self","4:2":"fn Bounds::union(& self, & Self)-> Self","4:3":"fn Bounds::union(& self, & Self)-> Self","4:4":"fn Bounds::union(& self, & Self)-> Self","4:5":"fn Bounds::union(& self, & Self)-> Self","4:6":"fn Bounds::union(& self, & Self)-> Self","5:0":"fn Bounds::union(& self, & Self)-> Self","5:1":"fn Bounds::union(& self, & Self)-> Self"},"l":{"0:1":[["geometry.rs",1119,1156]],"0:2":[["geometry.rs",1119,1156]],"0:3":[["geometry.rs",1119,1156]],"0:4":[["geometry.rs",1119,1156]],"0:5":[["geometry.rs",1119,1156]],"0:6":[["geometry.rs",1119,1156]],"1:0":[["geometry.rs",1119,1156]],"1:1":[["geometry.rs",1119,1156]],"1:2":[["geometry.rs",1164,1201]],"2:0":[["geometry.rs",1091,1128]],"2:1":[["geometry.rs",1091,1128]],"2:10":[["geometry.rs",1091,1128]],"2:11":[["geometry.rs",1091,1128]],"2:12":[["geometry.rs",1149,1186]],"2:13":[["geometry.rs",1149,1186]],"2:14":[["geometry.rs",1149,1186]],"2:15":[["geometry.rs",1149,1186]],"2:16":[["geometry.rs",1149,1186]],"2:17":[["geometry.rs",1149,1186]],"2:18":[["geometry.rs",1149,1186]],"2:19":[["geometry.rs",1149,1186]],"2:2":[["geometry.rs",1091,1128]],"2:20":[["geometry.rs",1149,1186]],"2:21":[["geometry.rs",1149,1186]],"2:22":[["geometry.rs",1149,1186]],"2:23":[["geometry.rs",1149,1186]],"2:24":[["geometry.rs",1149,1186]],"2:25":[["geometry.rs",1149,1186]],"2:26":[["geometry.rs",1149,1186]],"2:27":[["geometry.rs",1149,1186]],"2:28":[["geometry.rs",1149,1186]],"2:29":[["geometry.rs",1149,1186]],"2:3":[["geometry.rs",1091,1128]],"2:30":[["geometry.rs",1149,1186]],"2:31":[["geometry.rs",1149,1186]],"2:32":[["geometry.rs",1149,1186]],"2:33":[["geometry.rs",1149,1186]],"2:34":[["geometry.rs",1149,1186]],"2:35":[["geometry.rs",1149,1186]],"2:36":[["geometry.rs",1149,1186]],"2:37":[["geometry.rs",1149,1186]],"2:38":[["geometry.rs",1149,1186]],"2:39":[["geometry.rs",1149,1186]],"2:4":[["geometry.rs",1091,1128]],"2:40":[["geometry.rs",1149,1186]],"2:41":[["geometry.rs",1149,1186]],"2:42":[["geometry.rs",1149,1186]],"2:43":[["geometry.rs",1149,1186]],"2:44":[["geometry.rs",1149,1186]],"2:45":[["geometry.rs",1149,1186]],"2:46":[["geometry.rs",1149,1186]],"2:5":[["geometry.rs",1091,1128]],"2:6":[["geometry.rs",1091,1128]],"2:7":[["geometry.rs",1091,1128]],"2:8":[["geometry.rs",1091,1128]],"2:9":[["geometry.rs",1091,1128]],"3:0":[["geometry.rs",1149,1186]],"3:1":[["geometry.rs",1149,1186]],"3:2":[["geometry.rs",1149,1186]],"3:3":[["geometry.rs",1149,1186]],"3:4":[["geometry.rs",1149,1186]],"4:0":[["geometry.rs",1132,1169]],"4:1":[["geometry.rs",1132,1169]],"4:2":[["geometry.rs",1132,1169]],"4:3":[["geometry.rs",1132,1169]],"4:4":[["geometry.rs",1132,1169]],"4:5":[["geometry.rs",1132,1169]],"4:6":[["geometry.rs",1132,1169]],"5:0":[["geometry.rs",1149,1186]],"5:1":[["geometry.rs",1149,1186]]}},{"k":"fn:BoxShadow::blur_radius","d":{"1:2":"Sets the shadow blur radius.","2:22":"Sets the shadow blur radius.","2:23":"Sets the shadow blur radius.","2:24":"Sets the shadow blur radius.","2:25":"Sets the shadow blur radius.","2:26":"Sets the shadow blur radius.","2:27":"Sets the shadow blur radius.","2:28":"Sets the shadow blur radius.","2:29":"Sets the shadow blur radius.","2:30":"Sets the shadow blur radius.","2:31":"Sets the shadow blur radius.","2:32":"Sets the shadow blur radius.","2:33":"Sets the shadow blur radius.","2:34":"Sets the shadow blur radius.","2:35":"Sets the shadow blur radius.","2:36":"Sets the shadow blur radius.","2:37":"Sets the shadow blur radius.","2:38":"Sets the shadow blur radius.","2:39":"Sets the shadow blur radius.","2:40":"Sets the shadow blur radius.","2:41":"Sets the shadow blur radius.","2:42":"Sets the shadow blur radius.","2:43":"Sets the shadow blur radius.","2:44":"Sets the shadow blur radius.","2:45":"Sets the shadow blur radius.","2:46":"Sets the shadow blur radius.","3:0":"Sets the shadow blur radius.","3:1":"Sets the shadow blur radius.","3:2":"Sets the shadow blur radius.","3:3":"Sets the shadow blur radius.","3:4":"Sets the shadow blur radius.","5:0":"Sets the shadow blur radius.","5:1":"Sets the shadow blur radius."},"s":{"1:2":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","2:22":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","2:23":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","2:24":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","2:25":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","2:26":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","2:27":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","2:28":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","2:29":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","2:30":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","2:31":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","2:32":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","2:33":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","2:34":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","2:35":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","2:36":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","2:37":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","2:38":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","2:39":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","2:40":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","2:41":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","2:42":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","2:43":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","2:44":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","2:45":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","2:46":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","3:0":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","3:1":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","3:2":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","3:3":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","3:4":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","5:0":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self","5:1":"fn BoxShadow::blur_radius(mut self, Pixels)-> Self"},"l":{"1:2":[["style.rs",378,382]],"2:22":[["style.rs",372,376]],"2:23":[["style.rs",372,376]],"2:24":[["style.rs",372,376]],"2:25":[["style.rs",372,376]],"2:26":[["style.rs",372,376]],"2:27":[["style.rs",372,376]],"2:28":[["style.rs",372,376]],"2:29":[["style.rs",372,376]],"2:30":[["style.rs",372,376]],"2:31":[["style.rs",372,376]],"2:32":[["style.rs",372,376]],"2:33":[["style.rs",372,376]],"2:34":[["style.rs",376,380]],"2:35":[["style.rs",376,380]],"2:36":[["style.rs",376,380]],"2:37":[["style.rs",376,380]],"2:38":[["style.rs",376,380]],"2:39":[["style.rs",376,380]],"2:40":[["style.rs",376,380]],"2:41":[["style.rs",376,380]],"2:42":[["style.rs",376,380]],"2:43":[["style.rs",376,380]],"2:44":[["style.rs",376,380]],"2:45":[["style.rs",376,380]],"2:46":[["style.rs",376,380]],"3:0":[["style.rs",376,380]],"3:1":[["style.rs",376,380]],"3:2":[["style.rs",376,380]],"3:3":[["style.rs",376,380]],"3:4":[["style.rs",376,380]],"5:0":[["style.rs",376,380]],"5:1":[["style.rs",376,380]]}},{"k":"fn:BoxShadow::inset","d":{"1:2":"Marks the shadow as inset (drawn inside the element's bounds).","2:22":"Marks the shadow as inset (drawn inside the element's bounds).","2:23":"Marks the shadow as inset (drawn inside the element's bounds).","2:24":"Marks the shadow as inset (drawn inside the element's bounds).","2:25":"Marks the shadow as inset (drawn inside the element's bounds).","2:26":"Marks the shadow as inset (drawn inside the element's bounds).","2:27":"Marks the shadow as inset (drawn inside the element's bounds).","2:28":"Marks the shadow as inset (drawn inside the element's bounds).","2:29":"Marks the shadow as inset (drawn inside the element's bounds).","2:30":"Marks the shadow as inset (drawn inside the element's bounds).","2:31":"Marks the shadow as inset (drawn inside the element's bounds).","2:32":"Marks the shadow as inset (drawn inside the element's bounds).","2:33":"Marks the shadow as inset (drawn inside the element's bounds).","2:34":"Marks the shadow as inset (drawn inside the element's bounds).","2:35":"Marks the shadow as inset (drawn inside the element's bounds).","2:36":"Marks the shadow as inset (drawn inside the element's bounds).","2:37":"Marks the shadow as inset (drawn inside the element's bounds).","2:38":"Marks the shadow as inset (drawn inside the element's bounds).","2:39":"Marks the shadow as inset (drawn inside the element's bounds).","2:40":"Marks the shadow as inset (drawn inside the element's bounds).","2:41":"Marks the shadow as inset (drawn inside the element's bounds).","2:42":"Marks the shadow as inset (drawn inside the element's bounds).","2:43":"Marks the shadow as inset (drawn inside the element's bounds).","2:44":"Marks the shadow as inset (drawn inside the element's bounds).","2:45":"Marks the shadow as inset (drawn inside the element's bounds).","2:46":"Marks the shadow as inset (drawn inside the element's bounds).","3:0":"Marks the shadow as inset (drawn inside the element's bounds).","3:1":"Marks the shadow as inset (drawn inside the element's bounds).","3:2":"Marks the shadow as inset (drawn inside the element's bounds).","3:3":"Marks the shadow as inset (drawn inside the element's bounds).","3:4":"Marks the shadow as inset (drawn inside the element's bounds).","5:0":"Marks the shadow as inset (drawn inside the element's bounds).","5:1":"Marks the shadow as inset (drawn inside the element's bounds)."},"s":{"1:2":"fn BoxShadow::inset(mut self)-> Self","2:22":"fn BoxShadow::inset(mut self)-> Self","2:23":"fn BoxShadow::inset(mut self)-> Self","2:24":"fn BoxShadow::inset(mut self)-> Self","2:25":"fn BoxShadow::inset(mut self)-> Self","2:26":"fn BoxShadow::inset(mut self)-> Self","2:27":"fn BoxShadow::inset(mut self)-> Self","2:28":"fn BoxShadow::inset(mut self)-> Self","2:29":"fn BoxShadow::inset(mut self)-> Self","2:30":"fn BoxShadow::inset(mut self)-> Self","2:31":"fn BoxShadow::inset(mut self)-> Self","2:32":"fn BoxShadow::inset(mut self)-> Self","2:33":"fn BoxShadow::inset(mut self)-> Self","2:34":"fn BoxShadow::inset(mut self)-> Self","2:35":"fn BoxShadow::inset(mut self)-> Self","2:36":"fn BoxShadow::inset(mut self)-> Self","2:37":"fn BoxShadow::inset(mut self)-> Self","2:38":"fn BoxShadow::inset(mut self)-> Self","2:39":"fn BoxShadow::inset(mut self)-> Self","2:40":"fn BoxShadow::inset(mut self)-> Self","2:41":"fn BoxShadow::inset(mut self)-> Self","2:42":"fn BoxShadow::inset(mut self)-> Self","2:43":"fn BoxShadow::inset(mut self)-> Self","2:44":"fn BoxShadow::inset(mut self)-> Self","2:45":"fn BoxShadow::inset(mut self)-> Self","2:46":"fn BoxShadow::inset(mut self)-> Self","3:0":"fn BoxShadow::inset(mut self)-> Self","3:1":"fn BoxShadow::inset(mut self)-> Self","3:2":"fn BoxShadow::inset(mut self)-> Self","3:3":"fn BoxShadow::inset(mut self)-> Self","3:4":"fn BoxShadow::inset(mut self)-> Self","5:0":"fn BoxShadow::inset(mut self)-> Self","5:1":"fn BoxShadow::inset(mut self)-> Self"},"l":{"1:2":[["style.rs",390,394]],"2:22":[["style.rs",384,388]],"2:23":[["style.rs",384,388]],"2:24":[["style.rs",384,388]],"2:25":[["style.rs",384,388]],"2:26":[["style.rs",384,388]],"2:27":[["style.rs",384,388]],"2:28":[["style.rs",384,388]],"2:29":[["style.rs",384,388]],"2:30":[["style.rs",384,388]],"2:31":[["style.rs",384,388]],"2:32":[["style.rs",384,388]],"2:33":[["style.rs",384,388]],"2:34":[["style.rs",388,392]],"2:35":[["style.rs",388,392]],"2:36":[["style.rs",388,392]],"2:37":[["style.rs",388,392]],"2:38":[["style.rs",388,392]],"2:39":[["style.rs",388,392]],"2:40":[["style.rs",388,392]],"2:41":[["style.rs",388,392]],"2:42":[["style.rs",388,392]],"2:43":[["style.rs",388,392]],"2:44":[["style.rs",388,392]],"2:45":[["style.rs",388,392]],"2:46":[["style.rs",388,392]],"3:0":[["style.rs",388,392]],"3:1":[["style.rs",388,392]],"3:2":[["style.rs",388,392]],"3:3":[["style.rs",388,392]],"3:4":[["style.rs",388,392]],"5:0":[["style.rs",388,392]],"5:1":[["style.rs",388,392]]}},{"k":"fn:BoxShadow::new","d":{"1:2":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","2:22":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","2:23":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","2:24":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","2:25":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","2:26":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","2:27":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","2:28":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","2:29":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","2:30":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","2:31":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","2:32":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","2:33":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","2:34":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","2:35":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","2:36":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","2:37":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","2:38":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","2:39":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","2:40":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","2:41":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","2:42":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","2:43":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","2:44":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","2:45":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","2:46":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","3:0":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","3:1":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","3:2":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","3:3":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","3:4":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","5:0":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset.","5:1":"Creates a new [`BoxShadow`] with the given offset and color, matching the order\nof the CSS `box-shadow` property. Use the builder methods to set blur radius,\nspread radius, and inset."},"s":{"1:2":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","2:22":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","2:23":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","2:24":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","2:25":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","2:26":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","2:27":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","2:28":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","2:29":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","2:30":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","2:31":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","2:32":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","2:33":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","2:34":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","2:35":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","2:36":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","2:37":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","2:38":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","2:39":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","2:40":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","2:41":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","2:42":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","2:43":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","2:44":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","2:45":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","2:46":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","3:0":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","3:1":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","3:2":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","3:3":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","3:4":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","5:0":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self","5:1":"fn BoxShadow::new(Pixels, Pixels, Hsla)-> Self"},"l":{"1:2":[["style.rs",365,376]],"2:22":[["style.rs",359,370]],"2:23":[["style.rs",359,370]],"2:24":[["style.rs",359,370]],"2:25":[["style.rs",359,370]],"2:26":[["style.rs",359,370]],"2:27":[["style.rs",359,370]],"2:28":[["style.rs",359,370]],"2:29":[["style.rs",359,370]],"2:30":[["style.rs",359,370]],"2:31":[["style.rs",359,370]],"2:32":[["style.rs",359,370]],"2:33":[["style.rs",359,370]],"2:34":[["style.rs",363,374]],"2:35":[["style.rs",363,374]],"2:36":[["style.rs",363,374]],"2:37":[["style.rs",363,374]],"2:38":[["style.rs",363,374]],"2:39":[["style.rs",363,374]],"2:40":[["style.rs",363,374]],"2:41":[["style.rs",363,374]],"2:42":[["style.rs",363,374]],"2:43":[["style.rs",363,374]],"2:44":[["style.rs",363,374]],"2:45":[["style.rs",363,374]],"2:46":[["style.rs",363,374]],"3:0":[["style.rs",363,374]],"3:1":[["style.rs",363,374]],"3:2":[["style.rs",363,374]],"3:3":[["style.rs",363,374]],"3:4":[["style.rs",363,374]],"5:0":[["style.rs",363,374]],"5:1":[["style.rs",363,374]]}},{"k":"fn:BoxShadow::spread_radius","d":{"1:2":"Sets the shadow spread radius.","2:22":"Sets the shadow spread radius.","2:23":"Sets the shadow spread radius.","2:24":"Sets the shadow spread radius.","2:25":"Sets the shadow spread radius.","2:26":"Sets the shadow spread radius.","2:27":"Sets the shadow spread radius.","2:28":"Sets the shadow spread radius.","2:29":"Sets the shadow spread radius.","2:30":"Sets the shadow spread radius.","2:31":"Sets the shadow spread radius.","2:32":"Sets the shadow spread radius.","2:33":"Sets the shadow spread radius.","2:34":"Sets the shadow spread radius.","2:35":"Sets the shadow spread radius.","2:36":"Sets the shadow spread radius.","2:37":"Sets the shadow spread radius.","2:38":"Sets the shadow spread radius.","2:39":"Sets the shadow spread radius.","2:40":"Sets the shadow spread radius.","2:41":"Sets the shadow spread radius.","2:42":"Sets the shadow spread radius.","2:43":"Sets the shadow spread radius.","2:44":"Sets the shadow spread radius.","2:45":"Sets the shadow spread radius.","2:46":"Sets the shadow spread radius.","3:0":"Sets the shadow spread radius.","3:1":"Sets the shadow spread radius.","3:2":"Sets the shadow spread radius.","3:3":"Sets the shadow spread radius.","3:4":"Sets the shadow spread radius.","5:0":"Sets the shadow spread radius.","5:1":"Sets the shadow spread radius."},"s":{"1:2":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","2:22":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","2:23":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","2:24":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","2:25":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","2:26":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","2:27":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","2:28":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","2:29":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","2:30":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","2:31":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","2:32":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","2:33":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","2:34":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","2:35":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","2:36":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","2:37":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","2:38":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","2:39":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","2:40":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","2:41":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","2:42":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","2:43":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","2:44":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","2:45":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","2:46":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","3:0":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","3:1":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","3:2":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","3:3":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","3:4":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","5:0":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self","5:1":"fn BoxShadow::spread_radius(mut self, Pixels)-> Self"},"l":{"1:2":[["style.rs",384,388]],"2:22":[["style.rs",378,382]],"2:23":[["style.rs",378,382]],"2:24":[["style.rs",378,382]],"2:25":[["style.rs",378,382]],"2:26":[["style.rs",378,382]],"2:27":[["style.rs",378,382]],"2:28":[["style.rs",378,382]],"2:29":[["style.rs",378,382]],"2:30":[["style.rs",378,382]],"2:31":[["style.rs",378,382]],"2:32":[["style.rs",378,382]],"2:33":[["style.rs",378,382]],"2:34":[["style.rs",382,386]],"2:35":[["style.rs",382,386]],"2:36":[["style.rs",382,386]],"2:37":[["style.rs",382,386]],"2:38":[["style.rs",382,386]],"2:39":[["style.rs",382,386]],"2:40":[["style.rs",382,386]],"2:41":[["style.rs",382,386]],"2:42":[["style.rs",382,386]],"2:43":[["style.rs",382,386]],"2:44":[["style.rs",382,386]],"2:45":[["style.rs",382,386]],"2:46":[["style.rs",382,386]],"3:0":[["style.rs",382,386]],"3:1":[["style.rs",382,386]],"3:2":[["style.rs",382,386]],"3:3":[["style.rs",382,386]],"3:4":[["style.rs",382,386]],"5:0":[["style.rs",382,386]],"5:1":[["style.rs",382,386]]}},{"k":"fn:BrowserProfileStorageBridgeItem::destination","d":{"4:3":"Replacement destination for this storage class.","4:4":"Replacement destination for this storage class.","4:5":"Replacement destination for this storage class.","4:6":"Replacement destination for this storage class."},"s":{"4:3":"fn BrowserProfileStorageBridgeItem::destination(& self)-> BrowserProfileStorageDestination","4:4":"fn BrowserProfileStorageBridgeItem::destination(& self)-> BrowserProfileStorageDestination","4:5":"fn BrowserProfileStorageBridgeItem::destination(& self)-> BrowserProfileStorageDestination","4:6":"fn BrowserProfileStorageBridgeItem::destination(& self)-> BrowserProfileStorageDestination"},"l":{"4:3":[["app.rs",4404,4407]],"4:4":[["app.rs",4404,4407]],"4:5":[["app.rs",4405,4408]],"4:6":[["app.rs",4405,4408]]}},{"k":"fn:BrowserProfileStorageBridgeItem::id","d":{"4:3":"Stable app-defined item id.","4:4":"Stable app-defined item id.","4:5":"Stable app-defined item id.","4:6":"Stable app-defined item id."},"s":{"4:3":"fn BrowserProfileStorageBridgeItem::id(& self)-> & str","4:4":"fn BrowserProfileStorageBridgeItem::id(& self)-> & str","4:5":"fn BrowserProfileStorageBridgeItem::id(& self)-> & str","4:6":"fn BrowserProfileStorageBridgeItem::id(& self)-> & str"},"l":{"4:3":[["app.rs",4394,4397]],"4:4":[["app.rs",4394,4397]],"4:5":[["app.rs",4395,4398]],"4:6":[["app.rs",4395,4398]]}},{"k":"fn:BrowserProfileStorageBridgeItem::is_sensitive","d":{"4:3":"Whether the item may contain secrets or user-sensitive data.","4:4":"Whether the item may contain secrets or user-sensitive data.","4:5":"Whether the item may contain secrets or user-sensitive data.","4:6":"Whether the item may contain secrets or user-sensitive data."},"s":{"4:3":"fn BrowserProfileStorageBridgeItem::is_sensitive(& self)-> bool","4:4":"fn BrowserProfileStorageBridgeItem::is_sensitive(& self)-> bool","4:5":"fn BrowserProfileStorageBridgeItem::is_sensitive(& self)-> bool","4:6":"fn BrowserProfileStorageBridgeItem::is_sensitive(& self)-> bool"},"l":{"4:3":[["app.rs",4409,4412]],"4:4":[["app.rs",4409,4412]],"4:5":[["app.rs",4410,4413]],"4:6":[["app.rs",4410,4413]]}},{"k":"fn:BrowserProfileStorageBridgeItem::kind","d":{"4:3":"Browser profile storage class.","4:4":"Browser profile storage class.","4:5":"Browser profile storage class.","4:6":"Browser profile storage class."},"s":{"4:3":"fn BrowserProfileStorageBridgeItem::kind(& self)-> & BrowserProfileStorageKind","4:4":"fn BrowserProfileStorageBridgeItem::kind(& self)-> & BrowserProfileStorageKind","4:5":"fn BrowserProfileStorageBridgeItem::kind(& self)-> & BrowserProfileStorageKind","4:6":"fn BrowserProfileStorageBridgeItem::kind(& self)-> & BrowserProfileStorageKind"},"l":{"4:3":[["app.rs",4399,4402]],"4:4":[["app.rs",4399,4402]],"4:5":[["app.rs",4400,4403]],"4:6":[["app.rs",4400,4403]]}},{"k":"fn:BrowserProfileStorageBridgeItem::requires_migration","d":{"4:3":"Whether existing browser-profile data must be migrated.","4:4":"Whether existing browser-profile data must be migrated.","4:5":"Whether existing browser-profile data must be migrated.","4:6":"Whether existing browser-profile data must be migrated."},"s":{"4:3":"fn BrowserProfileStorageBridgeItem::requires_migration(& self)-> bool","4:4":"fn BrowserProfileStorageBridgeItem::requires_migration(& self)-> bool","4:5":"fn BrowserProfileStorageBridgeItem::requires_migration(& self)-> bool","4:6":"fn BrowserProfileStorageBridgeItem::requires_migration(& self)-> bool"},"l":{"4:3":[["app.rs",4414,4417]],"4:4":[["app.rs",4414,4417]],"4:5":[["app.rs",4415,4418]],"4:6":[["app.rs",4415,4418]]}},{"k":"fn:BrowserProfileStorageBridgeItem::to_text","d":{"4:3":"Content-safe summary that avoids ids, keys, cookie names, origins, paths, and values.","4:4":"Content-safe summary that avoids ids, keys, cookie names, origins, paths, and values.","4:5":"Content-safe summary that avoids ids, keys, cookie names, origins, paths, and values.","4:6":"Content-safe summary that avoids ids, keys, cookie names, origins, paths, and values."},"s":{"4:3":"fn BrowserProfileStorageBridgeItem::to_text(& self)-> String","4:4":"fn BrowserProfileStorageBridgeItem::to_text(& self)-> String","4:5":"fn BrowserProfileStorageBridgeItem::to_text(& self)-> String","4:6":"fn BrowserProfileStorageBridgeItem::to_text(& self)-> String"},"l":{"4:3":[["app.rs",4419,4428]],"4:4":[["app.rs",4419,4428]],"4:5":[["app.rs",4420,4429]],"4:6":[["app.rs",4420,4429]]}},{"k":"fn:BrowserProfileStorageBridgeItemBuilder::new","d":{"4:3":"Create a storage bridge item.","4:4":"Create a storage bridge item.","4:5":"Create a storage bridge item.","4:6":"Create a storage bridge item."},"s":{"4:3":"fn BrowserProfileStorageBridgeItemBuilder::new(impl Into < String >, BrowserProfileStorageKind, BrowserProfileStorageDestination)-> Self","4:4":"fn BrowserProfileStorageBridgeItemBuilder::new(impl Into < String >, BrowserProfileStorageKind, BrowserProfileStorageDestination)-> Self","4:5":"fn BrowserProfileStorageBridgeItemBuilder::new(impl Into < String >, BrowserProfileStorageKind, BrowserProfileStorageDestination)-> Self","4:6":"fn BrowserProfileStorageBridgeItemBuilder::new(impl Into < String >, BrowserProfileStorageKind, BrowserProfileStorageDestination)-> Self"},"l":{"4:3":[["app.rs",4442,4455]],"4:4":[["app.rs",4442,4455]],"4:5":[["app.rs",4443,4456]],"4:6":[["app.rs",4443,4456]]}},{"k":"fn:BrowserProfileStorageBridgeItemBuilder::requires_migration","d":{"4:3":"Mark the item as requiring migration from an existing browser profile.","4:4":"Mark the item as requiring migration from an existing browser profile.","4:5":"Mark the item as requiring migration from an existing browser profile.","4:6":"Mark the item as requiring migration from an existing browser profile."},"s":{"4:3":"fn BrowserProfileStorageBridgeItemBuilder::requires_migration(mut self)-> Self","4:4":"fn BrowserProfileStorageBridgeItemBuilder::requires_migration(mut self)-> Self","4:5":"fn BrowserProfileStorageBridgeItemBuilder::requires_migration(mut self)-> Self","4:6":"fn BrowserProfileStorageBridgeItemBuilder::requires_migration(mut self)-> Self"},"l":{"4:3":[["app.rs",4463,4467]],"4:4":[["app.rs",4463,4467]],"4:5":[["app.rs",4464,4468]],"4:6":[["app.rs",4464,4468]]}},{"k":"fn:BrowserProfileStorageBridgeItemBuilder::sensitive","d":{"4:3":"Mark the item as containing sensitive user or account data.","4:4":"Mark the item as containing sensitive user or account data.","4:5":"Mark the item as containing sensitive user or account data.","4:6":"Mark the item as containing sensitive user or account data."},"s":{"4:3":"fn BrowserProfileStorageBridgeItemBuilder::sensitive(mut self)-> Self","4:4":"fn BrowserProfileStorageBridgeItemBuilder::sensitive(mut self)-> Self","4:5":"fn BrowserProfileStorageBridgeItemBuilder::sensitive(mut self)-> Self","4:6":"fn BrowserProfileStorageBridgeItemBuilder::sensitive(mut self)-> Self"},"l":{"4:3":[["app.rs",4457,4461]],"4:4":[["app.rs",4457,4461]],"4:5":[["app.rs",4458,4462]],"4:6":[["app.rs",4458,4462]]}},{"k":"fn:BrowserProfileStorageBridgePlan::app_id","d":{"4:3":"Application identifier that scopes the bridge.","4:4":"Application identifier that scopes the bridge.","4:5":"Application identifier that scopes the bridge.","4:6":"Application identifier that scopes the bridge."},"s":{"4:3":"fn BrowserProfileStorageBridgePlan::app_id(& self)-> & str","4:4":"fn BrowserProfileStorageBridgePlan::app_id(& self)-> & str","4:5":"fn BrowserProfileStorageBridgePlan::app_id(& self)-> & str","4:6":"fn BrowserProfileStorageBridgePlan::app_id(& self)-> & str"},"l":{"4:3":[["app.rs",4506,4509]],"4:4":[["app.rs",4506,4509]],"4:5":[["app.rs",4507,4510]],"4:6":[["app.rs",4507,4510]]}},{"k":"fn:BrowserProfileStorageBridgePlan::cleanup","d":{"4:3":"Optional cleanup plan for hosted/browser state.","4:4":"Optional cleanup plan for hosted/browser state.","4:5":"Optional cleanup plan for hosted/browser state.","4:6":"Optional cleanup plan for hosted/browser state."},"s":{"4:3":"fn BrowserProfileStorageBridgePlan::cleanup(& self)-> Option < & StorageCleanupPlan >","4:4":"fn BrowserProfileStorageBridgePlan::cleanup(& self)-> Option < & StorageCleanupPlan >","4:5":"fn BrowserProfileStorageBridgePlan::cleanup(& self)-> Option < & StorageCleanupPlan >","4:6":"fn BrowserProfileStorageBridgePlan::cleanup(& self)-> Option < & StorageCleanupPlan >"},"l":{"4:3":[["app.rs",4521,4524]],"4:4":[["app.rs",4521,4524]],"4:5":[["app.rs",4522,4525]],"4:6":[["app.rs",4522,4525]]}},{"k":"fn:BrowserProfileStorageBridgePlan::destination_count","d":{"4:3":"Number of items routed to a destination.","4:4":"Number of items routed to a destination.","4:5":"Number of items routed to a destination.","4:6":"Number of items routed to a destination."},"s":{"4:3":"fn BrowserProfileStorageBridgePlan::destination_count(& self, BrowserProfileStorageDestination)-> usize","4:4":"fn BrowserProfileStorageBridgePlan::destination_count(& self, BrowserProfileStorageDestination)-> usize","4:5":"fn BrowserProfileStorageBridgePlan::destination_count(& self, BrowserProfileStorageDestination)-> usize","4:6":"fn BrowserProfileStorageBridgePlan::destination_count(& self, BrowserProfileStorageDestination)-> usize"},"l":{"4:3":[["app.rs",4541,4547]],"4:4":[["app.rs",4541,4547]],"4:5":[["app.rs",4542,4548]],"4:6":[["app.rs",4542,4548]]}},{"k":"fn:BrowserProfileStorageBridgePlan::has_browser_island","d":{"4:3":"Whether any item must remain a browser/WebView island.","4:4":"Whether any item must remain a browser/WebView island.","4:5":"Whether any item must remain a browser/WebView island.","4:6":"Whether any item must remain a browser/WebView island."},"s":{"4:3":"fn BrowserProfileStorageBridgePlan::has_browser_island(& self)-> bool","4:4":"fn BrowserProfileStorageBridgePlan::has_browser_island(& self)-> bool","4:5":"fn BrowserProfileStorageBridgePlan::has_browser_island(& self)-> bool","4:6":"fn BrowserProfileStorageBridgePlan::has_browser_island(& self)-> bool"},"l":{"4:3":[["app.rs",4582,4585]],"4:4":[["app.rs",4582,4585]],"4:5":[["app.rs",4583,4586]],"4:6":[["app.rs",4583,4586]]}},{"k":"fn:BrowserProfileStorageBridgePlan::has_cleanup","d":{"4:3":"Whether any item requires hosted/browser cleanup.","4:4":"Whether any item requires hosted/browser cleanup.","4:5":"Whether any item requires hosted/browser cleanup.","4:6":"Whether any item requires hosted/browser cleanup."},"s":{"4:3":"fn BrowserProfileStorageBridgePlan::has_cleanup(& self)-> bool","4:4":"fn BrowserProfileStorageBridgePlan::has_cleanup(& self)-> bool","4:5":"fn BrowserProfileStorageBridgePlan::has_cleanup(& self)-> bool","4:6":"fn BrowserProfileStorageBridgePlan::has_cleanup(& self)-> bool"},"l":{"4:3":[["app.rs",4577,4580]],"4:4":[["app.rs",4577,4580]],"4:5":[["app.rs",4578,4581]],"4:6":[["app.rs",4578,4581]]}},{"k":"fn:BrowserProfileStorageBridgePlan::has_hosted_profile_storage","d":{"4:3":"Whether any item remains in hosted browser profile storage.","4:4":"Whether any item remains in hosted browser profile storage.","4:5":"Whether any item remains in hosted browser profile storage.","4:6":"Whether any item remains in hosted browser profile storage."},"s":{"4:3":"fn BrowserProfileStorageBridgePlan::has_hosted_profile_storage(& self)-> bool","4:4":"fn BrowserProfileStorageBridgePlan::has_hosted_profile_storage(& self)-> bool","4:5":"fn BrowserProfileStorageBridgePlan::has_hosted_profile_storage(& self)-> bool","4:6":"fn BrowserProfileStorageBridgePlan::has_hosted_profile_storage(& self)-> bool"},"l":{"4:3":[["app.rs",4572,4575]],"4:4":[["app.rs",4572,4575]],"4:5":[["app.rs",4573,4576]],"4:6":[["app.rs",4573,4576]]}},{"k":"fn:BrowserProfileStorageBridgePlan::has_native_storage","d":{"4:3":"Whether any item uses app-owned native storage.","4:4":"Whether any item uses app-owned native storage.","4:5":"Whether any item uses app-owned native storage.","4:6":"Whether any item uses app-owned native storage."},"s":{"4:3":"fn BrowserProfileStorageBridgePlan::has_native_storage(& self)-> bool","4:4":"fn BrowserProfileStorageBridgePlan::has_native_storage(& self)-> bool","4:5":"fn BrowserProfileStorageBridgePlan::has_native_storage(& self)-> bool","4:6":"fn BrowserProfileStorageBridgePlan::has_native_storage(& self)-> bool"},"l":{"4:3":[["app.rs",4562,4565]],"4:4":[["app.rs",4562,4565]],"4:5":[["app.rs",4563,4566]],"4:6":[["app.rs",4563,4566]]}},{"k":"fn:BrowserProfileStorageBridgePlan::has_roadmap_work","d":{"4:3":"Whether roadmap work is present.","4:4":"Whether roadmap work is present.","4:5":"Whether roadmap work is present.","4:6":"Whether roadmap work is present."},"s":{"4:3":"fn BrowserProfileStorageBridgePlan::has_roadmap_work(& self)-> bool","4:4":"fn BrowserProfileStorageBridgePlan::has_roadmap_work(& self)-> bool","4:5":"fn BrowserProfileStorageBridgePlan::has_roadmap_work(& self)-> bool","4:6":"fn BrowserProfileStorageBridgePlan::has_roadmap_work(& self)-> bool"},"l":{"4:3":[["app.rs",4587,4590]],"4:4":[["app.rs",4587,4590]],"4:5":[["app.rs",4588,4591]],"4:6":[["app.rs",4588,4591]]}},{"k":"fn:BrowserProfileStorageBridgePlan::has_secure_credentials","d":{"4:3":"Whether any item uses secure credentials.","4:4":"Whether any item uses secure credentials.","4:5":"Whether any item uses secure credentials.","4:6":"Whether any item uses secure credentials."},"s":{"4:3":"fn BrowserProfileStorageBridgePlan::has_secure_credentials(& self)-> bool","4:4":"fn BrowserProfileStorageBridgePlan::has_secure_credentials(& self)-> bool","4:5":"fn BrowserProfileStorageBridgePlan::has_secure_credentials(& self)-> bool","4:6":"fn BrowserProfileStorageBridgePlan::has_secure_credentials(& self)-> bool"},"l":{"4:3":[["app.rs",4567,4570]],"4:4":[["app.rs",4567,4570]],"4:5":[["app.rs",4568,4571]],"4:6":[["app.rs",4568,4571]]}},{"k":"fn:BrowserProfileStorageBridgePlan::hosted_profile_id","d":{"4:3":"Hosted WebView profile id, when any item remains browser-owned.","4:4":"Hosted WebView profile id, when any item remains browser-owned.","4:5":"Hosted WebView profile id, when any item remains browser-owned.","4:6":"Hosted WebView profile id, when any item remains browser-owned."},"s":{"4:3":"fn BrowserProfileStorageBridgePlan::hosted_profile_id(& self)-> Option < & str >","4:4":"fn BrowserProfileStorageBridgePlan::hosted_profile_id(& self)-> Option < & str >","4:5":"fn BrowserProfileStorageBridgePlan::hosted_profile_id(& self)-> Option < & str >","4:6":"fn BrowserProfileStorageBridgePlan::hosted_profile_id(& self)-> Option < & str >"},"l":{"4:3":[["app.rs",4511,4514]],"4:4":[["app.rs",4511,4514]],"4:5":[["app.rs",4512,4515]],"4:6":[["app.rs",4512,4515]]}},{"k":"fn:BrowserProfileStorageBridgePlan::item_count","d":{"4:3":"Number of planned browser-profile storage items.","4:4":"Number of planned browser-profile storage items.","4:5":"Number of planned browser-profile storage items.","4:6":"Number of planned browser-profile storage items."},"s":{"4:3":"fn BrowserProfileStorageBridgePlan::item_count(& self)-> usize","4:4":"fn BrowserProfileStorageBridgePlan::item_count(& self)-> usize","4:5":"fn BrowserProfileStorageBridgePlan::item_count(& self)-> usize","4:6":"fn BrowserProfileStorageBridgePlan::item_count(& self)-> usize"},"l":{"4:3":[["app.rs",4536,4539]],"4:4":[["app.rs",4536,4539]],"4:5":[["app.rs",4537,4540]],"4:6":[["app.rs",4537,4540]]}},{"k":"fn:BrowserProfileStorageBridgePlan::items","d":{"4:3":"Planned storage items.","4:4":"Planned storage items.","4:5":"Planned storage items.","4:6":"Planned storage items."},"s":{"4:3":"fn BrowserProfileStorageBridgePlan::items(& self)-> & [BrowserProfileStorageBridgeItem]","4:4":"fn BrowserProfileStorageBridgePlan::items(& self)-> & [BrowserProfileStorageBridgeItem]","4:5":"fn BrowserProfileStorageBridgePlan::items(& self)-> & [BrowserProfileStorageBridgeItem]","4:6":"fn BrowserProfileStorageBridgePlan::items(& self)-> & [BrowserProfileStorageBridgeItem]"},"l":{"4:3":[["app.rs",4526,4529]],"4:4":[["app.rs",4526,4529]],"4:5":[["app.rs",4527,4530]],"4:6":[["app.rs",4527,4530]]}},{"k":"fn:BrowserProfileStorageBridgePlan::migration_count","d":{"4:3":"Number of items that require migration from a browser profile.","4:4":"Number of items that require migration from a browser profile.","4:5":"Number of items that require migration from a browser profile.","4:6":"Number of items that require migration from a browser profile."},"s":{"4:3":"fn BrowserProfileStorageBridgePlan::migration_count(& self)-> usize","4:4":"fn BrowserProfileStorageBridgePlan::migration_count(& self)-> usize","4:5":"fn BrowserProfileStorageBridgePlan::migration_count(& self)-> usize","4:6":"fn BrowserProfileStorageBridgePlan::migration_count(& self)-> usize"},"l":{"4:3":[["app.rs",4554,4560]],"4:4":[["app.rs",4554,4560]],"4:5":[["app.rs",4555,4561]],"4:6":[["app.rs",4555,4561]]}},{"k":"fn:BrowserProfileStorageBridgePlan::native_storage","d":{"4:3":"Optional app-owned native storage plan.","4:4":"Optional app-owned native storage plan.","4:5":"Optional app-owned native storage plan.","4:6":"Optional app-owned native storage plan."},"s":{"4:3":"fn BrowserProfileStorageBridgePlan::native_storage(& self)-> Option < & AppStoragePlan >","4:4":"fn BrowserProfileStorageBridgePlan::native_storage(& self)-> Option < & AppStoragePlan >","4:5":"fn BrowserProfileStorageBridgePlan::native_storage(& self)-> Option < & AppStoragePlan >","4:6":"fn BrowserProfileStorageBridgePlan::native_storage(& self)-> Option < & AppStoragePlan >"},"l":{"4:3":[["app.rs",4516,4519]],"4:4":[["app.rs",4516,4519]],"4:5":[["app.rs",4517,4520]],"4:6":[["app.rs",4517,4520]]}},{"k":"fn:BrowserProfileStorageBridgePlan::next_action","d":{"4:3":"Recommended next action.","4:4":"Recommended next action.","4:5":"Recommended next action.","4:6":"Recommended next action."},"s":{"4:3":"fn BrowserProfileStorageBridgePlan::next_action(& self)-> BrowserProfileStorageNextAction","4:4":"fn BrowserProfileStorageBridgePlan::next_action(& self)-> BrowserProfileStorageNextAction","4:5":"fn BrowserProfileStorageBridgePlan::next_action(& self)-> BrowserProfileStorageNextAction","4:6":"fn BrowserProfileStorageBridgePlan::next_action(& self)-> BrowserProfileStorageNextAction"},"l":{"4:3":[["app.rs",4531,4534]],"4:4":[["app.rs",4531,4534]],"4:5":[["app.rs",4532,4535]],"4:6":[["app.rs",4532,4535]]}},{"k":"fn:BrowserProfileStorageBridgePlan::sensitive_count","d":{"4:3":"Number of sensitive storage items.","4:4":"Number of sensitive storage items.","4:5":"Number of sensitive storage items.","4:6":"Number of sensitive storage items."},"s":{"4:3":"fn BrowserProfileStorageBridgePlan::sensitive_count(& self)-> usize","4:4":"fn BrowserProfileStorageBridgePlan::sensitive_count(& self)-> usize","4:5":"fn BrowserProfileStorageBridgePlan::sensitive_count(& self)-> usize","4:6":"fn BrowserProfileStorageBridgePlan::sensitive_count(& self)-> usize"},"l":{"4:3":[["app.rs",4549,4552]],"4:4":[["app.rs",4549,4552]],"4:5":[["app.rs",4550,4553]],"4:6":[["app.rs",4550,4553]]}},{"k":"fn:BrowserProfileStorageBridgePlan::to_text","d":{"4:3":"Content-safe summary that avoids app ids, profile ids, item ids, keys, origins, paths, and values.","4:4":"Content-safe summary that avoids app ids, profile ids, item ids, keys, origins, paths, and values.","4:5":"Content-safe summary that avoids app ids, profile ids, item ids, keys, origins, paths, and values.","4:6":"Content-safe summary that avoids app ids, profile ids, item ids, keys, origins, paths, and values."},"s":{"4:3":"fn BrowserProfileStorageBridgePlan::to_text(& self)-> String","4:4":"fn BrowserProfileStorageBridgePlan::to_text(& self)-> String","4:5":"fn BrowserProfileStorageBridgePlan::to_text(& self)-> String","4:6":"fn BrowserProfileStorageBridgePlan::to_text(& self)-> String"},"l":{"4:3":[["app.rs",4592,4607]],"4:4":[["app.rs",4592,4607]],"4:5":[["app.rs",4593,4608]],"4:6":[["app.rs",4593,4608]]}},{"k":"fn:BrowserProfileStorageBridgePlanBuilder::auth_session_hosted","d":{"4:3":"Keep hosted auth/session state in an explicit WebView profile island.","4:4":"Keep hosted auth/session state in an explicit WebView profile island.","4:5":"Keep hosted auth/session state in an explicit WebView profile island.","4:6":"Keep hosted auth/session state in an explicit WebView profile island."},"s":{"4:3":"fn BrowserProfileStorageBridgePlanBuilder::auth_session_hosted(self, impl Into < String >)-> Self","4:4":"fn BrowserProfileStorageBridgePlanBuilder::auth_session_hosted(self, impl Into < String >)-> Self","4:5":"fn BrowserProfileStorageBridgePlanBuilder::auth_session_hosted(self, impl Into < String >)-> Self","4:6":"fn BrowserProfileStorageBridgePlanBuilder::auth_session_hosted(self, impl Into < String >)-> Self"},"l":{"4:3":[["app.rs",4687,4694]],"4:4":[["app.rs",4687,4694]],"4:5":[["app.rs",4688,4695]],"4:6":[["app.rs",4688,4695]]}},{"k":"fn:BrowserProfileStorageBridgePlanBuilder::browser_island","d":{"4:3":"Keep browser-owned storage in a browser island because exact browser semantics are required.","4:4":"Keep browser-owned storage in a browser island because exact browser semantics are required.","4:5":"Keep browser-owned storage in a browser island because exact browser semantics are required.","4:6":"Keep browser-owned storage in a browser island because exact browser semantics are required."},"s":{"4:3":"fn BrowserProfileStorageBridgePlanBuilder::browser_island(self, impl Into < String >, BrowserProfileStorageKind)-> Self","4:4":"fn BrowserProfileStorageBridgePlanBuilder::browser_island(self, impl Into < String >, BrowserProfileStorageKind)-> Self","4:5":"fn BrowserProfileStorageBridgePlanBuilder::browser_island(self, impl Into < String >, BrowserProfileStorageKind)-> Self","4:6":"fn BrowserProfileStorageBridgePlanBuilder::browser_island(self, impl Into < String >, BrowserProfileStorageKind)-> Self"},"l":{"4:3":[["app.rs",4708,4715]],"4:4":[["app.rs",4708,4715]],"4:5":[["app.rs",4709,4716]],"4:6":[["app.rs",4709,4716]]}},{"k":"fn:BrowserProfileStorageBridgePlanBuilder::build_checked","d":{"4:3":"Build the checked browser-profile storage bridge plan.","4:4":"Build the checked browser-profile storage bridge plan.","4:5":"Build the checked browser-profile storage bridge plan.","4:6":"Build the checked browser-profile storage bridge plan."},"s":{"4:3":"fn BrowserProfileStorageBridgePlanBuilder::build_checked(self)-> Result < BrowserProfileStorageBridgePlan >","4:4":"fn BrowserProfileStorageBridgePlanBuilder::build_checked(self)-> Result < BrowserProfileStorageBridgePlan >","4:5":"fn BrowserProfileStorageBridgePlanBuilder::build_checked(self)-> Result < BrowserProfileStorageBridgePlan >","4:6":"fn BrowserProfileStorageBridgePlanBuilder::build_checked(self)-> Result < BrowserProfileStorageBridgePlan >"},"l":{"4:3":[["app.rs",4809,4834]],"4:4":[["app.rs",4809,4834]],"4:5":[["app.rs",4810,4835]],"4:6":[["app.rs",4810,4835]]}},{"k":"fn:BrowserProfileStorageBridgePlanBuilder::cleanup_plan","d":{"4:3":"Provide cleanup work used by `CleanupHostedStorage` destinations.","4:4":"Provide cleanup work used by `CleanupHostedStorage` destinations.","4:5":"Provide cleanup work used by `CleanupHostedStorage` destinations.","4:6":"Provide cleanup work used by `CleanupHostedStorage` destinations."},"s":{"4:3":"fn BrowserProfileStorageBridgePlanBuilder::cleanup_plan(mut self, StorageCleanupPlanBuilder)-> Self","4:4":"fn BrowserProfileStorageBridgePlanBuilder::cleanup_plan(mut self, StorageCleanupPlanBuilder)-> Self","4:5":"fn BrowserProfileStorageBridgePlanBuilder::cleanup_plan(mut self, StorageCleanupPlanBuilder)-> Self","4:6":"fn BrowserProfileStorageBridgePlanBuilder::cleanup_plan(mut self, StorageCleanupPlanBuilder)-> Self"},"l":{"4:3":[["app.rs",4638,4642]],"4:4":[["app.rs",4638,4642]],"4:5":[["app.rs",4639,4643]],"4:6":[["app.rs",4639,4643]]}},{"k":"fn:BrowserProfileStorageBridgePlanBuilder::cookies_to_secure_credentials","d":{"4:3":"Route browser cookies to secure credential storage.","4:4":"Route browser cookies to secure credential storage.","4:5":"Route browser cookies to secure credential storage.","4:6":"Route browser cookies to secure credential storage."},"s":{"4:3":"fn BrowserProfileStorageBridgePlanBuilder::cookies_to_secure_credentials(self, impl Into < String >)-> Self","4:4":"fn BrowserProfileStorageBridgePlanBuilder::cookies_to_secure_credentials(self, impl Into < String >)-> Self","4:5":"fn BrowserProfileStorageBridgePlanBuilder::cookies_to_secure_credentials(self, impl Into < String >)-> Self","4:6":"fn BrowserProfileStorageBridgePlanBuilder::cookies_to_secure_credentials(self, impl Into < String >)-> Self"},"l":{"4:3":[["app.rs",4674,4685]],"4:4":[["app.rs",4674,4685]],"4:5":[["app.rs",4675,4686]],"4:6":[["app.rs",4675,4686]]}},{"k":"fn:BrowserProfileStorageBridgePlanBuilder::has_cleanup_plan","d":{"4:3":"Whether a cleanup plan was provided.","4:4":"Whether a cleanup plan was provided.","4:5":"Whether a cleanup plan was provided.","4:6":"Whether a cleanup plan was provided."},"s":{"4:3":"fn BrowserProfileStorageBridgePlanBuilder::has_cleanup_plan(& self)-> bool","4:4":"fn BrowserProfileStorageBridgePlanBuilder::has_cleanup_plan(& self)-> bool","4:5":"fn BrowserProfileStorageBridgePlanBuilder::has_cleanup_plan(& self)-> bool","4:6":"fn BrowserProfileStorageBridgePlanBuilder::has_cleanup_plan(& self)-> bool"},"l":{"4:3":[["app.rs",4736,4739]],"4:4":[["app.rs",4736,4739]],"4:5":[["app.rs",4737,4740]],"4:6":[["app.rs",4737,4740]]}},{"k":"fn:BrowserProfileStorageBridgePlanBuilder::has_hosted_profile_id","d":{"4:3":"Whether a hosted profile id was provided.","4:4":"Whether a hosted profile id was provided.","4:5":"Whether a hosted profile id was provided.","4:6":"Whether a hosted profile id was provided."},"s":{"4:3":"fn BrowserProfileStorageBridgePlanBuilder::has_hosted_profile_id(& self)-> bool","4:4":"fn BrowserProfileStorageBridgePlanBuilder::has_hosted_profile_id(& self)-> bool","4:5":"fn BrowserProfileStorageBridgePlanBuilder::has_hosted_profile_id(& self)-> bool","4:6":"fn BrowserProfileStorageBridgePlanBuilder::has_hosted_profile_id(& self)-> bool"},"l":{"4:3":[["app.rs",4741,4744]],"4:4":[["app.rs",4741,4744]],"4:5":[["app.rs",4742,4745]],"4:6":[["app.rs",4742,4745]]}},{"k":"fn:BrowserProfileStorageBridgePlanBuilder::has_native_storage_plan","d":{"4:3":"Whether a native storage plan was provided.","4:4":"Whether a native storage plan was provided.","4:5":"Whether a native storage plan was provided.","4:6":"Whether a native storage plan was provided."},"s":{"4:3":"fn BrowserProfileStorageBridgePlanBuilder::has_native_storage_plan(& self)-> bool","4:4":"fn BrowserProfileStorageBridgePlanBuilder::has_native_storage_plan(& self)-> bool","4:5":"fn BrowserProfileStorageBridgePlanBuilder::has_native_storage_plan(& self)-> bool","4:6":"fn BrowserProfileStorageBridgePlanBuilder::has_native_storage_plan(& self)-> bool"},"l":{"4:3":[["app.rs",4731,4734]],"4:4":[["app.rs",4731,4734]],"4:5":[["app.rs",4732,4735]],"4:6":[["app.rs",4732,4735]]}},{"k":"fn:BrowserProfileStorageBridgePlanBuilder::hosted_profile_id","d":{"4:3":"Set the explicit hosted WebView profile id for browser-owned state.","4:4":"Set the explicit hosted WebView profile id for browser-owned state.","4:5":"Set the explicit hosted WebView profile id for browser-owned state.","4:6":"Set the explicit hosted WebView profile id for browser-owned state."},"s":{"4:3":"fn BrowserProfileStorageBridgePlanBuilder::hosted_profile_id(mut self, impl Into < String >)-> Self","4:4":"fn BrowserProfileStorageBridgePlanBuilder::hosted_profile_id(mut self, impl Into < String >)-> Self","4:5":"fn BrowserProfileStorageBridgePlanBuilder::hosted_profile_id(mut self, impl Into < String >)-> Self","4:6":"fn BrowserProfileStorageBridgePlanBuilder::hosted_profile_id(mut self, impl Into < String >)-> Self"},"l":{"4:3":[["app.rs",4644,4648]],"4:4":[["app.rs",4644,4648]],"4:5":[["app.rs",4645,4649]],"4:6":[["app.rs",4645,4649]]}},{"k":"fn:BrowserProfileStorageBridgePlanBuilder::http_cache_cleanup","d":{"4:3":"Clear hosted HTTP/cache state during logout, reset, or migration.","4:4":"Clear hosted HTTP/cache state during logout, reset, or migration.","4:5":"Clear hosted HTTP/cache state during logout, reset, or migration.","4:6":"Clear hosted HTTP/cache state during logout, reset, or migration."},"s":{"4:3":"fn BrowserProfileStorageBridgePlanBuilder::http_cache_cleanup(self, impl Into < String >)-> Self","4:4":"fn BrowserProfileStorageBridgePlanBuilder::http_cache_cleanup(self, impl Into < String >)-> Self","4:5":"fn BrowserProfileStorageBridgePlanBuilder::http_cache_cleanup(self, impl Into < String >)-> Self","4:6":"fn BrowserProfileStorageBridgePlanBuilder::http_cache_cleanup(self, impl Into < String >)-> Self"},"l":{"4:3":[["app.rs",4696,4706]],"4:4":[["app.rs",4696,4706]],"4:5":[["app.rs",4697,4707]],"4:6":[["app.rs",4697,4707]]}},{"k":"fn:BrowserProfileStorageBridgePlanBuilder::indexed_db_to_native","d":{"4:3":"Route IndexedDB data to native storage.","4:4":"Route IndexedDB data to native storage.","4:5":"Route IndexedDB data to native storage.","4:6":"Route IndexedDB data to native storage."},"s":{"4:3":"fn BrowserProfileStorageBridgePlanBuilder::indexed_db_to_native(self, impl Into < String >)-> Self","4:4":"fn BrowserProfileStorageBridgePlanBuilder::indexed_db_to_native(self, impl Into < String >)-> Self","4:5":"fn BrowserProfileStorageBridgePlanBuilder::indexed_db_to_native(self, impl Into < String >)-> Self","4:6":"fn BrowserProfileStorageBridgePlanBuilder::indexed_db_to_native(self, impl Into < String >)-> Self"},"l":{"4:3":[["app.rs",4665,4672]],"4:4":[["app.rs",4665,4672]],"4:5":[["app.rs",4666,4673]],"4:6":[["app.rs",4666,4673]]}},{"k":"fn:BrowserProfileStorageBridgePlanBuilder::item","d":{"4:3":"Add a bridge item.","4:4":"Add a bridge item.","4:5":"Add a bridge item.","4:6":"Add a bridge item."},"s":{"4:3":"fn BrowserProfileStorageBridgePlanBuilder::item(mut self, BrowserProfileStorageBridgeItemBuilder)-> Self","4:4":"fn BrowserProfileStorageBridgePlanBuilder::item(mut self, BrowserProfileStorageBridgeItemBuilder)-> Self","4:5":"fn BrowserProfileStorageBridgePlanBuilder::item(mut self, BrowserProfileStorageBridgeItemBuilder)-> Self","4:6":"fn BrowserProfileStorageBridgePlanBuilder::item(mut self, BrowserProfileStorageBridgeItemBuilder)-> Self"},"l":{"4:3":[["app.rs",4650,4654]],"4:4":[["app.rs",4650,4654]],"4:5":[["app.rs",4651,4655]],"4:6":[["app.rs",4651,4655]]}},{"k":"fn:BrowserProfileStorageBridgePlanBuilder::item_count","d":{"4:3":"Number of configured bridge items.","4:4":"Number of configured bridge items.","4:5":"Number of configured bridge items.","4:6":"Number of configured bridge items."},"s":{"4:3":"fn BrowserProfileStorageBridgePlanBuilder::item_count(& self)-> usize","4:4":"fn BrowserProfileStorageBridgePlanBuilder::item_count(& self)-> usize","4:5":"fn BrowserProfileStorageBridgePlanBuilder::item_count(& self)-> usize","4:6":"fn BrowserProfileStorageBridgePlanBuilder::item_count(& self)-> usize"},"l":{"4:3":[["app.rs",4726,4729]],"4:4":[["app.rs",4726,4729]],"4:5":[["app.rs",4727,4730]],"4:6":[["app.rs",4727,4730]]}},{"k":"fn:BrowserProfileStorageBridgePlanBuilder::local_storage_to_native","d":{"4:3":"Route localStorage data to native storage.","4:4":"Route localStorage data to native storage.","4:5":"Route localStorage data to native storage.","4:6":"Route localStorage data to native storage."},"s":{"4:3":"fn BrowserProfileStorageBridgePlanBuilder::local_storage_to_native(self, impl Into < String >)-> Self","4:4":"fn BrowserProfileStorageBridgePlanBuilder::local_storage_to_native(self, impl Into < String >)-> Self","4:5":"fn BrowserProfileStorageBridgePlanBuilder::local_storage_to_native(self, impl Into < String >)-> Self","4:6":"fn BrowserProfileStorageBridgePlanBuilder::local_storage_to_native(self, impl Into < String >)-> Self"},"l":{"4:3":[["app.rs",4656,4663]],"4:4":[["app.rs",4656,4663]],"4:5":[["app.rs",4657,4664]],"4:6":[["app.rs",4657,4664]]}},{"k":"fn:BrowserProfileStorageBridgePlanBuilder::native_storage_plan","d":{"4:3":"Provide the native storage plan used by `NativeStorage` destinations.","4:4":"Provide the native storage plan used by `NativeStorage` destinations.","4:5":"Provide the native storage plan used by `NativeStorage` destinations.","4:6":"Provide the native storage plan used by `NativeStorage` destinations."},"s":{"4:3":"fn BrowserProfileStorageBridgePlanBuilder::native_storage_plan(mut self, AppStoragePlanBuilder)-> Self","4:4":"fn BrowserProfileStorageBridgePlanBuilder::native_storage_plan(mut self, AppStoragePlanBuilder)-> Self","4:5":"fn BrowserProfileStorageBridgePlanBuilder::native_storage_plan(mut self, AppStoragePlanBuilder)-> Self","4:6":"fn BrowserProfileStorageBridgePlanBuilder::native_storage_plan(mut self, AppStoragePlanBuilder)-> Self"},"l":{"4:3":[["app.rs",4632,4636]],"4:4":[["app.rs",4632,4636]],"4:5":[["app.rs",4633,4637]],"4:6":[["app.rs",4633,4637]]}},{"k":"fn:BrowserProfileStorageBridgePlanBuilder::new","d":{"4:3":"Create an empty browser-profile storage bridge plan.","4:4":"Create an empty browser-profile storage bridge plan.","4:5":"Create an empty browser-profile storage bridge plan.","4:6":"Create an empty browser-profile storage bridge plan."},"s":{"4:3":"fn BrowserProfileStorageBridgePlanBuilder::new(impl Into < String >)-> Self","4:4":"fn BrowserProfileStorageBridgePlanBuilder::new(impl Into < String >)-> Self","4:5":"fn BrowserProfileStorageBridgePlanBuilder::new(impl Into < String >)-> Self","4:6":"fn BrowserProfileStorageBridgePlanBuilder::new(impl Into < String >)-> Self"},"l":{"4:3":[["app.rs",4621,4630]],"4:4":[["app.rs",4621,4630]],"4:5":[["app.rs",4622,4631]],"4:6":[["app.rs",4622,4631]]}},{"k":"fn:BrowserProfileStorageBridgePlanBuilder::roadmap_work","d":{"4:3":"Track app-specific storage migration work.","4:4":"Track app-specific storage migration work.","4:5":"Track app-specific storage migration work.","4:6":"Track app-specific storage migration work."},"s":{"4:3":"fn BrowserProfileStorageBridgePlanBuilder::roadmap_work(self, impl Into < String >, BrowserProfileStorageKind)-> Self","4:4":"fn BrowserProfileStorageBridgePlanBuilder::roadmap_work(self, impl Into < String >, BrowserProfileStorageKind)-> Self","4:5":"fn BrowserProfileStorageBridgePlanBuilder::roadmap_work(self, impl Into < String >, BrowserProfileStorageKind)-> Self","4:6":"fn BrowserProfileStorageBridgePlanBuilder::roadmap_work(self, impl Into < String >, BrowserProfileStorageKind)-> Self"},"l":{"4:3":[["app.rs",4717,4724]],"4:4":[["app.rs",4717,4724]],"4:5":[["app.rs",4718,4725]],"4:6":[["app.rs",4718,4725]]}},{"k":"fn:BrowserProfileStorageBridgePlanBuilder::to_text","d":{"4:3":"Content-safe builder summary.","4:4":"Content-safe builder summary.","4:5":"Content-safe builder summary.","4:6":"Content-safe builder summary."},"s":{"4:3":"fn BrowserProfileStorageBridgePlanBuilder::to_text(& self)-> String","4:4":"fn BrowserProfileStorageBridgePlanBuilder::to_text(& self)-> String","4:5":"fn BrowserProfileStorageBridgePlanBuilder::to_text(& self)-> String","4:6":"fn BrowserProfileStorageBridgePlanBuilder::to_text(& self)-> String"},"l":{"4:3":[["app.rs",4746,4755]],"4:4":[["app.rs",4746,4755]],"4:5":[["app.rs",4747,4756]],"4:6":[["app.rs",4747,4756]]}},{"k":"fn:BrowserProfileStorageBridgePlanBuilder::validate","d":{"4:3":"Validate without consuming the builder.","4:4":"Validate without consuming the builder.","4:5":"Validate without consuming the builder.","4:6":"Validate without consuming the builder."},"s":{"4:3":"fn BrowserProfileStorageBridgePlanBuilder::validate(& self)-> Result < () >","4:4":"fn BrowserProfileStorageBridgePlanBuilder::validate(& self)-> Result < () >","4:5":"fn BrowserProfileStorageBridgePlanBuilder::validate(& self)-> Result < () >","4:6":"fn BrowserProfileStorageBridgePlanBuilder::validate(& self)-> Result < () >"},"l":{"4:3":[["app.rs",4757,4807]],"4:4":[["app.rs",4757,4807]],"4:5":[["app.rs",4758,4808]],"4:6":[["app.rs",4758,4808]]}},{"k":"fn:BrowserProfileStorageDestination::to_text","d":{"4:3":"Stable label for logs and generated plans.","4:4":"Stable label for logs and generated plans.","4:5":"Stable label for logs and generated plans.","4:6":"Stable label for logs and generated plans."},"s":{"4:3":"fn BrowserProfileStorageDestination::to_text(self)-> & 'static str","4:4":"fn BrowserProfileStorageDestination::to_text(self)-> & 'static str","4:5":"fn BrowserProfileStorageDestination::to_text(self)-> & 'static str","4:6":"fn BrowserProfileStorageDestination::to_text(self)-> & 'static str"},"l":{"4:3":[["app.rs",4339,4349]],"4:4":[["app.rs",4339,4349]],"4:5":[["app.rs",4340,4350]],"4:6":[["app.rs",4340,4350]]}},{"k":"fn:BrowserProfileStorageKind::to_text","d":{"4:3":"Content-safe storage class label.","4:4":"Content-safe storage class label.","4:5":"Content-safe storage class label.","4:6":"Content-safe storage class label."},"s":{"4:3":"fn BrowserProfileStorageKind::to_text(& self)-> & 'static str","4:4":"fn BrowserProfileStorageKind::to_text(& self)-> & 'static str","4:5":"fn BrowserProfileStorageKind::to_text(& self)-> & 'static str","4:6":"fn BrowserProfileStorageKind::to_text(& self)-> & 'static str"},"l":{"4:3":[["app.rs",4297,4311]],"4:4":[["app.rs",4297,4311]],"4:5":[["app.rs",4298,4312]],"4:6":[["app.rs",4298,4312]]}},{"k":"fn:BrowserProfileStorageNextAction::to_text","d":{"4:3":"Stable action label for logs, setup screens, and generated agents.","4:4":"Stable action label for logs, setup screens, and generated agents.","4:5":"Stable action label for logs, setup screens, and generated agents.","4:6":"Stable action label for logs, setup screens, and generated agents."},"s":{"4:3":"fn BrowserProfileStorageNextAction::to_text(self)-> & 'static str","4:4":"fn BrowserProfileStorageNextAction::to_text(self)-> & 'static str","4:5":"fn BrowserProfileStorageNextAction::to_text(self)-> & 'static str","4:6":"fn BrowserProfileStorageNextAction::to_text(self)-> & 'static str"},"l":{"4:3":[["app.rs",4370,4380]],"4:4":[["app.rs",4370,4380]],"4:5":[["app.rs",4371,4381]],"4:6":[["app.rs",4371,4381]]}},{"k":"fn:BrowserWebViewPolicy::allowed_origins","d":{"4:5":"Restrict host-directed navigations and exact-origin bridge messages to\nthese origins. Strict sandbox messages use an opaque origin and are\nauthenticated by their frame source plus the per-host nonce instead.","4:6":"Restrict host-directed navigations and exact-origin bridge messages to\nthese origins. Strict sandbox messages use an opaque origin and are\nauthenticated by their frame source plus the per-host nonce instead."},"s":{"4:5":"fn BrowserWebViewPolicy::allowed_origins(mut self, impl IntoIterator < Item = impl Into < SharedString > >)-> Self","4:6":"fn BrowserWebViewPolicy::allowed_origins(mut self, impl IntoIterator < Item = impl Into < SharedString > >)-> Self"},"l":{"4:5":[["webview.rs",650,659]],"4:6":[["webview.rs",650,659]]}},{"k":"fn:BrowserWebViewPolicy::credentialless","d":{"4:5":"Enable or disable credentialless iframe loading where supported.","4:6":"Enable or disable credentialless iframe loading where supported."},"s":{"4:5":"fn BrowserWebViewPolicy::credentialless(mut self, bool)-> Self","4:6":"fn BrowserWebViewPolicy::credentialless(mut self, bool)-> Self"},"l":{"4:5":[["webview.rs",675,679]],"4:6":[["webview.rs",675,679]]}},{"k":"fn:BrowserWebViewPolicy::downloads","d":{"4:5":"Permit or deny user-activated downloads in sandboxed hosted frames.\n\nThis has no effect with [`BrowserWebViewSandbox::Unrestricted`], whose\nunsandboxed content remains subject to the browser's own policy.","4:6":"Permit or deny user-activated downloads in sandboxed hosted frames.\n\nThis has no effect with [`BrowserWebViewSandbox::Unrestricted`], whose\nunsandboxed content remains subject to the browser's own policy."},"s":{"4:5":"fn BrowserWebViewPolicy::downloads(mut self, bool)-> Self","4:6":"fn BrowserWebViewPolicy::downloads(mut self, bool)-> Self"},"l":{"4:5":[["webview.rs",687,694]],"4:6":[["webview.rs",687,694]]}},{"k":"fn:BrowserWebViewPolicy::loading","d":{"4:5":"Select eager or lazy iframe loading.","4:6":"Select eager or lazy iframe loading."},"s":{"4:5":"fn BrowserWebViewPolicy::loading(mut self, BrowserWebViewLoading)-> Self","4:6":"fn BrowserWebViewPolicy::loading(mut self, BrowserWebViewLoading)-> Self"},"l":{"4:5":[["webview.rs",681,685]],"4:6":[["webview.rs",681,685]]}},{"k":"fn:BrowserWebViewPolicy::permission","d":{"4:5":"Delegate a browser Permission Policy capability to the hosted frame.","4:6":"Delegate a browser Permission Policy capability to the hosted frame."},"s":{"4:5":"fn BrowserWebViewPolicy::permission(mut self, WebViewPermissionKind)-> Self","4:6":"fn BrowserWebViewPolicy::permission(mut self, WebViewPermissionKind)-> Self"},"l":{"4:5":[["webview.rs",661,667]],"4:6":[["webview.rs",661,667]]}},{"k":"fn:BrowserWebViewPolicy::referrer_policy","d":{"4:5":"Set the iframe referrer policy.","4:6":"Set the iframe referrer policy."},"s":{"4:5":"fn BrowserWebViewPolicy::referrer_policy(mut self, impl Into < SharedString >)-> Self","4:6":"fn BrowserWebViewPolicy::referrer_policy(mut self, impl Into < SharedString >)-> Self"},"l":{"4:5":[["webview.rs",669,673]],"4:6":[["webview.rs",669,673]]}},{"k":"fn:BrowserWebViewPolicy::require_cooperative_bridge","d":{"4:5":"Require Kael to install its authenticated message bridge in the hosted\ndocument, logging an integration error when browser origin rules block it.","4:6":"Require Kael to install its authenticated message bridge in the hosted\ndocument, logging an integration error when browser origin rules block it."},"s":{"4:5":"fn BrowserWebViewPolicy::require_cooperative_bridge(mut self, bool)-> Self","4:6":"fn BrowserWebViewPolicy::require_cooperative_bridge(mut self, bool)-> Self"},"l":{"4:5":[["webview.rs",696,701]],"4:6":[["webview.rs",696,701]]}},{"k":"fn:BrowserWebViewPolicy::strict","d":{"4:5":"Start with Kael's restrictive browser policy.","4:6":"Start with Kael's restrictive browser policy."},"s":{"4:5":"fn BrowserWebViewPolicy::strict()-> Self","4:6":"fn BrowserWebViewPolicy::strict()-> Self"},"l":{"4:5":[["webview.rs",633,636]],"4:6":[["webview.rs",633,636]]}},{"k":"fn:BrowserWebViewPolicy::trusted_same_origin","d":{"4:5":"Preserve origin storage for trusted hosted content.","4:6":"Preserve origin storage for trusted hosted content."},"s":{"4:5":"fn BrowserWebViewPolicy::trusted_same_origin(mut self)-> Self","4:6":"fn BrowserWebViewPolicy::trusted_same_origin(mut self)-> Self"},"l":{"4:5":[["webview.rs",638,642]],"4:6":[["webview.rs",638,642]]}},{"k":"fn:BrowserWebViewPolicy::unrestricted","d":{"4:5":"Remove iframe sandboxing for app-controlled content.","4:6":"Remove iframe sandboxing for app-controlled content."},"s":{"4:5":"fn BrowserWebViewPolicy::unrestricted(mut self)-> Self","4:6":"fn BrowserWebViewPolicy::unrestricted(mut self)-> Self"},"l":{"4:5":[["webview.rs",644,648]],"4:6":[["webview.rs",644,648]]}},{"k":"fn:Button::checked","d":{"4:3":"Mark a checkbox-, switch-, or radio-like button checked or unchecked.","4:4":"Mark a checkbox-, switch-, or radio-like button checked or unchecked.","4:5":"Mark a checkbox-, switch-, or radio-like button checked or unchecked.","4:6":"Mark a checkbox-, switch-, or radio-like button checked or unchecked."},"s":{"4:3":"fn Button::checked(mut self, bool)-> Self","4:4":"fn Button::checked(mut self, bool)-> Self","4:5":"fn Button::checked(mut self, bool)-> Self","4:6":"fn Button::checked(mut self, bool)-> Self"},"l":{"4:3":[["elements/button.rs",106,110]],"4:4":[["elements/button.rs",106,110]],"4:5":[["elements/button.rs",110,114]],"4:6":[["elements/button.rs",110,114]]}},{"k":"fn:Button::custom_focus_style","d":{"4:5":"Let the caller-owned renderer provide the visible keyboard focus treatment.","4:6":"Let the caller-owned renderer provide the visible keyboard focus treatment."},"s":{"4:5":"fn Button::custom_focus_style(mut self)-> Self","4:6":"fn Button::custom_focus_style(mut self)-> Self"},"l":{"4:5":[["elements/button.rs",134,138]],"4:6":[["elements/button.rs",134,138]]}},{"k":"fn:Button::description","d":{"4:3":"Set a longer accessible description for the control.","4:4":"Set a longer accessible description for the control.","4:5":"Set a longer accessible description for the control.","4:6":"Set a longer accessible description for the control."},"s":{"4:3":"fn Button::description(mut self, impl Into < SharedString >)-> Self","4:4":"fn Button::description(mut self, impl Into < SharedString >)-> Self","4:5":"fn Button::description(mut self, impl Into < SharedString >)-> Self","4:6":"fn Button::description(mut self, impl Into < SharedString >)-> Self"},"l":{"4:3":[["elements/button.rs",94,98]],"4:4":[["elements/button.rs",94,98]],"4:5":[["elements/button.rs",98,102]],"4:6":[["elements/button.rs",98,102]]}},{"k":"fn:Button::disabled","d":{"4:0":"Disable the button.","4:1":"Disable the button.","4:2":"Disable the button.","4:3":"Disable the button.","4:4":"Disable the button.","4:5":"Disable the button.","4:6":"Disable the button."},"s":{"4:0":"fn Button::disabled(mut self)-> Self","4:1":"fn Button::disabled(mut self)-> Self","4:2":"fn Button::disabled(mut self)-> Self","4:3":"fn Button::disabled(mut self)-> Self","4:4":"fn Button::disabled(mut self)-> Self","4:5":"fn Button::disabled(mut self)-> Self","4:6":"fn Button::disabled(mut self)-> Self"},"l":{"4:0":[["elements/button.rs",55,59]],"4:1":[["elements/button.rs",55,59]],"4:2":[["elements/button.rs",55,59]],"4:3":[["elements/button.rs",100,104]],"4:4":[["elements/button.rs",100,104]],"4:5":[["elements/button.rs",104,108]],"4:6":[["elements/button.rs",104,108]]}},{"k":"fn:Button::full_width","d":{"4:5":"Stretch the semantic button root across the available horizontal space.","4:6":"Stretch the semantic button root across the available horizontal space."},"s":{"4:5":"fn Button::full_width(mut self)-> Self","4:6":"fn Button::full_width(mut self)-> Self"},"l":{"4:5":[["elements/button.rs",128,132]],"4:6":[["elements/button.rs",128,132]]}},{"k":"fn:Button::label","d":{"4:0":"Set the visible label for the button.","4:1":"Set the visible label for the button.","4:2":"Set the visible label for the button.","4:3":"Set the visible label for the button.","4:4":"Set the visible label for the button.","4:5":"Set the visible label for the button.","4:6":"Set the visible label for the button."},"s":{"4:0":"fn Button::label(mut self, impl Into < SharedString >)-> Self","4:1":"fn Button::label(mut self, impl Into < SharedString >)-> Self","4:2":"fn Button::label(mut self, impl Into < SharedString >)-> Self","4:3":"fn Button::label(mut self, impl Into < SharedString >)-> Self","4:4":"fn Button::label(mut self, impl Into < SharedString >)-> Self","4:5":"fn Button::label(mut self, impl Into < SharedString >)-> Self","4:6":"fn Button::label(mut self, impl Into < SharedString >)-> Self"},"l":{"4:0":[["elements/button.rs",49,53]],"4:1":[["elements/button.rs",49,53]],"4:2":[["elements/button.rs",49,53]],"4:3":[["elements/button.rs",88,92]],"4:4":[["elements/button.rs",88,92]],"4:5":[["elements/button.rs",92,96]],"4:6":[["elements/button.rs",92,96]]}},{"k":"fn:Button::on_click","d":{"4:0":"Register a callback invoked when the button is clicked by mouse or keyboard.","4:1":"Register a callback invoked when the button is clicked by mouse or keyboard.","4:2":"Register a callback invoked when the button is clicked by mouse or keyboard.","4:3":"Register a callback invoked when the button is clicked by mouse or keyboard.","4:4":"Register a callback invoked when the button is clicked by mouse or keyboard.","4:5":"Register a callback invoked when the button is clicked by mouse or keyboard.","4:6":"Register a callback invoked when the button is clicked by mouse or keyboard."},"s":{"4:0":"fn Button::on_click(mut self, impl Fn (& crate :: ClickEvent , & mut Window , & mut App) + 'static)-> Self","4:1":"fn Button::on_click(mut self, impl Fn (& crate :: ClickEvent , & mut Window , & mut App) + 'static)-> Self","4:2":"fn Button::on_click(mut self, impl Fn (& crate :: ClickEvent , & mut Window , & mut App) + 'static)-> Self","4:3":"fn Button::on_click(mut self, impl Fn (& crate :: ClickEvent , & mut Window , & mut App) + 'static)-> Self","4:4":"fn Button::on_click(mut self, impl Fn (& crate :: ClickEvent , & mut Window , & mut App) + 'static)-> Self","4:5":"fn Button::on_click(mut self, impl Fn (& crate :: ClickEvent , & mut Window , & mut App) + 'static)-> Self","4:6":"fn Button::on_click(mut self, impl Fn (& crate :: ClickEvent , & mut Window , & mut App) + 'static)-> Self"},"l":{"4:0":[["elements/button.rs",61,68]],"4:1":[["elements/button.rs",61,68]],"4:2":[["elements/button.rs",61,68]],"4:3":[["elements/button.rs",124,131]],"4:4":[["elements/button.rs",124,131]],"4:5":[["elements/button.rs",140,147]],"4:6":[["elements/button.rs",140,147]]}},{"k":"fn:Button::pressed","d":{"4:3":"Mark a toggle button pressed or released.","4:4":"Mark a toggle button pressed or released.","4:5":"Mark a toggle button pressed or released.","4:6":"Mark a toggle button pressed or released."},"s":{"4:3":"fn Button::pressed(mut self, bool)-> Self","4:4":"fn Button::pressed(mut self, bool)-> Self","4:5":"fn Button::pressed(mut self, bool)-> Self","4:6":"fn Button::pressed(mut self, bool)-> Self"},"l":{"4:3":[["elements/button.rs",118,122]],"4:4":[["elements/button.rs",118,122]],"4:5":[["elements/button.rs",122,126]],"4:6":[["elements/button.rs",122,126]]}},{"k":"fn:Button::render_with","d":{"4:0":"Render the button body with caller-owned visuals.","4:1":"Render the button body with caller-owned visuals.","4:2":"Render the button body with caller-owned visuals.","4:3":"Render the button body with caller-owned visuals.","4:4":"Render the button body with caller-owned visuals.","4:5":"Render the button body with caller-owned visuals.","4:6":"Render the button body with caller-owned visuals."},"s":{"4:0":"fn Button::render_with(mut self, impl Fn (ButtonRenderState , & Window , & App) -> AnyElement + 'static)-> Self","4:1":"fn Button::render_with(mut self, impl Fn (ButtonRenderState , & Window , & App) -> AnyElement + 'static)-> Self","4:2":"fn Button::render_with(mut self, impl Fn (ButtonRenderState , & Window , & App) -> AnyElement + 'static)-> Self","4:3":"fn Button::render_with(mut self, impl Fn (ButtonRenderState , & Window , & App) -> AnyElement + 'static)-> Self","4:4":"fn Button::render_with(mut self, impl Fn (ButtonRenderState , & Window , & App) -> AnyElement + 'static)-> Self","4:5":"fn Button::render_with(mut self, impl Fn (ButtonRenderState , & Window , & App) -> AnyElement + 'static)-> Self","4:6":"fn Button::render_with(mut self, impl Fn (ButtonRenderState , & Window , & App) -> AnyElement + 'static)-> Self"},"l":{"4:0":[["elements/button.rs",70,77]],"4:1":[["elements/button.rs",70,77]],"4:2":[["elements/button.rs",70,77]],"4:3":[["elements/button.rs",133,140]],"4:4":[["elements/button.rs",133,140]],"4:5":[["elements/button.rs",149,156]],"4:6":[["elements/button.rs",149,156]]}},{"k":"fn:Button::role","d":{"4:3":"Override the semantic role for button-like controls such as links or tabs.","4:4":"Override the semantic role for button-like controls such as links or tabs.","4:5":"Override the semantic role for button-like controls such as links or tabs.","4:6":"Override the semantic role for button-like controls such as links or tabs."},"s":{"4:3":"fn Button::role(mut self, AccessibilityRole)-> Self","4:4":"fn Button::role(mut self, AccessibilityRole)-> Self","4:5":"fn Button::role(mut self, AccessibilityRole)-> Self","4:6":"fn Button::role(mut self, AccessibilityRole)-> Self"},"l":{"4:3":[["elements/button.rs",82,86]],"4:4":[["elements/button.rs",82,86]],"4:5":[["elements/button.rs",86,90]],"4:6":[["elements/button.rs",86,90]]}},{"k":"fn:Button::selected","d":{"4:3":"Mark a tab- or option-like button selected or unselected.","4:4":"Mark a tab- or option-like button selected or unselected.","4:5":"Mark a tab- or option-like button selected or unselected.","4:6":"Mark a tab- or option-like button selected or unselected."},"s":{"4:3":"fn Button::selected(mut self, bool)-> Self","4:4":"fn Button::selected(mut self, bool)-> Self","4:5":"fn Button::selected(mut self, bool)-> Self","4:6":"fn Button::selected(mut self, bool)-> Self"},"l":{"4:3":[["elements/button.rs",112,116]],"4:4":[["elements/button.rs",112,116]],"4:5":[["elements/button.rs",116,120]],"4:6":[["elements/button.rs",116,120]]}},{"k":"fn:ButtonRenderState::has_label","d":{"4:3":"Returns true when the button has a visible label.","4:4":"Returns true when the button has a visible label.","4:5":"Returns true when the button has a visible label.","4:6":"Returns true when the button has a visible label."},"s":{"4:3":"fn ButtonRenderState::has_label(& self)-> bool","4:4":"fn ButtonRenderState::has_label(& self)-> bool","4:5":"fn ButtonRenderState::has_label(& self)-> bool","4:6":"fn ButtonRenderState::has_label(& self)-> bool"},"l":{"4:3":[["elements/button.rs",22,25]],"4:4":[["elements/button.rs",22,25]],"4:5":[["elements/button.rs",22,25]],"4:6":[["elements/button.rs",22,25]]}},{"k":"fn:ButtonRenderState::label_len_bytes","d":{"4:3":"Length of the configured label in bytes, without exposing label text.","4:4":"Length of the configured label in bytes, without exposing label text.","4:5":"Length of the configured label in bytes, without exposing label text.","4:6":"Length of the configured label in bytes, without exposing label text."},"s":{"4:3":"fn ButtonRenderState::label_len_bytes(& self)-> usize","4:4":"fn ButtonRenderState::label_len_bytes(& self)-> usize","4:5":"fn ButtonRenderState::label_len_bytes(& self)-> usize","4:6":"fn ButtonRenderState::label_len_bytes(& self)-> usize"},"l":{"4:3":[["elements/button.rs",27,30]],"4:4":[["elements/button.rs",27,30]],"4:5":[["elements/button.rs",27,30]],"4:6":[["elements/button.rs",27,30]]}}]}